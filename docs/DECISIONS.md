> ⚠ **이 문서에 적힌 요금·기간 숫자는 작성 당시 값이다.** 현재 값의 유일한 출처는 `lib/trial.ts` 다.

# 결정 기록 (DECISIONS.md)

형식: 날짜 · 결정 · 이유. 최신이 위.

- 2026-09-08 · **CLAUDE.md 규칙 11 의 「글꼴은 Pretendard 하나다」를 개정한다 — 손님 사이트만 서체 선택 가능** · 테마 엔진(v6) 착수 지시에 따라 소상공인이 `/{상호}/edit` 에서 글씨체 5종(기본 고딕 · 명조 제목 · 손글씨 제목 · 둥근 제목 · 부드러운 전체)을 고를 수 있게 한다. **onstori.com 첫 페이지·메뉴·`/admin`·`/{상호}/edit` 세 화면은 Pretendard 고정이며 여기에 다른 서체를 넣지 않는다.** **왜 규칙을 고쳤나:** 2026-09-07 에 명조(Noto Serif KR 405KB)를 폐기한 이유는 **onstori.com 의 속도**였고(LCP 8.3→5.2초), 그 이유는 손님 사이트에 서체를 주는 것과 어긋나지 않는다. 그러나 규칙 문장이 「글꼴은 Pretendard 하나다 · `Noto Serif` 참조가 코드에 다시 생기면 안 된다」로 **범위 없이** 적혀 있어, 그대로 두면 다음 세션이 이 작업을 규칙 위반으로 보고 되돌린다. 조사 단계에서 충돌을 발견해 착수 전에 문장을 고친다(회장님 지시). **함께 정한 것:** ①서체 파일은 자체 호스팅하되 **저장소가 아니라 R2** 에 둔다 — 저장소에 넣으면 Vercel 배포 스토리지가 또 찬다(2026-09-07 100% 도달). 구글 폰트 CDN 은 되살리지 않는다(Pretendard 자체 호스팅으로 일부러 끊은 사슬이다). ②Pretendard 와 같은 **unicode-range 동적 서브셋** 방식이라 새 글자가 나와도 깨지지 않아야 한다. ③시안에 있던 여섯 번째 서체 `serif-all`(명조 전체)은 **뺀다** — 5개다. **단일 출처는 `config/design.ts`**, 사이트별 저장은 `SiteDoc.theme.font`. ⚠ 규칙 11 의 나머지(토큰만 쓰기 · 하드코딩 금지 · 굵기 4단계 · 대비 기준 · 모션 허용목록)는 그대로다

- 2026-09-06 · **[최종 확정] 무료 30일 · 정지 후 60일 삭제 · 손님 문의 60일** · 같은 날 앞서 정한 "무료 14일 → D+28 자동 삭제"를 **대체한다**. 확정 숫자: **무료 체험 30일**(전 기능, 31일째 정지) → **정지**(비공개·검색 제외, 자료 보관, 사장님이 들어오면 "결제하면 바로 다시 켜집니다" 화면) → **정지 후 60일** 사장님 콘텐츠 영구 삭제 + 손님 견적 문의 파기. 총 수명 30+60=90일. 삭제 예고는 **30일 전·7일 전** 문자. **단일 출처는 `lib/trial.ts`** — `TRIAL_DAYS(30)` / `DELETE_AFTER_SUSPEND_DAYS(60)` / `INQUIRY_RETENTION_DAYS(60)`. SUSPEND_DAYS 는 TRIAL_DAYS 와 같은 값이라 중복 상수를 만들지 않았다. **왜 정지일 기준인가:** 삭제 기한을 가입일에서 파생하면 결제 실패로 오늘 정지된 사장님이 가입이 오래됐다는 이유로 곧바로 삭제 대상이 된다 — `sites.suspended_at` 컬럼을 새로 만들어(마이그레이션 `20260906140000`) 정지 순간을 기록한다. **마케팅 분리:** 화면·문자·콜 스크립트에는 "30일 무료"만 쓰고 60일 유예는 약관·개인정보처리방침에만 적는다(회장님 지시) — `lib/trial.ts` 가 화면용 `COPY` 와 법무용 `LEGAL` 을 따로 내보낸다. **손님 문의는 두 기준 중 먼저 오는 때에 파기한다** — 접수일 1년(견적 폼 동의 문구가 약속한 값) 또는 정지일 60일. 손님 개인정보는 오래 갖는 것 자체가 위험해서 사장님 콘텐츠와 분리했다. **비용 안전장치:** 무료 기간이 2배가 되어 `MONTHLY_SITE_CAP`(월 300, env 로 조정)을 `api/generate` 에 조용히 걸었다 — 상한값·잔여수를 화면에 표시하지 않는다. ⚠ 이전 확정 "14일 지나도 절대 삭제 금지"(`docs/admin.md:99`)와 "14일 이후 자동 삭제 문구 7곳 유지"(`docs/admin.md:131`)는 **이 결정으로 폐기**

- 2026-09-06 · **요금 모델 확정 — 월 49,000원 구독, D+14 정지, D+28 자동 삭제** · 종전 "정회원 49,000원 **1회** 결제 + 월 9,900원 유지비 검토 중 + 30일 뒤 사람이 삭제"를 폐기하고 다음으로 확정한다. **① 요금은 월 49,000원 구독**(빌링키 정기결제, 매달 자동 결제) — 홈페이지를 유지하는 동안 계속 청구된다. **② D0 가입·14일 무료 → D+14 미결제 시 정지**(비공개, 자료는 보관) **→ D+14~28 유예 14일**(결제하면 즉시 복구) **→ D+28 자동 삭제**(삭제 전 문자 예고). 값의 **단일 출처는 `lib/trial.ts`**(TRIAL_DAYS·GRACE_DAYS·DELETE_DAYS·MEMBERSHIP_PRICE·COPY) — CLAUDE.md 규칙 9 로 못 박았다. **왜 단일 출처가 필요했나:** 전수 조사 결과 저장소 42개 파일에 요금·기간 진술이 **182곳** 흩어져 있었고 그중 **175곳이 확정안과 어긋났다**(손님·사장님 화면 53곳 포함). 대표적으로 `pay-modal.tsx` 에는 `lib/trial.ts` 의 `TRIAL_NOTICE` 와 **똑같은 문장이 통째로 복사**돼 있었고, 그 사본은 정지·유예 단계가 빠져 "14일 이후 자동 삭제"라고 **손님에게 사실과 다른 말**을 하고 있었다. 문장을 복사하는 방식으로는 정책이 바뀔 때마다 반드시 어긋난다. **③ 코드 영향:** 기존 `api/billing/*` 은 토스 **일반결제(1회성)** 로 짜여 있었고(2026-09-05 `16af5cb`, 기술참모 배치본 · 미검토) 그 계약조차 하지 않았다 — 빌링키 정기결제로 다시 짰다(`lib/toss.ts`·`api/billing/subscribe`·`cancel`·크론 청구, 마이그레이션 `20260906120000`). 유예·자동삭제·삭제 예고도 크론에 없어 새로 넣었다. ⚠ 토스 가맹 심사(2~3주) 전이라 **정기결제는 실호출로 검증한 적이 없다**

- 2026-09-11 · **통신판매업신고번호 발급 — 제2026-다산-0825호.** `config/company.ts` 의 `mailOrderNo` 한 줄을 교체했다(임시 문구 「신고 진행 중」 제거). 그 한 줄이 **네 자리**를 동시에 바꾼다: 개인정보처리방침 §1 · 이용약관 제1조 · 본사 푸터 · **결제 모달(이번에 신설)**. ★ 저장소를 훑다가 **결제 모달에 판매자 신원이 아예 없다**는 것을 발견했다 — 전자상거래법 제13조는 계약 «체결 전»에 판매자 신원·거래조건을 알리라고 하는데, 본사 푸터에는 있어도 그 모달이 뜨는 `/my` 와 `/{slug}/edit` 두 화면에는 **푸터가 없었다.** 상호·대표·사업자등록번호·통신판매업신고·전화 + 이용약관·개인정보처리방침 링크(새 탭)를 결제 버튼 아래에 넣었다. ⚠ **손님 사이트(`/{slug}`)에는 넣지 않는다** — 그 홈페이지의 사업자는 사장님이지 이안월드가 아니다(손님 사이트는 `components/sections/site-chrome.tsx` 로 본사 푸터와 분리돼 있음을 확인). ⚠ **남은 것 둘:** ①번호의 지자체 표기가 「다산」인지 「경기남양주」인지 **신고증 원본 대조 필요**(주소를 등록증에 맞춘 것과 같은 이유 — 법정 표시 항목은 원본과 글자가 같아야 한다) ②**구매안전서비스(KB에스크로) 표시가 화면 어디에도 없다** — 선불식 통신판매는 결제대금예치 사실을 표시해야 한다. 확인증은 2026-09-03 등록돼 있으나 게시가 빠져 있다
- 2026-09-06 · **법무 페이지를 "읽기 쉬운 요약"에서 "법정 기재사항을 다 갖춘 문서"로 올린다** — 1차 초안은 쉬운 말로 잘 쓰였지만 법이 열거한 필수 항목이 여러 개 비어 있었다. 개인정보보호법 제30조·제26조·제28조의8, 전자상거래법 제10조·제17조, 약관규제법 제7조의 **각호를 한 줄씩 대조**해 채웠다. 빠져 있던 것 중 큰 것: ①**권익침해 구제 4개 기관**(분쟁조정위·침해신고센터·대검·경찰) 통째로 없었음 — 시행령 제31조 필수라 심사에서 바로 걸린다 ②**파기 절차·방법** 없었음 ③**전화번호**가 방침·약관·푸터 어디에도 없었음(전자상거래법 제10조 필수, 토스 가맹 직결) ④**국외이전**이 "국외" 한 글자뿐 — 국가·항목·시기·방법·보유기간·거부절차를 채웠다 ⑤**쿠키 거부 방법**, **열람청구 접수 부서**, **만 14세 미만 미수집**, **자동화된 결정 해당없음**. 약관에서는 ⑥**책임제한에 고의·중과실 예외가 없어** 약관규제법 제7조로 조항 전체가 무효가 될 소지가 있었고 ⑦**손님 문의에 대한 위탁 문서**(제26조 제1항 7개 항목)가 없었다 — 손님 정보의 처리자는 사장님이고 온스토리는 수탁자인데, 그 문서가 없으면 사장님이 위법 상태가 된다. 제7조를 그 문서로 다시 썼다. **가장 위험했던 것은 ⑧약속과 실제의 불일치** — 문의 폼이 손님에게 "1년 뒤 삭제합니다"라고 동의를 받는데 그 삭제 코드가 어디에도 없었다(크론은 만료 처리 하나뿐). `/api/cron/expire` 에 3단계를 추가해 실제로 파기하게 만들었다. 사업자 정보는 `config/company.ts` 단일 출처로 모아, 통신판매업신고번호가 나오면 한 줄만 고치면 세 화면이 함께 바뀐다. ⚠ **변호사 검토는 여전히 안 받았다** — 열거된 항목은 전수 대조했으나 해석·판단이 필요한 부분(환불 산정 기준, 위탁 범위)은 남아 있다

- 2026-09-06 · **후기·실적 표기 방침 개정(CLAUDE.md 규칙 7)** — 종전 "후기 별점 없음 + 실적 카운터는 `story_entries` 실데이터 집계만"에서, **회원(사장님)이 직접 입력한 후기·시공 건수는 표시하고 게시 내용의 책임은 회원에게 둔다**로 바꿨다. 우리 코드·AI가 자동 생성하거나 부풀리는 것은 계속 금지. 예외로 **샘플 사이트(slug `sample`)에 한해** 예시 후기·예시 시공 건수를 넣되 후기 옆에 "예시" 표시를 달고, 실고객 사례로 교체할 때 표시를 뗀다. **별점 UI는 현행 유지(만들지 않는다).** 이유: 후기 0건인 빈 화면으로는 콜드콜 시연에서 제품을 보여줄 수 없는데, 우리가 지어내는 것과 사장님이 직접 올리는 것은 표시광고법상 책임 주체가 다르다 — 회원 게시물로 성격을 분명히 하고 이용약관 제7조에 "직접 올리신 내용의 책임은 사장님께 있다"를 명시했다. 샘플만 예외로 두는 이유는 실고객 사이트에 예시가 섞이면 구분이 무너지기 때문

- 2026-09-06 · **법무 페이지를 신고번호 없이 공개한다** — `/privacy`·`/terms` 의 빈칸 3개 중 사업자등록번호(`139-24-01865`)·주소(경기도 남양주시 다산순환로 397-80, 401)를 채우고, 통신판매업신고번호 자리는 **"신고 진행 중 (발급 후 게시)"** 임시 문구로 두고 `noindex`·푸터 링크·sitemap 보류를 전부 되돌렸다. 이유: Meta 앱 심사와 토스 가맹이 **개인정보처리방침 공개 URL** 을 요구하는데, 신고번호 발급을 기다리면 그 둘이 함께 멈춘다. 신고번호는 발급되는 대로 한 줄 교체

- 2026-09-06 · **크론을 연다 — `CRON_SECRET` 을 Vercel Production 에 등록** · 만료 처리와 D-3·D-1 안내 문자가 여기에 걸려 있었다. 없는 동안 `/api/cron/expire` 가 401 로 닫혀 있어서, **만료돼도 손님에게는 홈페이지가 그대로 보였다** — 에디터 차단 화면의 "지금 비공개 상태입니다" 문구가 사실이 아니었다(만료 판정은 날짜 기반이라 에디터만 즉시 잠긴다). 등록 후 테스트 사이트로 체인 전체를 실증했다: `trial_ends_at` 과거 → 크론 `{"expired":1}` → `status='expired'` → 공개 페이지 **404**(`lib/sites.ts` 가 anon 클라이언트로 읽고 RLS `sites_public_read` 가 `trial`·`active` 만 허용) → 에디터는 200 유지. **부작용 둘:** ①크론이 매일 03:00 KST 에 돌며 **D-3·D-1 문자를 솔라피로 실제 발송**한다(실측 155바이트 안팎 → 전부 LMS 구간, 단문 대비 약 3배 요금 — 문구를 90바이트 이하로 줄이면 SMS 로 떨어진다) ②`seeds/*.json` 에 같은 slug 가 있는 사이트는 파일 폴백 때문에 만료로도 막히지 않는다(현재 cafecroft·cleanhaus·niv, 실고객 해당 없음)

- 2026-09-05 · **플로팅 연결 위젯은 값을 갖지 않는다** — 전화·카톡은 `quoteForm` 섹션에서 파생한다(`contactOf`) · 사본을 늘리지 않기 위한 결정. 같은 번호가 두 곳에 살면 한쪽만 고쳐지는 순간 죽은 `tel:` 링크가 생긴다. 위젯이 갖는 것은 `kind`·`label` 뿐이고, 값이 없는 종류는 아예 렌더하지 않는다. `SiteDoc.widgets` 는 **optional** 이다 — 필수로 두면 `lib/sites.ts` 의 `SiteDoc.parse`(safeParse 아님)에서 기존 발행본이 전부 죽는다(실사이트 18건+시드 3건으로 확인). 렌더 마운트는 **공개·미리보기 두 셸 모두**(`app/[slug]/page.tsx` · `app/[slug]/preview/preview-client.tsx`) — 한 곳만 넣으면 갈라진다(로고 오버레이가 이미 그렇게 갈라져 있다). 마운트는 `</footer>` **뒤**여야 한다: 앞에 두면 자리 확보용 스페이서가 마지막 섹션과 footer 사이만 벌려서, 맨 아래로 스크롤했을 때 고정 바가 footer 를 덮는다(실측 −72px → 뒤로 옮겨 +20px)

- 2026-09-05 · 전화번호 판정 단일화(`lib/phone.ts`, 숫자 9자리) — 온보딩·생성 서버·렌더·완성도 네 곳이 같은 기준을 쓴다 · 기준이 네 지점에서 제각각이었고 서버(`z.string().min(9)`)가 **글자 수**만 봐서 클라이언트보다 약했다 — 브라우저를 안 쓰는 요청 하나면 안내 문구가 `quoteForm.phone`에 그대로 박혔고, 렌더러는 그 값으로 죽은 `tel:` 링크를 만들었다. 형식(하이픈·국가번호)은 강제하지 않고 링크만 정규화한다. **부작용: 번호가 안내 문구인 기존 사이트는 contact 10점이 빠지고 히어로 CTA 가 `#quote` 로 바뀐다** — 저장된 값은 건드리지 않았고, 그 사장님 에디터에 "＋10점 · 전화·주소 확인" 힌트가 떠서 스스로 고칠 동선이 생긴다. 버그가 아니라 규칙이 정직해진 결과다
- 2026-09-05 · 카카오 개발자 콘솔에서 앱(Onstori)의 **카카오맵/로컬(`OPEN_MAP_AND_LOCAL`) 서비스를 켜기로** 결정 — 대시보드에서만 가능한 설정이라 여기 기록(불변규칙 1) · 온보딩 1단계 "플레이스에서 불러오기"의 카카오 채널이 죽어 있었다: 로그인용으로 이미 있는 `KAKAO_REST_API_KEY`로 `dapi.kakao.com/v2/local/search/keyword`를 부르면 `403 NotAuthorizedError — App(Onstori) disabled OPEN_MAP_AND_LOCAL service`가 온다(2026-09-05 실호출 확인). 로그인 스코프와 **별개 설정**이라 카카오 로그인이 멀쩡해도 로컬 API만 막힌다. `app/api/place-search/route.ts`가 `if (!r.ok) return []`로 삼켜 500도 콘솔 에러도 남지 않아 눈에 띄지 않았다. 네이버(NAVER API HUB)만으로도 검색은 되지만 라우트가 두 채널을 병합·중복제거하도록 이미 짜여 있어, 카카오는 **네이버 장애·월 한도(검색 API 합산 775,000회) 소진 시의 예비 채널**로 값이 있다. **⏳ 켜는 작업은 사장님이 직접 — 아직 적용 전이다.** 켠 뒤 `curl "https://onstori.com/api/place-search?q=<상호>"` 응답에 `"source":"kakao"` 항목이 섞이면 반영된 것
- 2026-09-04 · [정정] `goodmoksu`·`whitedobae` 는 **실제 고객이 아니라 사장님이 만든 테스트 사이트**다 — 문서 4곳이 이 둘을 '실제 고객'으로 적고 있었고 전부 '테스트 사이트'로 고쳤다(DECISIONS·PROGRESS·sprint-plan·presale) · 특히 `docs/presale.md` 의 당근 홍보 문구가 "완성본 예시: onstori.com/goodmoksu (실제 고객 사이트)"였다 — 아직 실고객이 0인데 있다고 말하는 셈이라 불변규칙 7(표시광고법)에 걸린다. **현재 실고객 0** 이라는 사실은 선판매 게이트·P5 진입 판단의 전제이므로 여기 남긴다. 사이트와 그 데이터는 그대로 두었다(표기만 정정)
- 2026-09-03 · P5(결제) 진입 조건을 선판매 게이트(사진 수신 5건→유료 전환 30%)에서 **문의 접수(DB+알림)·에디터 라이브 미리보기·플로팅 연결 위젯 3개가 프로덕션에서 동작 + 실제 사장님에게 시연**하는 것으로 변경 · 무료 체험 한 달 안에 사장님이 "문의가 왔다"를 경험하지 못하면 결제 시스템이 완벽해도 전환율은 0에 가깝다 — 결제보다 가치 체감이 먼저다. 선판매 게이트는 폐기가 아니라 이 3개 위에서 진행한다(`docs/specs/2026-09-03-sprint-plan.md`)
- 2026-09-03 · P5 진입 3조건의 작업 순서를 **R2-1(저장소 추상화) → 견적 접수(G1→마이그레이션→G2) → 에디터 라이브 미리보기 → 플로팅 위젯 → R2-2(백필)** 로 확정하고 **worktree 병렬 없이 단일 터미널 순차 진행** 하기로 결정 · 처음 시도하는 조합이라 여러 작업을 병렬로 벌이면 병합·마이그레이션 순서 리스크가 있다 — 순차 진행으로 그 리스크를 피한다(`docs/specs/2026-09-03-sprint-plan.md` 2절)
- 2026-09-03 · 견적 접수(문의)는 `QuoteForm`의 zod 스키마를 바꾸지 않고 **컴포넌트 교체**(`tel:` 링크 카드 → 실제 폼)로 구현, 첨부 사진은 **비공개 버킷**(signed URL 10분)에 저장, 알림(솔라피 SMS·Resend 이메일)은 **env가 없으면 해당 채널을 건너뛴다**(throw 금지) · 스키마 변경 없이 배포 리스크를 줄이고, 사진은 개인정보라 비공개가 기본이며, 알림 채널 미설정이 접수 자체를 막아서는 안 된다(`docs/specs/inquiry.md`)
- 2026-09-03 · 이미지·사진(향후 영상 포함) 저장소를 **Supabase Storage → Cloudflare R2**로 이전(DB·Auth·RLS는 Supabase 유지) · Supabase Free는 전송량 5GB/월, Pro($25)도 250GB/월 이후 과금인데 R2는 저장만 과금이고 전송량은 무료다 — 사진·영상이 늘고 방문이 몰릴수록 차이가 커진다. 두 단계로 진행: **R2-1**(저장 계층 추상화 `lib/storage.ts` + 신규 업로드 전부 R2)을 견적 접수보다 **먼저**, **R2-2**(기존 뱅크 638장 백필)는 견적 접수 라이브 **후**(`docs/specs/storage-r2.md`)
- 2026-09-03 · 쇼케이스 랜딩 캐러셀에서 니브인테리어·클린하우스·카페크로프트(가짜 경력·후기·시공사례가 담긴 데모 시드) 3건을 제외, 테스트 사이트 goodmoksu·whitedobae 2건만 남김 · 가짜 경력·후기가 실제 시공 사례처럼 노출되고 있었다. (2026-09-04 정정: 이 두 사이트를 '실제 고객'으로 적었으나 둘 다 사장님이 만든 테스트 사이트다) `showcase` 테이블 행만 삭제, `seeds/*.json` 콘텐츠 파일은 그대로 둠(추후 필요하면 재등록 가능)
- 2026-09-03 · 발행 사이트 3곳(whitedobae·mong-filates·testtesttest)의 진행 과정 카피에 있던 근거 없는 경력 표현("오랜 노하우를 바탕으로" 등)을 제거하고, 카피 프롬프트의 날조 금지 규칙을 숫자 없는 경력·숙련도 암시 표현까지 확대(`lib/generate.ts`) · CLAUDE.md 불변규칙 7(표시광고법) 위반을 실사이트 감사 중 발견. draft+published 동시 수정, 이전 published는 site_versions에 스냅샷. 폴백 모델 생성물을 사후 식별하도록 `inferred.copyModel` 기록 추가(`lib/gemini.ts`)
- 2026-09-02 · WIF 마무리를 **①(Vercel 키 env 제거)만 하고 ②③(키 삭제·정책 예외 해제)은 며칠 관찰 후**로 나눔 · 세 단계의 되돌리기 난이도가 다르다. ③은 새 키 발급 자체를 막아 **되돌릴 문을 닫는** 유일한 지점이므로 마지막에 둔다. ②③을 미뤄도 남은 키는 Vercel 에서 빠져 어디서도 안 쓰이므로 손해가 거의 없다. 인증 판정은 람다당 캐시되므로 관찰은 한 번이 아니라 여러 번·콜드스타트를 섞어서 한다

- 2026-09-02 · Vercel→Vertex 인증을 **WIF(Workload Identity Federation)**로 전환 · 장기 SA 키를 없애기 위함. `lib/vertex.ts`가 WIF → SA JSON → ADC 3분기로 하나의 `AuthClient`에 수렴한다. audience 는 **Allowed audiences**(`https://vercel.com/ianworld`) 방식 — 코드에서 audience 를 넘길 필요가 없어 어긋날 자리가 하나 줄고 env 도 하나 적다. WIF 풀/프로바이더는 둘 다 `vercel`, issuer `https://oidc.vercel.com/ianworld`, SA 가장은 subject 정확 일치로만 허용. 프로바이더에 attribute-condition 은 걸지 않았다 — issuer 가 이미 팀 한정이고 SA 바인딩이 subject 정확 일치라 같은 제약이 세 겹이 되는데, STS 오류가 불친절해 디버깅 면이 늘어나는 손해가 더 크다. **SA JSON 분기는 롤백용으로 남긴다**

- 2026-09-02 · `/api/generate` 요청 제한 카운터를 **Postgres에 둔다**(인메모리 아님) · Vercel 서버리스는 요청마다 다른 인스턴스일 수 있어 인메모리로는 막히지 않는다. 증가·판정은 `rate_limit_hit` 함수가 upsert 한 번으로 원자적으로 처리(동시 10회 max=3 실측에서 정확히 3개 통과). 한도 IP당 1시간 5회·24시간 20회 — CGNAT 공유를 감안한 값. **판정 실패 시 통과시킨다**: 카운터가 죽었다고 정상 사장님의 생성을 막는 손해가 더 크다

- 2026-09-01 · 로그에 노출된 서비스 계정 키를 **교체**하고 `.env.local`에서는 **아예 제거** · 노출 이력이 있는 장기 자격증명은 폐기가 원칙. 신 키 `520195620d…` 발급 → Vercel Production 교체·Redeploy → **구 키를 먼저 비활성화한 상태에서** 프로덕션 사이트 생성 200/6.6초 확인(비활성 키는 토큰을 발급할 수 없으므로, 이것이 "Vercel 환경변수가 실제로 갱신됐는가"를 가리는 유일한 검사다) → 구 키 `a29494b9…` 영구 삭제. 로컬은 ADC로 도는 것을 실증했으므로 `.env.local`에 장기 키를 두지 않는다 — 이번 유출도 그 파일을 읽다 났다. ⚠ **이전 배포로 롤백하면 Redeploy 필수** — 그 배포엔 삭제된 구 키가 주입돼 있다

- 2026-09-01 · [중대] 카카오 로그인을 **Supabase 프로바이더 → 카카오 OIDC 직결**로 변경 · Supabase(GoTrue)의 카카오 프로바이더는 scope에 `account_email`이 하드코딩돼 있고 요청 scope는 **덧붙기만** 된다(실측: `scopes=profile_nickname`을 보내면 `account_email profile_image profile_nickname profile_nickname`으로 나감). 이메일 동의항목은 비즈 앱 전환 없이는 못 켜서 카카오가 **KOE205**로 막았다. 그래서 `/auth/kakao`가 카카오 authorize를 직접 열어 `openid profile_nickname`만 요청하고, `/auth/callback`이 code→id_token 교환 후 `signInWithIdToken({provider:'kakao'})`로 세션을 만든다(`lib/kakao.ts`). ⚠ **Supabase의 카카오 프로바이더는 계속 켜둬야 한다** — id_token의 `aud`를 프로바이더 Client ID와 대조하기 때문. state는 httpOnly 쿠키로 검증, nonce는 요청하지 않는다. 새 환경변수 `KAKAO_REST_API_KEY`·`KAKAO_CLIENT_SECRET`

- 2026-09-01 · Vercel→Vertex 인증용 **서비스 계정 키를 임시로 발급**하고 조직 정책(`iam.disableServiceAccountKeyCreation`, 상위 조직 상속·적용 중) 예외를 프로젝트 단위로 사용 · 오늘 배포를 막지 않기 위한 한시 조치. **WIF 전환 시 이 키는 폐기 예정** — 전환 완료 시 ① 정책 예외 해제 ② 키 삭제 ③ Vercel `GOOGLE_SERVICE_ACCOUNT_JSON` 제거. 백로그는 PROGRESS "P5 진입 전 검토" 항목(예상 2~3시간)

- 2026-09-01 · [실측 확정] 이미지 모델 **역할별 병행**: 히어로=`gemini-3-pro-image`, 그 외(갤러리·시공사례·about·process)=`gemini-3.1-flash-image` · 동일 프롬프트 히어로 5쌍 + 갤러리 1쌍 실측 결과. **Pro는 화면 한쪽을 비우는 구도**를 반복 생성 — 히어로엔 상호명·헤드라인이 얹히므로 여백이 곧 실용성. **Flash는 프롬프트 이행이 정확**("매입 천장등" 지시에서 조명을 주인공으로) 하고 프레임을 꽉 채워 갤러리에 적합. 설계서 2026-08-31 원안과 같은 배치이나 이번엔 근거가 있음. 비용($0.134 vs $0.039)은 히어로가 소수라 결정 변수 아님. 리포트: https://claude.ai/code/artifact/3cbb3ccb-fe41-4ce2-8fc3-7b48e1313c09
- 2026-09-01 · 로컬 Vertex 인증은 서비스 계정 키가 아니라 **ADC**(`gcloud auth application-default login`) · 장기 자격증명 파일을 만들지 않는 편이 안전. `lib/vertex.ts`는 ADC 우선, `GOOGLE_SERVICE_ACCOUNT_JSON` 있으면 그걸 사용(ADC가 안 되는 Vercel용). gcloud 바이너리가 PATH에 없어 `getProjectId()`가 실패하므로 ADC 파일의 `quota_project_id`를 폴백으로 읽음
- 2026-09-01 · [중대] AI 접근 경로를 Gemini API(`?key=`) → **Vertex AI(서비스 계정 인증)**로 이관 · 이 지역 Gemini API는 GCP 결제와 별개인 **선불 크레딧 충전**을 요구해서, 보유한 GCP 무료 체험판 이월 크레딧 ₩435,523(2026-12-01 만료)이 놀게 됨. Vertex는 일반 GCP 결제라 크레딧 소진 가능. 벤더는 여전히 Gemini 모델(2026-08-31 결정 유지) — 바뀐 건 전송·인증 계층뿐. `lib/vertex.ts` 신설, `geminiJson()` 시그니처 유지해 `lib/generate.ts`는 무수정. ~~리스크: 체험판 크레딧은 "특정 사용량에 적용"이라 Vertex 적용 여부는 첫 비용 리포트로 확인해야 함~~ → **2026-09-01 비용 리포트로 확인, 크레딧 정상 차감**(₩435,523 중 ₩25,243.93 사용, 잔액 ₩410,279). 이 결정의 전제가 실측으로 확인됐다 (docs/vertex-setup.md 7절)
- 2026-09-01 · GCP 결제 계정 무료 체험판 → 유료 계정 업그레이드 · 체험판 상태로는 유료 API 호출 자체가 불가. 크레딧 ₩435,523은 업그레이드 후에도 이월돼 2026-12-01까지 유효. 체험판 "청구 없음" 보호막이 사라지므로 예산 알림 설정 필요

- 2026-09-01 · P4 로그인 방식 = 카카오 OAuth + 이메일 6자리 OTP(매직링크 불채택) · 소상공인은 폰 메일앱에서 링크를 열면 세션이 로그인 시도한 브라우저와 분리됨 — 코드 입력은 어디서든 동작. 필요한 대시보드 설정(카카오 앱·프로바이더·Redirect URL·OTP 템플릿)은 `docs/auth-setup.md` 체크리스트로 관리
- 2026-09-01 · P4 API 구조 = service-role + 서버 세션 검증 유지(anon-key 클라이언트 데이터 접근으로 전환 안 함) · 최소 변경으로 `loadOwnedSite` 한 곳만 고쳐 게이트 API 5곳 전환. RLS는 계속 심층 방어. 세션 갱신은 미들웨어 재도입 없이(MIDDLEWARE_INVOCATION_FAILED 전력) Route Handler의 `@supabase/ssr` 서버 클라이언트가 쿠키를 다시 쓰는 방식
- 2026-09-01 · P4 소유 판정 = owner_id 있으면 세션 uid 일치 필수, 무주인(owner_id null) 사이트만 anonId 폴백 유지 · 가입 없이 만들고 바로 편집하는 기존 흐름 보존. claim(로그인 직후) 시 owner_id 부여 + **anon_id 소거**(재claim 방지·상태 명확화). 로그인 상태 생성은 처음부터 owner_id 저장
- 2026-09-01 · globals.css @import 순서 수정(Pretendard를 tailwind 앞으로) · CSS 명세상 @import는 다른 규칙보다 앞이어야 함 — Turbopack dev 서버가 이걸로 기동 실패(빌드는 통과라 그동안 안 보였음)
- 2026-09-01 · 마이그레이션은 원격 `supabase db push`로 진행 (`db reset` 로컬 검증 생략) · 작업 머신에 Docker 미설치로 로컬 Supabase 스택 실행 불가 — 적용 전 `migration list --linked`로 순서 확인, 적용 후 RPC 호출로 검증
- 2026-09-01 · [중대] 주소 체계 전면 전환: 서브도메인 → 경로 방식(onstori.com/{slug}) · 네이버 서치어드바이저는 캡차로 대량 자동 등록 불가 — 서브도메인이면 고객 수만큼 수동 등록+사이트맵 제출+~2주 수집 대기, 경로면 도메인 1회 등록+통합 사이트맵 1개+도메인 권위 승계. 서브도메인은 본사 내부 기능 전용으로 보류(어드민 '서브도메인 만들기' 자리만). 구 서브도메인 링크는 301 경로 리다이렉트. 예약 슬러그 261개 선점
- 2026-09-01 · [사실 확인] Gemini API 무료 티어의 이미지 모델 일일 쿼터 = 0 (첫 호출부터 429·FreeTier 위반 3종 동시) · Google AI Pro 구독은 Gemini 앱 혜택이지 API 쿼터와 무관 — 이미지뱅크 실생성은 API 결제 연결(종량제) 후 가능. fal.ai 불채택(사용자 결정), 나노바나나(gemini-3-pro-image 히어로 / 3.1-flash-image 갤러리 병행) 확정
- 2026-09-01 · 이미지뱅크 품질 체계: 프롬프트 조합(역할 촬영지시×업종 씬×무드×변주축) + dHash 해밍≤6 중복 차단 + quality_score 우선순위 + 어드민 검수(승인/거부/점수) — 품질은 모델보다 선별에서 나온다. 필요량의 2~3배 생성 후 선별
- 2026-09-01 · 어드민 임시 인증 = ADMIN_KEY 쿠키 (P4에서 Supabase Auth 화이트리스트로 교체)

- 2026-08-31 · AI 벤더를 Gemini 단일화(카피·분류=gemini-3.5-flash 폴백 2.5-flash, 이미지=gemini-3-pro-image/3.1-flash-image 벤치 예정) · 키 1개 관리, 계정에 3.x 세대 접근 확인. LLM엔 카피 조각만 맡기고 SiteDoc 구조는 코드 조립(안정성). 사실 날조 금지 프롬프트 규칙
- 2026-08-31 · 선판매 채널을 콜드콜→당근마켓(비즈프로필)+지인으로 변경, 오퍼="1개월 무료→월 9,900원(언제든 해지)" · 2단 게이트: ①사진 수신 5건/2주→P3 ②유료 전환 30%→P5. 체험 기간 24h→30일로 변경(정책 일치). 통신판매업 신고·토스 가맹 지금 시작 필요(1개월 뒤 첫 결제)
- 2026-08-31 · 어드민 콘솔 기획 확정(docs/admin.md) — 이미지뱅크 CRUD·신청 접수함 포함, P4 전 임시 게이트는 ADMIN_KEY

- 2026-08-31 · P1 완료: 데이터 소스는 DB 우선 + 시드 폴백(lib/sites.ts 한 곳에서 전환) · 쇼케이스 시드는 파일로 관리, 실사이트는 DB. Vercel 환경변수(NEXT_PUBLIC_SUPABASE_URL·ANON_KEY)는 대시보드에서 등록(Production) — service_role 키는 Vercel에 올리지 않음(현재 서버 코드가 미사용)

- 2026-08-31 · 서브도메인 라우팅을 미들웨어 → next.config beforeFiles rewrites(경로 allowlist)로 전환 · Vercel에서 미들웨어가 코드와 무관하게 MIDDLEWARE_INVOCATION_FAILED(최소 미들웨어로 확인). 부수 확인: lookahead 정규식 source는 Vercel 라우팅 전체를 깨뜨림 → 경로 명시 매핑만 사용. 원본 미들웨어는 debug/full-middleware 브랜치 보관
- 2026-08-31 · [해결] Vercel 전 페이지 NOT_FOUND의 진짜 원인 = 도메인(onstori.com)이 붙은 프로젝트가 onstori-pwk2였고 그 프로젝트의 Framework Preset이 "Other"(정적 배포만 생성, 빌드 12초). Next.js로 변경 + 캐시 없이 Redeploy로 해결 · 교훈: 같은 저장소로 프로젝트가 2개 생겨 있었음(onstori / onstori-pwk2) — 확인은 도메인이 붙은 쪽에서. 중복 프로젝트(onstori)는 삭제 권장

- 2026-08-31 · 빌드 게이트 도입: P2/P3 진입 전 선판매 테스트(30콜) 결과 확인 · "만들면 팔린다" 가정에 4개월을 걸지 않기 위해
- 2026-08-31 · v1 활성 범위 = 시공·출장 12업종 + 카페·식당 2업종, 템플릿 QUOTE·VISIT 2종 · 랜딩 포트폴리오 탭([전체][인테리어][시공·건설][서비스·출장][카페·식당]) 구성과 이미지 뱅크 비용 최소화
- 2026-08-31 · 온보딩에서 카테고리·템플릿을 묻지 않음(추론 파이프라인) · 카테고리는 내부 어휘이지 사장님 어휘가 아님
- 2026-08-31 · 게이미피케이션은 UI보다 뼈대 먼저: config 규칙표 + data-tour 앵커 규약을 P1부터 강제 · 앵커는 소급 불가
- 2026-08-31 · 저장소 1개 + Next.js 앱 1개 모노레포 · 1인+Claude Code 규모에서 분리는 스키마 불일치(꼬임)의 주원인
- 2026-08-31 · 사이트 = 코드가 아니라 DB의 JSON(draft/published 분리, 발행 스냅샷) · 수정·백업·롤백을 데이터 문제로 단순화
- 2026-08-31 · 주소 체계 = {slug}.onstori.com 서브도메인(경로형 아님), /my는 본사 도메인 · 차별화 + 고객의 "내 사이트" 소유감
- 2026-08-31 · Vercel Hobby·Supabase Free로 시작, 첫 유료 고객 시점에 Pro 전환 · Hobby는 약관상 비상업, Free는 자동 백업 없음(주 1회 수동 dump로 보완)
- 2026-08-31 · 이미지는 나노바나나 + 이미지 뱅크(사전 생성·검수·카탈로그), 워드마크 로고는 폰트 조판 SVG · 한글 렌더 정확성과 건당 원가 ~0원
- 2026-08-31 · 히어로 무비: 정가 199,000/프로모션 99,000/초기 10건 0원(베타·지인 대상), 수정 3회 시스템 강제, "AI 아님" 표현 금지 · 표시광고법
- 2026-08-31 · 후기 별점 기능 없음, 실적 카운터는 실데이터 집계만 · 표시광고법 + 컨셉 일치

## 2026-09-05 — 이야기 엔진 전환 · 레멘토 구조 · 14일 정회원

- **포지셔닝**: 홈페이지 빌더 → "사장님 이야기 엔진". 글쓰기 금지 · 문자 링크만(카톡 로그인) · 다운로드 없음. 60초 녹화 → 자막 영상·글·사진 카드 → 쇼츠·릴스·쓰레드·X(유료)·네이버(복붙)·홈페이지 6곳. 단일 출처는 기획1 `/mainplan`(content/mainplan/data.js). 회장님: "기획은 더 이상 추가·수정하지 않는다."
- **첫 페이지·메뉴**: remento.co 의 레이아웃 배치·섹션 순서·마케팅 흐름·정보 구조를 적극 참고하고 내용은 전부 온스토리. 기존 섹션(포트폴리오·3단계·차별점·가격 밴드)은 삭제하지 않고 자리만 재배치 — 삭제는 회장님 결정.
- **레멘토 참고 방침 확정(09-05 저녁, CLAUDE.md 규칙 9)**: 레이아웃·정보 구조 = 적극 벤치마킹 / 색상 = 독자 팔레트 메인 `#005B2A`·서브 `#273D3D`(레멘토 포레스트·크림·라임 조합 사용 안 함) / 아이콘·이미지·세부 UI = 유사하되 변형. 09-05 1차본(포레스트 #1E332D·크림·라임 토큰)은 이 방침 이전 산출물이라 **색·세부 UI 조정 작업이 남아 있다** — 사업 로직 파일은 그대로, 시각 파일만 손댄다(기획1 #remento 조정 목록).
- **요금**: 14일 전 기능 무료 → 정회원 49,000원(토스 1회 결제). 14일 뒤 미결제 시 expired(비공개) → 30일 뒤 삭제(사람). 월 9,900원 유지비는 **결정 대기**(화면에 표기하지 않음). 회원가입(카카오/이메일) 필수 — 정회원 버튼이 로그인으로 보낸다.
- **온보딩**: 한 페이지 폼 → 5단계. 네이버 플레이스 불러오기는 네이버 지역검색 API + 카카오 로컬 API 로 이름·업종·주소·전화만(영업시간·사진·소개는 API에 없음), 크롤링 금지(법적 위험). 세부 업종은 기존 14업종 id 로 매핑(스키마 불변). 분위기 다크/화이트×8색 → `theme.palette` premium/clean + `theme.accent`(스키마 기존 필드). 로고는 `settings.logo`(섹션 스키마 밖).
- **녹화 링크**: `/rec/{slug}?k={주차}.{HMAC12}` — DB 없이 검증, 그 주·지난 주만 유효. 업로드는 브라우저→R2 서명 PUT(Vercel 4.5MB 한도 회피), R2 env 없으면 서버 경유 폴백(≤20MB). `story_entries` 에 question·video_key·media_status 컬럼 추가(마이그레이션 20260905090000).
- **정직 원칙 유지**: 후기 0건인 리뷰 페이지에 가짜 후기를 넣지 않는다. "이런 사장님께" 카드는 후기가 아님을 명시. 별점 없음(규칙 7).
- **백업**: 변경 전 원본은 `fable51plandept/backup-onstori-2026-09-05/`. push 전 `git tag backup-2026-09-05`.
