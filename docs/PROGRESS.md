# PROGRESS.md — 작업 인수인계

## 2026-09-17 — 최우선 셋 [16] → [18] → [17]

**커밋** `3b33141`([16]) · `846e056`([18]) · `3d51158`([17])
**검사** `npm run check` **0** · `npx tsc --noEmit` **0** · `npm run build` **0**

> 보고서 세 장: `docs/AI/완료/지시-16-캔버스중계.md` · `지시-18-틱톡-파일직접올리기.md` ·
> `지시-17-녹화끝에서-바로올리기.md`

### 다음 창이 꼭 알아야 할 것 다섯

1. 🔴 **녹화는 이제 «캔버스 중계»다.** 카메라를 직접 녹화하지 않는다 — 9:16 고정 캔버스에
   그려서 그 캔버스를 녹화한다. 스위치는 `config/recording.ts` 의 **`CANVAS_RELAY`** 하나다.
   ⚠ 캔버스 크기를 바꾸려면 **비율(9:16)을 지켜라** — 자르는 계산이 그 비율을 전제로 한다.
   ⚠ 녹화 화면에 [카메라 전환]을 넣으면 **중계를 다시 시작해야 한다.** 지금은 `setup` 화면에만 있다.
2. 🔴 **틱톡은 `FILE_UPLOAD` 다.** `PULL_FROM_URL` 로 되돌리지 마라 — `img.onstori.com` 이
   틱톡에 인증돼 있지 않아 **네 번 다 거절당했다.** 인스타는 그대로 `PULL_FROM_URL` 이다.
3. 🔴 **서명된 R2 주소에 `HEAD` 를 쓰지 마라 — 403 이다.** 서명은 `GET` 에만 유효하다.
   크기를 알고 싶으면 `Range: bytes=0-0` 의 `Content-Range` 를 읽어라.
4. 🔴 **파일 «이름»과 «내용»이 다르다.** 저장소의 `.webm` 녹화가 실제로는 mp4 다.
   형식이 필요하면 `lib/media-sniff.ts` 로 **파일 머리를 봐라.** 확장자를 믿지 마라.
5. 🔴 **`?oneshot=` 은 «딱 한 번만» 돌아야 한다.** 목록이 다시 그려지거나 새로고침할 때마다
   돌면 **같은 영상이 SNS 에 여러 번** 올라간다. `ranOneShot` + 주소에서 지우기, 둘 다 필요하다.

### 아직 남은 지시

**[19] 숏폼 무대 + 디자인 전면 개편 · [20] 홈 화면 바로가기 · [21] 심사용 로그인 한영 병기.**
⚠ [19] 의 **⓪번**(「여러 편을 걸었는데 한 편만 나온다」)이 가장 구체적이다 —
`app/api/site/video/manage/route.ts` 에 **「걸기(attach)」 action 이 없다**는 것이 권반장 진단이다.

---

## 2026-09-16 저녁 — 권반장 지시 [13]·[15]·[14]

**커밋** `015eeae`([13]) · `4d0d3c7`([15]) · `481cdfd`([14])
**검사** `npm run check` **0** · `npx tsc --noEmit` **0** · `npm run build` **0**

> 보고서 세 장: `docs/AI/완료/지시-13-영상관문.md` · `지시-15-가입직후-점수.md` · `지시-14-단건상품-custom.md`

### 다음 창이 꼭 알아야 할 것 다섯

1. 🔴 **가입은 이제 9단계다.** 마지막이 「영상」 관문이고, 끄고 켜는 것은
   `config/onboarding.ts` 의 **`REQUIRE_VIDEO_TO_FINISH`** 하나다
   (환경변수 `NEXT_PUBLIC_ONBOARDING_REQUIRE_VIDEO=off`). **다른 곳에 조건을 만들지 마라.**
   끄면 8단계로 예전과 똑같이 돈다 — 실측으로 확인했다.
2. 🔴 **`app/api/story/submit` 이 이제 점수를 다시 센다.** 전에는 안 셌고, 그래서 영상을 찍어도
   10점이 안 붙었다. 점수 «규칙»(`config/completeness.ts`)은 여전히 무수정이다.
3. 🔴 **가입 직후 점수는 «50·60·70» 셋으로 갈린다** — 「시작한 이유」를 쓰면 AI 가 지어 준
   첫 이야기가 `visible=true` 로 들어가 `story_1`(10점)이 붙는다. 그리고
   **「연결 버튼」 10점은 가입에서 절대 안 붙는다**(위젯을 만드는 코드는 편집화면뿐).
   ⇒ 어떤 길로도 **75를 못 넘는다.** 대표님의 「뭔가 수정을 해야만」은 지켜진다.
4. 🔴 **`/api/site/video` 에 `publishNow` 가 생겼다. 가입 관문 전용이다.**
   평소에 쓰면 편집화면의 낡은 doc 이 곧 덮어쓴다. 그 이유를 그 파일 주석에 적어 뒀다.
5. 🔴 **단건 상품의 단일 출처는 `config/custom-products.ts` 다.** `lib/trial.ts`(월 구독)와 섞지 마라.
   결제 버튼은 **일부러 없다** — 단건 상점아이디가 아직 없다.

### 실측용으로 만든 테스트 홈페이지 넷

`zz-gate-test-a` · `-b` · `-c` · `-off` — **운영 DB 에 진짜로 생겼다.** 지우지 않고
`status='suspended'` 로 돌려 뒀다(불변 규칙 10). 지울지는 대표님·권반장 판단이다(`docs/WAITING.md`).

---

## 2026-09-16 오후 — 권반장 지시 [11]·[12]

**커밋** `d771057`([12]) · `0e88f03`([11])
**검사** `npm run check` **0** · `npx tsc --noEmit` **0** · `npm run build` **0**

> 보고서 두 장: `docs/AI/완료/지시-12-코드글자-노출.md` · `지시-11-모바일-완성도패널.md`
> ⚠ **두 지시는 지시함 파일이 아니라 «채팅»으로 왔다.** `docs/AI/반장-지시.md` 의 「끝난 것」에
>   기록해 두었다. 권반장이 나중에 「대기 중」에 같은 것을 적으면 **이미 끝난 것**이다.

### 다음 창이 꼭 알아야 할 것 넷

1. 🔴 **폰 편집화면의 왼쪽 칸은 이제 «접힌 것»이 기본이다.** 여는 것은 자바스크립트가 아니라
   **CSS**(`app/globals.css` 의 `.editor-fold` · 1024px 이상에서 되돌린다).
   `matchMedia` 로 바꾸지 마라 — 서버가 그린 화면과 어긋나 **첫 그리기에 깜빡인다.**
2. 🔴 **접는 덩어리 «안»에 `data-tour` 앵커를 넣지 마라.** 폰에서 `display:none` 이 되어
   투어가 가리킬 자리가 사라진다(규칙 3). `score-bar` 는 일부러 **밖**에 뒀다.
3. 🔴 **편집화면의 「75점」은 이제 `SITEMAP_MIN_SCORE` 에서 온다.** 화면에 숫자를 다시 적지 마라.
   ⚠ 다만 그 문장은 **작은 거짓말**이다 — 실제 문턱은 셋(75점·전화번호·직접 고침)이다.
   고치려면 대표님 허락이 필요하다(`docs/WAITING.md`).
4. 🔴 **노트북 1024~1279px 에는 [11] 의 병이 그대로 남아 있다** — 본문이 화면의 73~78% 아래에서
   시작한다. `globals.css` 의 접기 규칙 `1024px` → `1280px` 한 줄이면 되지만
   **권반장 판단 대기**다. 마음대로 바꾸지 마라.

### 안 건드린 것

`app/new/wizard.tsx` — 아직 T-0022 가 잡고 있다. 그래서 「내 도메인 연결」 약속 문구는 여전히 대기.
⚠ `app/[slug]/edit/*` **자물쇠는 풀렸다**(T-0025 병합됨). [11] 이 그 첫 작업이었다.

---

## 2026-09-16 낮 — 권반장 지시 [8]·[9]·[10] 세 건

**커밋** `8fc5f3b` · `fb896e6` · `9f73ff4` · `7cfc338` · `ceec1f4` · `e81f789`
**검사** `npm run check` **0** · `npx tsc --noEmit` **0** · `npm run build` **0**

> 보고서 세 장: `docs/AI/완료/지시-08-사이트맵-예시제외.md` · `지시-09-카피관리-배선.md` · `지시-10-도메인연결-검토.md`

### 다음 창이 꼭 알아야 할 것 다섯

1. 🔴 **`sites` 표에 `sample` 칸은 «없다».** 예시 여부의 단일 출처는 `lib/sites.ts` 의 `SAMPLE_SLUGS` 다.
   조회 `select` 에 없는 칸을 적으면 `app/sitemap.ts` 의 `catch` 가 400 을 삼켜 **사이트맵이 통째로 빈다.**
2. 🔴 **`config/faq.ts` 는 이제 상수가 아니라 함수다** — `faqGroups(snsLine)` · `faqFeatured(snsLine)`.
   **기본값을 일부러 안 뒀다.** 새 화면이 생기면 `tsc` 가 배선 누락을 대신 잡는다.
3. 🔴 **`readConfig()` 를 읽는 화면을 새로 만들면 `app/api/admin/config/route.ts` 의
   `revalidatePath` 목록에도 반드시 더한다.** 안 그러면 저장하고 **한 시간** 옛 글자가 나간다.
4. 🔴 **`/how-it-works` 1단계에 `${COPY.trialShort}` 가 글자 그대로 손님에게 보인다**
   (`app/how-it-works/page.tsx:15` — 그 줄만 백틱이 아니라 큰따옴표). **대표님 허락 대기 중이다. 마음대로 고치지 마라.**
5. 🔴 **Vercel 요금제가 Hobby 인지 Pro 인지 아무도 모른다.** Hobby 면 상업적 사용이 약관 위반이다.
   도메인 연결 일은 **그 확인 전에는 시작하지 않는다.** (`docs/WAITING.md`)

### 안 건드린 것

`app/new/wizard.tsx` · `app/[slug]/edit/*` — T-0022·T-0024 가 아직 잡고 있다(권반장 09-16 경고).
그래서 `copyright` 배선과 「내 도메인 연결」 약속 문구 수정이 **병합 대기**로 남았다.

---

## 2026-09-16 새벽 — 로고 만들기 전면 개편 (권반장 지시 [5])

> 이 문서에 적힌 요금·기간 숫자는 **작성 당시 값**이다. 지금 값은 `lib/trial.ts` 가 진실이다.

**배포 완료** `1c9821c` · `npm run check` **0** · `npx tsc --noEmit` **0** · `npm run build` **0**

### 무엇을 했나

옛 `wordmarks()` 4안을 통째로 걷어내고 지시하신 순서 그대로 다시 만들었다.
①파일 첨부(올리면 아래가 접힌다) → ②이름 → ③한글/영문 → ④모양 5 → ⑤글꼴 5 → ⑥심볼 15·이니셜

| 새 파일 | 무엇 |
|---|---|
| `lib/logo-maker.ts` | 모양·글꼴·심볼의 **단일 출처**. SVG 문자열만 만든다 |
| `lib/logo-embed.ts` | 저장할 때 **글꼴을 심는다** |
| `app/new/logo-picker.tsx` | 화면 전부 |
| `scripts/build-font-ranges.mjs` | 「글자 → 글꼴 조각」 지도 (prebuild) |
| `scripts/logo-test.ts` | 검사 29건 (`npm run check` 에 들어감) |
| `scripts/logo-sheet.ts` | 눈으로 볼 한 장 — `npx tsx scripts/logo-sheet.ts` |

### 만들면서 찾은 것 셋 — 이게 더 중요하다

**① 미리보기와 저장된 로고가 달라질 뻔했다.**
저장된 로고는 `<img>` 로 쓰이는데 **그렇게 쓰인 SVG 는 바깥 글꼴을 못 부른다.**
사장님 PC 에 Pretendard 는 거의 없으니 실제로는 「맑은 고딕」이 된다.
⇒ 미리보기는 **인라인 SVG**(웹폰트 먹음), 저장할 때는 **글꼴을 심는다**(30~60KB).

**② 🔴 `/fonts.css` 가 404 다.** (운영에서 실측)
Next 가 `app/fonts.css` 를 번들에 넣어 버려 주소로 못 받는다. 모르고 뒀으면
**글꼴 심기가 조용히 실패**해 ①이 그대로 재발했을 것이다.
⇒ 지도를 빌드 때 뽑아 `public/fonts/pretendard/ranges.json` 으로 둔다(`prebuild`).

**③ ⚠ 글꼴 5개 중 실물이 있는 것은 1개뿐이다.** — 아래 「남은 것」 참고

### ⚠ 한계 — 대표님이 아셔야 할 것

| 글꼴 | 파일 | 실제로 |
|---|---|---|
| **기본**(Pretendard) | ✅ 자체 호스팅 | **어디서 봐도 똑같다**(심어 넣음) |
| 아주 굵게·둥글게·명조·손글씨 | ❌ 없음 | 그 글꼴이 **깔린 PC 에서만** 그렇게 보인다 |

넷은 저장소에도 R2 에도 파일이 없다. 자간·굵기를 달리해 **골라도 티는 나게** 했지만
「고른 그 글꼴」이 보장되지는 않는다.

⚠ Pretendard 는 굵기 축이 **400~700** 으로 좁혀져 있어(`app/fonts.css`) **900 이 없다.**
「최대 굵은 워드」의 실제 최대는 700 이다.

⚠ 「펜타그리드」는 저장소에서 **못 찾았다** — 권반장 보고가 맞다. 그 이름의 글꼴·파일이 없다.

### ⚠ 못 한 확인

**`/new` 를 브라우저로 끝까지 통과해 보지 못했다.** 내장 브라우저가 `localhost` 를
`https` 로 바꿔 버려 연결이 안 됐다. 대신 로고 출력을 **검사 29건 + 눈으로 볼 14장**으로 증명했다.
⇒ 다음 세션에서 `/new` 실통과를 **먼저** 하는 것을 권한다.

### 권반장 지시함 — 남은 것 넷

| | | 예상 |
|---|---|---|
| [6] | 첫 화면 이미지 교체 + 프롬프트 | 40분 |
| [7] | 편집화면에서 채널 고치는 길 | 30분 |
| [2] | 가입에 사진 올리기(15장) | 1시간 |
| [3] | 🔴 가입에 문의 채널(CTA) — 본체 | 2시간 |

★ [4] 는 **권반장이 이미 했다.** 중복하지 말 것.

### ⚠ 미커밋 변경 9개는 «내 것이 아니다»

세션 끝 시점에 `CLAUDE.md`·`app/[slug]/edit/*`·`app/page.tsx` 등 **9개**가 미커밋 상태다.
`c36462c`(권반장 밤 워크플로) 이후에 생긴 것이라 **권반장이 작업 중인 것으로 보인다.**
**건드리지 않았다.** 내 로고 작업은 전부 `1c9821c` 로 커밋·푸시됐다.

---

## 2026-09-15 (3) — 플레이스 선택 버그 · 문구 · 어드민 상태 · SEO 실측 (권반장)

**배포 완료 · 실물 확인까지 마침** · `check` 0 · `tsc` 0 · `build` 0 · 커밋 `3114420`·`0490e27`

### 🔴 「플레이스에서 고르면 아무 반응이 없다」 — 버그가 아니었다

매장을 누르면 값(상호·주소·전화·업종)은 **다 들어갔다.** 그런데 `setPlaces(null)` 로
**목록만 사라지고 아무 말도 안 했다.** 상호만 위 칸에 보이니 「가게명만 불러온다」로 보였고,
주소·전화는 **3단계에 있어서 그 자리에서 볼 길이 없었다.**

⇒ 고른 것을 **초록 카드로 남긴다**: ✓ 선택되었습니다 + 무엇이 들어왔는지 줄마다
\+ 「… 정보를 바탕으로 홈페이지 제작을 시작합니다」 + [다시 고르기]

★ 배운 것: **값이 조용히 들어가는 것은 «안 된 것»과 구별되지 않는다.** 말을 해야 한다.

### 문자 — **지금 한 통도 안 나간다** (대표님이 재확인 요청하신 것)

DB 실측: 사이트 2곳 **모두 견본 판정**에 걸려 크론이 건너뛴다.
그와 **별개로** 주 1회 기본 채널이 `email` 이라, 문자는 사장님이 **신청해야만** 나간다. 자물쇠 둘.

### 🔴 SEO — 경쟁사 홈ON 원본 HTML 을 받아 대조했다

보고서: `fable51plandept/AI_Context/BANJANG/SEO-홈온-분석-2026-09-15.md`

| 찾은 것 | |
|---|---|
| ① | 홈ON 은 `naver-site-verification` **3개** · `google-site-verification` **2개**. **자동이 아니라 «등록»했다.** 우리는 0개였다 |
| ② | JSON-LD `sameAs` 에 **네이버 블로그·네이버 지도 플레이스**를 넣었다. 네이버가 **이미 아는 것에 붙는 것**이 가장 빠르다 |
| ③ | 🔴 홈ON 은 고객 사이트를 sitemap 에 **바로** 넣는다. 우리는 `status === "active"`(유료)를 요구해 **무료 체험 30일 동안 검색에 아예 안 잡힌다** |

③은 **정책 판단이라 대표님 결정 대기**로 올렸다(`docs/WAITING.md`).
★ 권반장 추천: `trial` 도 넣되 **완성도 점수·전화번호·「한 번이라도 직접 고쳤다」 셋은 관문으로 남긴다.**
⚠ `trial` 의 `noindex` 도 같이 풀어야 한다 — **한 쌍**이다.

**넣은 것:** 소유확인 태그를 **환경변수로** 받는 자리(값 없으면 태그 자체가 안 나간다) ·
`sameAs`(사장님이 직접 넣은 `https://` 주소만) · `og:locale`·`og:type`·`og:siteName`.

### 실물 확인 — 눈으로 봤다

| | |
|---|---|
| `/g/interior` | **308 → `/interior` → 200** ✅ 404 풀림 |
| `/x/sample-interior` | **404** ✅ (운영자 아니면 있는지조차 안 알린다 — 설계대로) |
| `/new` JS 청크 | 새 문구 5종 **전부 배포됨** ✅ |
| `/api/slug-check` | 추천·한글·중복·예약어 **네 갈래 모두 정상** ✅ |

### 클코님께 넘긴 것 — `docs/AI/반장-지시.md`

[2] 사진 올리기(15장) · [3] 🔴 문의 채널 CTA(최대 2개) · [4] 채널 연결(= **SEO 엔진**)

## 2026-09-15 (2) — 시험 사이트 대청소 · `/g/` 폐기 · 비공개 보관실 · 주소 칸 복구 (권반장)

**`npm run check` 종료코드 0 · `npx tsc --noEmit` 0 · `npm run build` 0** · 아직 push 안 함

### DB — 시험 사이트 17곳을 지웠다 (대표님 지시·승인)

지우기 «전»에 18곳 전부를 표로 뽑아 확인했다. **회원 주인이 있는 곳은 한 곳도 없었고**
전부 `trial` 상태의 시험·견본이었다. `sample-interior` 1곳만 남겼다.
연결 자료(이야기·문의·SNS·구독)는 `on delete cascade` 로 함께 정리됐다 — 찌꺼기 없음.

⚠ 지운 목록은 `AI_Context/BANJANG/LATEST.md` 에 그대로 적어 뒀다. 되돌릴 수 없다.

### 🔴 `/g/` 를 폐기했다 — **만든 홈페이지가 안 열리던 진짜 이유**

`/{상호}` 의 「보이기」 판정이 **`isPremade()`(문자 금지 판정)와 한 덩어리**였다.
운영자가 만들면 `settings.premade = true` 가 심기므로 **만들자마자 `/{상호}` 에서 사라졌다.**
대표님이 `onstori.com/feliz` 를 못 만드신 것이 이것이다.

⇒ **두 질문을 갈랐다.** 보이기 = 「발행됐는가」뿐(`lib/sites.ts`) · 문자 = `isPremade()` 그대로.
🔴 **`lib/premade.ts` 는 손대지 않았다.** 09-13 P0 의 자물쇠다. 보이기 때문에 그걸 풀면 그 사고가 되살아난다.

`/g/{상호}` 는 **파일을 지우지 않고 308 영구 이동만** 남겼다 — 이미 보내 둔 영업 링크가 안 깨지게.

### 🆕 비공개 보관실 `/x/{상호}` — **어떤 사이트도 삭제하지 않는다**

| 누가 | `/{상호}` | `/x/{상호}` |
|---|---|---|
| 손님 | 「쉬고 있어요」 안내만 (RLS 가 내용을 안 준다) | **404** — 있는지조차 안 알려 준다 |
| 운영자 | **`/x/{상호}` 로 자동 이동** | 홈페이지 원본 그대로 + 상태·고치기 링크 |

★ **되살리는 데 할 일이 없다.** 결제가 들어오면 `status` 가 돌아오고 그대로 열린다 — 아무것도 안 지웠으니까.
⚠ 자물쇠 순서: `isAdmin()` 을 **자료를 읽기 전에** 본다. 뒤집으면 그 사이로 샌다.

### 가입 3단계 「홈페이지 주소」 칸 복구 — 이탈 원인을 되살리지 않게

09-12 에 없앴던 칸이다(이탈 1위). **원인은 칸이 아니라 「한글을 쳤을 때 아무 말도 안 한 것」**이었다.
⇒ ①**비어 있는** 주소 3개를 단추로 먼저 ②한글은 **그 자리에서** 이유를 말한다(서버를 안 기다린다)
③**비워 두면 예전 그대로** 서버가 짓는다 — 안 건드린 사장님에겐 변화가 없다.

한글 상호를 영문으로 옮기는 `romanize()` 를 `lib/slug.ts` 에 넣었다 —
「다산 리모델링」 → `dasan` · `dasan-interior` · `dasanrimodelring`.
⚠ **완벽한 표기법이 아니다.** 그래서 «추천»일 뿐이고 옆 칸에서 고칠 수 있는 것이 핵심이다.

### 샘플 사이트 표시 (대표님 허락)

`sample-interior` 상호를 **「다산 리모델링 (샘플사이트)」** 로 바꾸고, 히어로 위 작은 글자
「샘플 예시 사이트」는 **뺐다.** 로고 자리는 **모든 페이지에 항상** 있고 히어로는 첫 화면뿐이라,
심사관이 어느 페이지로 들어와도 보이는 쪽을 골랐다.
⚠ 지도 「실제 매장 주소는 아닙니다」·문의 「샘플 사이트지만 문의는 실제로 접수됩니다」는 **그대로 뒀다.**

## 2026-09-15 — 브랜드 v1 · 지시함 신설 · 견본 문자 P0 · 검사기 두 번 강화

> 이 문서에 적힌 요금·기간 숫자는 **작성 당시 값**이다. 지금 값은 `lib/trial.ts` 가 진실이다.

**배포 완료** · `npm run check` **종료코드 0** · `npx tsc --noEmit` **종료코드 0**

### 오늘 들어온 커밋

| 커밋 | 무엇 |
|---|---|
| `fda9486` | 검사기가 `.claude/worktrees/`(main 의 사본 7벌)를 훑어 77건을 잘못 잡던 것 |
| `d0c7990` | **브랜드 v1** — 로고·파비콘 전면 교체 + 사장님 편집화면 밝기 전환 |
| `1d0447d` | **권반장 지시함** 신설 (`docs/AI/반장-지시.md`) |
| `6763634` | 🔴 **P0** — 견본 사이트로 삭제 예고 문자가 나가는 구멍 |
| `e3b8d8b` | 그 P0 를 **검사가 못 잡았던** 이유 둘을 고침 |

### 🔴 P0 — 견본 사이트로 삭제 예고 문자가 나갈 뻔했다

견본은 위저드가 네이버·카카오에서 불러온 **그 가게의 진짜 번호**를 갖고 있다.
그 사이트가 정지되면 만료 크론 ④번이 그 번호로 삭제 예고 문자를 보냈다.
①번(무료 종료 예고)에는 걸름이 있었고 **④번에만 빠져 있었다.**

**★ 지시는 3줄이었으나 4줄이 필요했다.** ④ 구간의 조회에 `owner_id`·`anon_id` 가 없었다.
`isPremade` 가 그 두 칸을 보는데 없으면 「주인이 아무도 없다」로 읽혀
**정지된 사이트가 전부 견본으로 판정** → 계약하신 진짜 사장님께 예고 문자가 **한 통도 안 간다.**
막으려던 것과 정반대 사고였다.

### ★★ 그 P0 가 «검사 초록불»인 채로 빠져나갔다 — 이유가 둘이었다

| | 무엇이 문제였나 |
|---|---|
| ① | 검사가 「파일 안 **어느 하나의** select 가 두 칸을 가지면 통과」였다. ①번 덕에 ④번이 비어도 초록불 |
| ② | 🔴 창을 「`.from("sites")` 뒤 **8줄**」로 잡았는데, **내가 어제 넣은 경고 주석 7줄**이 그 창을 밀어내 검사가 그 조회를 **찾지도 못했다** |

②가 아프다. **「이 칸을 꼭 읽어라」고 적은 경고가 그 경고를 검사하는 눈을 가렸다.**
⇒ 창을 줄 수가 아니라 **문장 끝(세미콜론)** 으로 잡았다. 주석이 몇 줄이든 안 뚫린다.

**★ 증명했다.** 임시 복사본에 어제의 사고 상태를 재현해 돌렸다 —
고치기 전 종료코드 **0**(놓침) → 고친 뒤 종료코드 **1**, `expire/route.ts:173` 를 정확히 지목.
⚠ 실제 저장소 파일은 안 건드렸다(임시 폴더에 복사해서 쟀다).

### ⚠ 작업 중 내가 낸 실수 — 기록해 둔다

사고 상태를 시험하려고 **실제 파일**을 일시적으로 망가뜨렸다가, 되돌리기 명령이 실패했는데
`grep` 결과를 잘못 읽고 **복원된 줄 알았다.** `git diff` 로 확인해 즉시 복원했다.

★ 배운 것: **시험하려고 실제 파일을 건드리지 않는다.** 임시 폴더에 복사해서 잰다
(`prove.js` 가 그 방식이다 — 복사 → 망가뜨림 → 검사 → 복사본 삭제).

### 검사기 범위를 좁힌 이유 (실측)

`settings` 를 읽는 조회는 저장소에 **22곳**인데 대부분 점수 계산·사이트맵·관리자 화면이라
견본 판정과 무관하다. 전부에 두 칸을 요구하면 **오탐 16건**이 나 검사가 못 쓰게 된다.
⇒ 「`isPremade` 를 **부르는** 파일」로 좁혔다. 지금 6곳을 본다.

### ⚠ 확인했으나 «지금은» 위험이 없는 것 — 대표님 판단이 필요하다

**사이트맵에 예시 사이트가 실릴 수 있다.** `app/sitemap.ts` 의 `sitemapVerdict` 는
`site.sample` 을 **안 본다.** 어제 JSON-LD 에서는 예시를 뺐는데 사이트맵은 안 봤다.

**지금은 안 실려 있다** — 운영 사이트맵을 읽어 확인했다(손님 사이트 0곳, 본사 화면 7개뿐).
`sample-interior` 이 다른 관문(완성도 점수·첫 편집 여부)에 걸려 빠졌다.

⚠ 다만 **유튜브 심사용으로 그 사이트를 채우면 점수가 올라 들어갈 수 있다.**
그러면 「다산 리모델링」이라는 **없는 업체가 구글에 색인된다.** 한 줄이면 막는다.
**이번 범위 밖이라 안 건드렸다.**

### ⚠ db push — **여전히 둘뿐이다**

| 마이그레이션 | 무엇 |
|---|---|
| `20260912190000_story_caption` | 이야기별 캡션 저장 칸 |
| `20260912200000_story_soft_delete` | 이야기 되돌릴 수 있는 삭제 표시 |

---

## 2026-09-13 (14) 새벽 — 상무님 반증 21건 처리

> 이 문서에 적힌 요금·기간 숫자는 **작성 당시 값**이다. 지금 값은 `lib/trial.ts` 가 진실이다.

**배포 완료** · **검사 260건 전부 통과**(`npm run check`) · 린트 오류 0 · 운영에서 **읽어서** 확인했다.

### 🔴 가장 컸던 것 셋

**1. 서버가 이메일을 통과시키고 있었다** (`app/api/generate` zod `optional`).
화면만 막고 서버는 열려 있었다. 그 틈으로 들어온 사이트는 `notify` 를 아예 안 심어
**문의가 와도 사장님이 영영 모른다.** 이제 주 1회 알림의 기본도 메일이라 그 상품 자체가 안 간다.

**6. 「한 사람에게 주 한 통」이 무력화돼 있었다.**
중복 제거의 열쇠가 전부 «번호»인데 대표님 결정으로 기본 채널이 메일이 됐다 ⇒
**같은 메일로 사이트 둘을 가진 분은 주 두 통.** 9/12 지시 A2 가 막으려던 일이 되살아났다.

| | 전 | 후 |
|---|---|---|
| 열쇠 | 번호 하나 | **닿는 곳 전부** — `mail:…` + (신청하신 경우) `tel:…` |
| 판정 | 번호가 같으면 한 사람 | **열쇠를 하나라도 공유하면** 한 사람 |

⚠ 애매하면(`wantsSms` 를 안 준 경우) **번호를 열쇠로 쓴다** — 덜 보내는 쪽으로 기운다.
잘못 묶여 한 통 못 받는 것은 다음 주에 회복되지만, 두 통이 나가면 못 되돌린다.
**검사 8건**을 붙였다(같은 메일 · 대소문자 · 메일과 번호로 이어진 셋 · 닿을 곳 없는 후보끼리).

**7. `notify_last_error` 를 지우는 코드가 0곳이었다.**
주소를 고쳐 정상으로 돌아와도 영원히 failing 으로 남는다. 그러면 운영자 계기판이
「고장난 곳」을 늘 부풀려 보여 주고 **결국 아무도 그 화면을 안 본다** — 그때 진짜 고장을 못 찾는다.
보내기가 성공한 순간 지운다.

### 나머지 — 21건 처리표

| # | 무엇 | 결과 |
|---|---|---|
| 1 | 서버 이메일 필수 | ✅ 위 |
| 2 | 「📞 」 빈 버튼 | ✅ **이미 고쳐져 있었다**(어젯밤 커밋). 상무님이 그 전 코드를 보셨다 |
| 3 | `phone` 을 `.default("")` 로 | ✅ 불변 규칙 2 — 스키마·렌더러·에디터·문서 **네 곳 동시** |
| 4 | 「전화번호를 입력해 주세요」가 손님 화면에 | ✅ 빈 값으로 |
| 5 | 히어로 CTA 글자 | ✅ 렌더러에서 — 기존·신규 동시. 「견적 문의하기」(부회장 안) |
| 6 | 주 한 통 무력화 | ✅ 위 |
| 7 | 오류 표시가 안 지워짐 | ✅ 위 |
| 8 | `readWeekly` 가 kakao 로 되돌림 | ✅ **이미 고쳐져 있었다** — 어젯밤 메일 기본으로 바꾸며 `email` 로 |
| 9 | 메일 칸이 문의 폼 안 | ✅ 밖으로. ★ 다만 아래 「사실대로」 참고 |
| 10 | 녹화 링크 문자가 금지어 검사를 안 거침 | ✅ 문장에서 빼고, 주간 크론과 **같은 검사** |
| 11 | `AUTO_RETRIES` 3 → 1 | ✅ 두 겹 거짓말이 함께 사라졌다. 「(1/1)」도 안 보인다 |
| 12 | 「가게에 직접 연락」 | ✅ 「다시 열릴 예정입니다」 (「곧」 안 씀) |
| 13 | 금지어 「정지」 | ✅ 화면 문구만. 법무·운영자 화면은 정확해야 하므로 그대로 |
| 14 | 편집화면 이름 | ✅ **「홈페이지 관리」** — 다른 문구들이 이미 그 이름으로 부르고 있었다 |
| 15 | 홈페이지만 «질문»이 캡션 | ✅ SNS 와 같은 순서로. 한 영상에 두 얼굴이었다 |
| 16 | 저장 5000 vs 올리기 2200 | ✅ 같은 숫자로. 3000자가 조용히 잘렸다 |
| 17 | 유튜브 열쇠 이름 | ⬜ **재현되지 않았다** — 아래 참고 |
| 18 | CTA 「수업 문의」 | ✅ `TEMPLATE_WORDS` 한 곳에 |
| 19 | 「수업 사진」 | ✅ 같음 |
| 20 | 「단정한 학원」 템플릿 | ✅ 켰다(`inWizard: true`) |
| 21 | 「수업료」 | ✅ 기본 제목이 템플릿을 따른다 |

### ★ 사실대로 — 고치지 않았거나, 전제가 달랐던 것

**17번은 재현되지 않았다.** 코드가 읽는 이름과 화면 문구가 **둘 다 `GOOGLE_OAUTH_CLIENT_ID`·
`GOOGLE_OAUTH_CLIENT_SECRET`** 이다(`lib/sns/youtube.ts:66~67`). 저장소 전체에서 구글 열쇠 이름은
그 둘뿐이다. 무엇이 어긋나 보이셨는지 알려 주시면 다시 보겠다.

**9번의 전제가 지금 코드에선 성립하지 않는다.** 문의 폼은 **지울 수 없다** —
삭제 버튼 자체가 안 그려지고(`ui.tsx`) `deleteSection` 도 거부한다. 그래도 **옮겼다.**
이유는 다르다: 이제 그 주소로 **주 1회 「이번 주 질문」**이 간다. 「문의 받기」 안에 있으면
사장님이 그 사실을 알 길이 없다.

**5번은 운영 화면으로 확인할 수 없었다.** 새 글자(「견적 문의하기」)는 **전화형 CTA 에
번호가 없을 때만** 나오는데, 지금 살아 있는 사이트가 전부 견적형이라 그 길을 안 탄다.
빌드·타입 검사까지만 확인했다. 카페·학원 사이트를 하나 만들면 그때 눈으로 보인다.

**2번·8번은 이미 고쳐져 있었다.** 어젯밤 커밋에 들어간 것이라 상무님이 그 전 코드를 보신 듯하다.

### ⚠ 문구를 제가 정했다 — 확인해 주십시오

13번은 **금지어만** 알려 주시고 대신 쓸 말은 안 주셨다. 12번에서 승인하신 어법에 맞춰 이렇게 했다:

| 어디 | 바꾼 말 |
|---|---|
| 가입 화면·결제 모달 | 「홈페이지가 **비공개로 바뀌어** 손님에게 보이지 않지만, 자료는 그대로 보관합니다」 |
| 쉬는 상태 안내 | 「무료 기간이 끝나 홈페이지가 **비공개로 바뀌었습니다**」 |
| FAQ | 같은 어법 |
| FAQ 「문자로 온 링크」 | 「**메일로** 온 링크를 누르고… 문자나 카톡으로 받고 싶으시면 홈페이지 관리에서」 |
| FAQ 「문자·이메일로 60초 안에」 | 「**이메일로** 알려 드립니다」 ⚠ 문의 알림은 이메일로만 간다 — 전엔 사실이 아니었다 |

마음에 안 드시면 말씀해 주시면 바꾼다.

### ⚠ db push — **여전히 둘뿐이다**

| 마이그레이션 | 무엇 |
|---|---|
| `20260912190000_story_caption` | 이야기별 캡션 저장 칸 |
| `20260912200000_story_soft_delete` | 이야기 되돌릴 수 있는 삭제 표시 |

★ 15번을 고치며 `caption` 칸을 **따로** 읽게 했다 — 한 조회에 섞으면 칸이 없는 지금 DB 에서
**영상 걸기가 통째로 멈춘다.** db push 전에도 안전하고, 끝나면 저절로 사장님 글이 쓰인다.

```
npx supabase db push
```

### ⚠ 막힌 것 — 사람 손이 필요하다

| 무엇 | 왜 | 누가 |
|---|---|---|
| `npx supabase db push` | 위 둘 | 회장님 |
| Vercel `REVIEW_SITE = sample-interior` | 유튜브 감사 스크린샷의 마지막 걸음 | 회장님 |
| **교육 사진을 이미지 은행에 채우기** | 849장 중 교육 **0장**. 갤러리·진행과정은 재고가 없으면 섹션이 안 생긴다 | 회장님 |
| 17번이 무엇이 어긋났는지 | 코드에서는 이름이 같다 | 상무님 확인 |
| 13번 문구 승인 | 대신 쓸 말을 제가 정했다 | 회장님 |
| 솔라피 수신(STOP) 설정 | 받는 주소를 솔라피에 등록해야 STOP 이 온다 | 회장님 |
| 영상관리 최신 판본 | 「지우는 길」이 인스타 앱에서 실제로 되는지 확인 전 | 확인 필요 |
| 노출/비노출 | `visible` 재사용 금지(완성도 점수가 조용히 내려간다). 새 칸 필요 | db push 뒤 |
| 요금제 이원화 | 35~50시간 · 결정 3건 먼저 | 회장님 |

---

## 2026-09-13 (13) 새벽 — 번호 비공개 · 알림 메일 기본 · 견본을 /g/ 로

> 이 문서에 적힌 요금·기간 숫자는 **작성 당시 값**이다. 지금 값은 `lib/trial.ts` 가 진실이다.

**배포 완료** · **검사 230건 전부 통과**(`npm run check`) · 운영에서 **읽어서** 확인했다.

### 🔴 먼저 — 내가 사고를 냈고, 되돌렸다

견본을 손님 주소에서 가리는 일을 `lib/sites.ts` 에 넣으면서 **`owner_id`·`anon_id` 를
`select` 에 안 넣었다.** 그러면 「주인이 아무도 없다」로 읽혀 **모든 사이트가 견본**이 된다.
배포 직후 `/moksu`·`/bls`·`/store-2` 가 **전부 404** 가 됐다.

| | |
|---|---|
| 언제 | 배포 직후 ~ 약 4분 |
| 무엇이 | 손님 사이트 전부가 404 |
| 왜 못 잡았나 | **타입 검사도 빌드도 못 잡는다.** Supabase 의 `select` 는 글자열이라 빠진 칸이 `undefined` 로 조용히 온다 |
| 되돌린 방법 | 칸 둘을 넣고 재배포 · 16곳 전수 확인 |
| 다시 안 나게 | `scripts/sites-columns-test.ts` 신설 — `isPremade` 쓰는 파일 다섯의 `select` 를 훑는다. `npm run check` 에 넣었다 |

★ 배운 것: **자물쇠를 «자료 읽는 곳»으로 옮길 때는 그 자물쇠가 «무엇을 읽어야 하는지»도
함께 옮겨야 한다.** 판정 함수만 부르면 입력이 비어도 「안전한 쪽」으로 기우는데,
이번엔 그 안전한 쪽이 **전면 차단**이었다.

### 1·2. 🔴 문자 — 이제 나가는 곳이 **0곳**이다

일곱 곳 전부 껐다(표시 `premade=true` + `weekly.on=false`, 자물쇠 둘).

```
npx tsx scripts/mark-premade.ts     ← 지금 상태 보기
```

| | |
|---|---|
| 껐다 | annongworld · ongyu · cafe · banchan · peacecoffe · interior2 · barun |
| 지금 | **16곳 전부 ✅ 안 나간다 — 0곳** |
| 백업 | `backups/premade-mark-2026-09-13.json` |

⚠ `mark-premade` 가 **표시만 붙이고 `weekly.on` 은 안 껐다.** 자물쇠 하나는 자물쇠가 아니다 — 고쳤다.

### 3. ★ 전화번호를 손님 화면에서 뺐다 — **기본이 비공개**

**번호가 나오던 자리가 넷이었다.** 번호는 `settings.phone` 에만 있는 것이 아니라
홈페이지를 만들 때 **섹션 안으로 복사돼** 들어간다(`lib/generate.ts`):

| 자리 | 어디 |
|---|---|
| ① 견적 문의 「📞 …」 | `components/sections/quote-form.tsx` |
| ② 오시는 길 「전화하기 …」 | `components/sections/index.tsx` |
| ③ 위쪽 띠 전화 단추 | `site-chrome.tsx` |
| ④ 아래쪽 바 전화 단추 | 같음 |

★ 네 곳에 각각 조건을 달면 **한 곳을 빼먹는다.** 그래서 **손님에게 나가는 자료에서
한 번 지운다**(`lib/phone-privacy.ts` ← `lib/sites.ts`). 네 곳 모두 「없으면 안 보인다」로
이미 돼 있어서 함께 사라진다.

★ **켜고 끄는 스위치**는 홈페이지 관리자 「연결」 탭 맨 위 — [숨기기(권장)] / [보이기].
⚠ **표를 안 만들었다 — db push 불필요.** `settings.phonePublic` 에 넣는다.

**운영 확인:** `/sample-interior`·`/moksu`·`/bls` — 전화 흔적 **0건.**

★ 덤으로 규칙 12 위반을 하나 잡았다: 번호가 비공개면 「전화 문의」 단추가 문의 폼으로
떨어지는데 **글자는 그대로 「전화 문의」**였다. 전화가 실제로 걸릴 때만 전화라고 말하게 고쳤다.

### 4·5·6. ★ 주 1회 알림의 기본을 **메일**로

| | 값 | 비용 |
|---|---|---|
| 기본 | **이메일** | 거의 0원 |
| 선택 | 카카오톡 | 13원 |
| 선택 | 문자 | 20원 |

⚠ **이메일은 무조건 간다.** 그래야 「매주 질문이 옵니다」가 거짓말이 아니다.
문자·카톡을 고르시면 메일과 **함께** 간다 — 메일을 «대신» 하는 것이 아니다.

⚠ 전에는 크론이 **번호가 없으면 후보에서 탈락**시켰다. 기본이 메일인 지금 그대로 뒀으면
**번호를 안 적으신 사장님이 한 통도 못 받는다.** 그 줄을 걷어냈다.

★ 가입 3단계 문구는 대표님 확정본 그대로 넣었다. 그리고 완성 화면의
「첫 질문은 **문자로** 보내드릴게요」도 **«메일로»** 로 고쳤다 — 안 고쳤으면 그 줄이
곧바로 거짓말이 됐다.

### 7·8·9·10. ★ 상무님이 찾은 넷 — 전부 맞았다

**🔴 10. 문자가 나가는 «다섯째» 자리가 있었다.**
`app/api/story/send-link` 는 `loadOwnedSite` 를 쓰는데 그것이 **운영자를 그냥 통과시킨다.**
회장님이 견본 편집화면에서 [녹화 링크 문자로 받기] 를 누르시면 **그 가게 진짜 번호로**
문자가 갔다. 크론 둘만 막고 여기를 안 막았던 것이다. 같은 자물쇠를 걸었다.
★ 링크는 그대로 돌려준다 — 막는 것은 문자뿐이고, 화면이 왜 안 갔는지 말한다.

**🔴 7. 견본 주소를 `/g/{주소}` 로 갈랐다.**
⚠ 상무님 경고대로 **`/{주소}` 페이지에는 조건을 한 줄도 안 넣었다.** 거기에 넣으면
손님 사이트 200곳의 ISR 이 무너진다. 대신 «자료를 읽는 곳»에서 가른다.
**빌드 결과로 확인:** `/[slug]` 는 `●`(ISR 그대로), `/g/[slug]` 는 `ƒ`(force-dynamic).

**운영 전수 확인 — 16곳:**

| 무엇 | `/{주소}` | `/g/{주소}` | 곳 |
|---|---|---|---|
| 손님·예시 | **200** | 404 | moksu · bls · store-2 · 김팀장·박팀장 시험 · sample-interior |
| 견본 | **404** | **200** | annongworld · ongyu · cafe · banchan · peacecoffe · interior2 · barun · herotest 셋 |

⚠ 도중에 `sample-interior` 이 404 가 됐다 — 주인이 없어 견본으로 잡혔다. 그런데 그건
콜드콜용 «견본»이 아니라 우리 «예시»이고 **랜딩 포트폴리오가 그 주소를 쓴다.**
**보이기 판정에서만** 뺐다 — 문자 판정에서는 그대로 견본이다(예시로 문자가 가면 안 된다).

**🔴 8. 옆문 둘을 막았다.** `/{주소}/preview` · `POST /api/inquiry`.
★ 다만 사실대로 적는다: preview 의 「어차피 발행본만 보여 주니 권한이 필요 없다」는
**원래 판단은 지금도 맞다.** 7번으로 `/{주소}` 가 막히면서 그때부터 뒷문이 되는 것이라 막았다.

**9. 넘길 때 바꿀 것이 일곱이 아니라 열이었다.** 빠진 넷을 채웠다 —
`status=trial` · `suspended_at=null` · `delete_notices` 비우기 · `weekly.lastSentAt` 비우기.
⚠ `lastSentAt` 이 남으면 새 사장님이 **첫 주를 통째로 굶는다.**
⚠ 무료 기간도 **오늘부터** 다시 센다 — 안 그러면 받자마자 며칠이 깎여 있다.

### 11. 김팀장 JSON-LD — 붙였다. 다만 **번호는 뺐다**

지시서대로 붙였고 **「`</script>` 를 막는 한 줄」·영업시간·별점 제외**는 그대로 뒀다.

★★ **`telephone` 은 «공개로 켜 두신 분»만 싣는다.** 원안은 그냥 실었는데, 그 사이
대표님이 번호를 비공개로 정하셨다. 그대로 실으면 **화면에서 숨긴 번호를 검색엔진에는
넘겨주는** 꼴이 된다. 김팀장도 같은 것을 확인해 뒀다 — 구글 필수는 **이름·주소 둘뿐**이고
`telephone` 은 권장이라 빼도 안 깨진다(`★전화번호-미공개-SEO영향.md`).
★ 이메일도 안 넣었다 — `settings.notify.email` 은 «사장님이 알림 받는 주소»라 공개용이
아니다. 김팀장이 같은 경고를 남겼다.

⚠ **운영 화면에서는 아직 확인할 수 없다** — 정회원(`active`)이 `sample-interior` 한 곳뿐인데
그건 예시라 일부러 뺀다. 그래서 **검사 14건**으로 증명했다(`scripts/jsonld-test.ts`).

### 12·13. 박팀장 둘

| | 결과 |
|---|---|
| 12 `/interior2` 메뉴 중복 | ✅ **이미 고쳐 배포됐다.** 운영 재확인 — 작업 사진·소개·진행 과정·견적 문의 |
| 13 샘플 「샘플입니다」 | ✅ `/sample-interior` 맨 위 — 「온스토리가 만든 예시입니다」. 시드 3곳도 자동 |

### 14. ★ 교육·레슨 카테고리 — 켰다

기술참모님 지적이 맞았다. 꺼져 있어서 교육 세부업종 **15개가 전부 `cafe`** 로 떨어졌다.

```
npx tsx scripts/edu-check.ts
  일본어  → 학원·교습소 · 교육·레슨 · consult ✅
  배드민턴 → 운동·체육  · 교육·레슨 · consult ✅
  코딩   → 학원·교습소 · 교육·레슨 · consult ✅
```

**⚠ 무엇이 어색한가 — 회장님이 물어보신 것:**

| 어색한 점 | 왜 | 어떻게 |
|---|---|---|
| **사진이 안 나온다** | 이미지 은행 849장의 태그 24종에 **교육이 0종**이다. 갤러리·진행과정은 재고가 없으면 **섹션 자체가 안 만들어진다** | 교육 사진을 은행에 채워야 한다 (히어로는 AI 가 만들어서 괜찮다) |
| CTA 가 「문의하기」 | `consult` 는 견적형이 아니라 상담형이라 「견적 문의」가 아니다 | 지금은 맞는 말이다. 「체험 신청」이 더 나으면 문구를 정해 주시면 바꾼다 |
| 진행 과정 섹션이 없다 | 그건 `quote` 템플릿에만 붙는다 | 교육에도 필요하면 말씀해 주시면 붙인다 |

### ⚠ db push 가 필요한 것 — **어제와 같다. 둘뿐이다**

| 마이그레이션 | 무엇 |
|---|---|
| `20260912190000_story_caption` | 이야기별 캡션 저장 칸 |
| `20260912200000_story_soft_delete` | 이야기 되돌릴 수 있는 삭제 표시 |

★ 오늘 만든 것(번호 비공개·넘겨주기·견본 갈래·메일 알림)은 **전부 DB 변경이 필요 없게** 설계했다.

```
npx supabase db push
```

### ⚠ 막힌 것 — 사람 손이 필요하다

| 무엇 | 왜 | 누가 |
|---|---|---|
| `npx supabase db push` | 위 둘 | 회장님 |
| Vercel `REVIEW_SITE = sample-interior` | 유튜브 감사 스크린샷의 마지막 걸음 | 회장님 |
| **교육 사진을 이미지 은행에 채우기** | 지금은 교육 홈페이지에 사진이 안 붙는다 | 회장님 |
| 솔라피 수신(STOP) 설정 | 받는 주소를 솔라피에 등록해야 STOP 이 온다 | 회장님 |
| 4번 「문자로 받기 체크박스」의 뜻 | **주 1회 알림**의 채널로 읽고 그렇게 만들었다. 만약 «문의 알림»을 문자로도 받는 것이면 2026-09-11 에 비용 때문에 빼신 것과 부딪힌다 | 회장님 한마디 |
| 영상관리 최신 판본 | 「지우는 길」이 인스타 앱에서 실제로 되는지 확인 전 | 확인 필요 |
| 노출/비노출 | `visible` 재사용 금지(완성도 점수가 조용히 내려간다). 새 칸 필요 | db push 뒤 |
| 요금제 이원화 | 35~50시간 · 결정 3건 먼저 | 회장님 |

---

## 2026-09-13 (12) 야간 — 남의 가게로 갈 문자를 막고 · 넘겨주는 길을 놓았다

> 이 문서에 적힌 요금·기간 숫자는 **작성 당시 값**이다. 지금 값은 `lib/trial.ts` 가 진실이다.

**배포 완료** — `9cea4ea` · `42c6400` · `b57b6f7` (main). 운영에서 **읽어서** 확인했다.
**검사 209건 전부 통과** — 이제 `npm run check` 한 줄로 전부 돈다.

### A. 🔴 문자 — 계약도 안 한 가게로 나갈 뻔했다 (상무님이 찾았다)

**무엇이 위험했나.** 위저드가 네이버·카카오에서 **그 가게의 진짜 전화번호**를 불러와
`settings.phone` 에 넣는다. 주 1회 알림은 **켜진 채로** 심긴다. 그런데 주간 크론이
**「주인이 있는가」를 보지 않았다.** ⇒ 콜드콜용으로 만들어 둔 견본이 **다음 화요일
아침 9시 크론의 대상**이 된다.

**자물쇠 셋을 걸었다** (`lib/premade.ts`):

| 자물쇠 | 무엇인가 | 왜 |
|---|---|---|
| ① `settings.premade = true` | 운영자가 만들었다는 표시 | **진짜 자물쇠** |
| ② 주인이 아무도 없다 | `owner_id` 도 `anon_id` 도 없음 | 씨앗 스크립트가 만든 시연용 |
| ③ 만들 때부터 꺼 둔다 | `weekly.on = false` | 운영자 쿠키로 만들면 자동 |

⚠ **②만으로는 못 막는다.** 회장님이 브라우저로 만들면 `anon_id` 가 붙어 ②에 안 걸린다 —
지금 켜져 있는 곳이 **전부 그렇다.** 상무님이 제안하신 잠금은 ②였고, 그것만으로는
새는 것을 확인해서 ①을 더했다. 주간 크론·만료 크론 **양쪽**에 같은 자물쇠를 걸었다.

**A5 — 문자가 실제로 나가는 상태인가:** `GET /api/admin/notify-check` → `{"channels":{"sms":true,…}}`
**★ 문자는 켜져 있다. 진짜로 나간다.**

### A-★ 회장님 결정이 필요한 것 — 아직 🔴 나가는 곳 **7곳**

회장님 결정 3 대로 **지금은 채우지 않았다.** 아래가 A 를 끝낸 뒤의 실제 상태다.

| # | 주소 | 상호 | 번호 |
|---|---|---|---|
| 1 | `annongworld` | 안녕월드 | …7025 |
| 2 | `ongyu` | 온규 본점 | …2575 |
| 3 | `cafe` | 박팀장완주테스트 | …8888 |
| 4 | `banchan` | 우리동네 반찬가게 | …2369 |
| 5 | `peacecoffe` | 평화와평화 | …9839 |
| 6 | `interior2` | 욕실 인테리어 전문가 | …7025 |
| 7 | `barun` | 바른전기 | …9238 |

⚠ **앞서 「6곳」이라고 보고한 것은 틀렸다. 7곳이다** — `cafe` 가 빠져 있었다.
⚠ 1번과 6번은 끝 네 자리가 같지만 **다른 번호**다(가운데가 다르다). 번호를 가리고 세면
  또 틀리므로 번호별로 묶어 세었다 — **서로 다른 번호 7개 = 화요일에 나갈 문자 7통.**

**멈추는 법 (회장님이 한 줄):**

```
npx tsx scripts/mark-premade.ts --on annongworld ongyu cafe banchan peacecoffe interior2 barun
```

지금 상태 보기: `npx tsx scripts/mark-premade.ts`
⏰ **마감은 9/15(화) 오전 9시**다. 그전에만 하시면 된다.

### B. 🔴 넘겨주는 길이 아예 없었다 — 놓았다

**무엇이 막혔나.** 기존 「가져가기」는 **그 브라우저의 익명표**로만 됐다. 회장님 PC 에서
만든 것이라 사장님 폰에는 그 익명표가 없다. 사장님이 새로 만들면 딴 홈페이지가 되고
공들여 채운 내용이 버려진다.

**한 번만 쓰이는 열쇠를 만들었다** (`lib/handover.ts`):

```
onstori.com/claim/{상호주소}?k={서명}      ← 14일간 유효
npx tsx scripts/handover-link.ts <slug>    ← 링크 뽑기
npx tsx scripts/handover-link.ts           ← 넘겨줄 수 있는 곳 목록
```

★ **「한 번만」이 어떻게 지켜지나 — 표를 새로 만들지 않았다.** 가져가는 순간 주인이
박히고, 이 열쇠는 **주인이 없을 때만** 듣는다(`.is("owner_id", null)`). 두 번째 사람이
같은 링크를 열면 아무 일도 안 난다. **⇒ db push 가 필요 없다.**

★ 넘겨받으면 `premade` 표시를 떼고 주 1회 알림을 **기본값으로 켠다.** 켜진다는 사실을
**누르기 전에** 화면에서 알린다 — 켠 뒤에 알리면 그것이 광고다.

⚠ **녹화 링크와 섞이면 큰일이다.** 둘이 같은 비밀을 쓴다. 서명 문구가 같았다면 매주
사장님께 나가는 **녹화 문자가 곧 소유권 양도 링크**가 됐을 것이다. 문구를 다르게 하고
검사로 못 박았다(`검사 15건`).

**B2 — 운영자는 여러 곳을 만들 수 있게 했다.** 사장님은 1곳 그대로다.

### C·E — 팀장님들이 찾은 다섯

| 누구 | 무엇 | 결과 |
|---|---|---|
| 박팀장 C1 | 조사 오류(「…를 해요」) | ✅ 문구 출처를 `WEEKLY_NOTICE` 한 곳으로 |
| 박팀장 C2 | 랜딩 「SNS 발행 6곳」 | ✅ `config/channels.ts` 한 곳에서 «준비 중» 표시 |
| 박팀장 C3 | `/interior2` 메뉴 중복 | ✅ **운영 확인** — 작업 사진·소개·진행 과정·견적 문의 |
| 김팀장 E1 | 틱톡 「연결 끊기」가 틱톡에 안 알림 | ✅ 유튜브와 순서를 맞춤 |
| 김팀장 E2 | 해지·정지해도 SNS 토큰이 남음 | ✅ `purgeSnsForSite()` 하나로 양쪽에서 |

★ C3 의 진짜 원인: 메뉴를 앵커 id 로만 중복 제거하고 **«이름»은 안 봤다.** AI 가 지은
제목이 고정 제목과 글자까지 같아져 둘이 나란히 섰다.
★ E2 가 왜 중요한가: 그만 쓰시는 분의 인스타·틱톡에 **글을 올릴 수 있는 열쇠**를 우리가
계속 쥐고 있었다. 유튜브 약관은 「동의 철회 시 즉시 폐기」를 **명시적으로 요구**한다.

### D. 유튜브 감사 준비 — 상태 확인 (읽기만 했다)

| 항목 | 상태 |
|---|---|
| D1 심사관 로그인 | ✅ `onstori.com/login/review` — 살아 있다 |
| D2 게이트 | ✅ **`review`** · `auditPassed: false` — **`on` 아니다** |
| D3 심사용 사이트 영상 | ✅ `sample-interior`(다산 리모델링) — 영상 있는 줄 **12건** |
| D4 스크린샷 | ⚠ **아래 env 한 줄이 남았다** |

⚠⚠ **회장님이 Vercel 에 넣으셔야 하는 것 (그대로):**

```
REVIEW_SITE = sample-interior
```

넣고 Redeploy 하면 심사관이 로그인하는 순간 그 사이트가 그 계정 것이 되고,
편집화면·연결·올리기를 전부 해 볼 수 있다 — 그래야 스크린샷 4종을 찍을 수 있다.

### ⚠ db push 가 필요한 것

| 마이그레이션 | 무엇 |
|---|---|
| `20260912190000_story_caption` | 이야기별 캡션 저장 칸 |
| `20260912200000_story_soft_delete` | 이야기 되돌릴 수 있는 삭제 표시 |

**이 두 개뿐이다.** 오늘 만든 넘겨주기·문자 차단은 **DB 변경이 필요 없게** 설계했다.

```
npx supabase db push
```

### ⚠ 막힌 것 — 사람 손이 필요하다

| 무엇 | 왜 막혔나 | 누가 |
|---|---|---|
| `npx supabase db push` | 위 두 마이그레이션 | 회장님 (아침) |
| Vercel `REVIEW_SITE = sample-interior` | 유튜브 감사 스크린샷의 마지막 한 걸음 | 회장님 |
| 아직 🔴 7곳 | 회장님 결정 3 — 「A 뒤에 판단한다」 | 회장님 |
| 솔라피 수신(STOP) 설정 | 받는 주소를 솔라피 쪽에 등록해야 STOP 이 우리에게 온다 | 회장님 |
| 영상관리 최신 판본 | 「지우는 길」이 **인스타 앱에서 실제로 되는지** 확인 전이라 문구를 못 쓴다 | 확인 필요 |
| 노출/비노출 | `visible` 를 재사용하면 **완성도 점수가 조용히 내려간다**(규칙 12). 새 칸이 필요 | db push 뒤 |
| 카드 3회 실패 사이트가 「정회원」 표시 | 표시와 실제가 다르다 | 별건 |
| `content/mainplan` 「14일」 29곳 | 기획 문서라 운영 화면은 아님 | 별건 |
| 요금제 이원화 | 35~50시간 · 결정 3건이 먼저 | 회장님 |

### ★ 오늘의 교훈

**「6곳」이 사실은 7곳이었다.** 번호를 가려 놓고 세면 틀린다. 그리고 상무님이 주신
잠금(②)만 걸었으면 **한 곳도 못 막을 뻔했다** — 브라우저로 만든 것은 익명표가 붙기
때문이다. 남이 준 설계라도 **실제 데이터로 한 번은 세어 봐야 한다.**

---

## 2026-09-14 (11) 야간 — 요금·기간 단일출처 · 조사 57곳 · 해지하면 정지 · 요금제 이원화 «준비»

### A. 요금·기간 — ★ 지금 값은 맞다. 무너진 것은 «검사»였다

**A1** `TRIAL_DAYS = 30` · `MEMBERSHIP_PRICE = 49,900` — **랜딩과 같다.**
부회장이 걱정한 「14일·49,000」은 해당 없음. 다만 **단일 출처 파일의 머리말**이 아직
「월 49,000원」이라 **진실의 원본이 스스로 틀린 값**을 말하고 있었다 → 고쳤다.

**A2** ★★ 금액은 지켜지고 있었고 **기간이 무너져 있었다.**
「30일」·「60일」이 **손님 화면과 법무 문서에 글자로** 박혀 있었다 —
첫 페이지 · FAQ · 사업이야기 · 리뷰 · 블로그 · 작동방식 · 약관 · 개인정보처리방침 · 운영자 회원표.

> ★★★ **그리고 검사기가 기간을 아예 안 보고 있었다.**
> 그래서 열 곳 넘게 박히는 동안 매번 「0건 통과」를 찍었다.
> **검사가 조용하면 사람은 지켜지고 있다고 믿는다.** 그것이 제일 위험했다.

`scripts/verify.mjs` 에 기간 검사를 더했다. 지금 **금액 0건 · 기간 0건**.
⚠ 요금과 무관한 숫자(인스타 60일 토큰 · 「최근 30일」 매출 · 「시행 30일 전」 예고)는
파일에 「기간 출처: lib/trial.ts」 한 줄을 달아 **왜 무관한지 적고** 뺐다.

**A3** 만료 크론의 「3일 전·1일 전」이 손으로 박혀 있었다 → `TRIAL_NOTICE_DAYS`.
조회 창도 그 값에서 계산한다(따로 적으면 어긋난다).

### B. 결정 6건 반영

| # | 결정 | 결과 |
|---|---|---|
| 1 | 해지 → 문자 끔 | ✅ **끄는 일은 끄는 자리(해지 창구)에서** 한다 |
| 2 | STOP 문구 + 처리 | ✅ 문구 + **받는 창구**(`/api/sms/inbound`)를 함께 만들었다 |
| 3 | 진짜 6곳만 켬 | ✅ **이미 만족** — 진짜 6곳 on · 시험 9곳 off |
| 4 | 영어 해시태그 | ✅ 체크박스(기본 꺼짐) + 직접 입력칸. 가게 설정이라 한 번 켜면 유지 |
| 5 | 영상관리 최신 판본 | ⚠ 판정만 끝났다 — 아래 «막힘» |
| 6 | db push | 대표님 아침 |

⚠⚠ **B2 는 절반만 살아 있다.** 문구는 나가는데 **솔라피 수신(인바운드) 설정이 아직**이라
사람이 STOP 을 보내도 **아무 데도 도착하지 않는다.** 설정 전에는 그 약속이 빈말이다.

### C. 박팀장이 찾은 것

**C1** ★ 가입 3단계 placeholder 가 「`${업종}를 해요`」로 박혀 있었다 —
세부 업종 **109개 중 57개**가 「고기집**를** · 한식**를** · 미용실**를**」로 깨졌다.
`josa()` 로 받침을 보고 고른다. `scripts/josa-check.ts` 로 109개 전부 확인했다.
SNS 발행 라우트 두 줄(「릴스 **이** 가져갈」)도 같이 고쳤다.

**C2** ★ 약관 제11조가 **없는 버튼을 가리키고 있었다.**
「수정 화면에서 해지」라 했지만 해지 버튼은 **마이페이지에만** 있고 수정 화면에는
「해지」라는 글자조차 없다. 제5조⑤는 이미 「마이페이지」라 **한 약관이 스스로 두 말**을 했다.
★ 없는 버튼을 가리키는 약관은 전자상거래법 제21조의2(가입만큼 쉬운 해지)를 «지킨 척»만 하는 것이다.
⚠ `/privacy` §17 의 메뉴 이름은 **문제 없었다**(실제 메뉴와 같다).

**C3** `/interior2` 메뉴 「작업 사진」 중복 — **아직 안 고쳤다**(막힘).

### F. 요금제 이원화 — 조사만. **코드는 한 줄도 안 건드렸다**

**「요금제는 하나」가 코드 전체의 전제다.** 뿌리는 상수 하나(`MEMBERSHIP_PRICE`)이고
거기서 화면 문구 6종 → 결제 3경로 → 약관 4곳 → 운영자 표까지 **24곳**이 매달려 있다.

★ 다행히 단일 출처 규칙을 지켜 놔서 **금액을 둘로 늘리는 일 자체는 어렵지 않다.**
진짜 빠진 것은 금액이 아니라 **「어느 플랜이 어느 기능을 갖는가」라는 개념 자체**다 —
지금 판정은 「무료 끝났나(expired)」 이분법 하나뿐이고, 기능을 켜고 끄는 **다섯 자리**가
전부 그 한 값만 본다. `sites.plan` 칸은 있으나 항상 `'light'` 만 쓰고 **아무도 안 읽으며**,
제약이 `('light','pro','business')` 라 `'premium'` 은 **애초에 안 들어간다.**

| 묶음 | 무엇 | 예상 |
|---|---|---|
| ① | DB 칸 만들기 (billing.plan·amount · payments.plan · sites.plan 제약 교체) | 1.5시간 |
| ② | 플랜 표와 판정 함수 (`PLANS` · `can(plan, 기능)`) ← **진짜 심장** | 6시간 |
| ③ | 결제 배관 (checkout·subscribe·크론 청구·어드민 전환) | 3.5시간 |
| ④ | 화면 (문구 6종 · 결제 모달 · 홈 가격 섹션) | 12시간 |
| ⑤ | 기능 잠금 다섯 자리 · 채널·한도 | 5시간 |
| ⑥ | 약관 · 운영자 표 · 매출 집계 · 검사기 · FAQ · 온보딩 | 9시간 |
| | **합계** | **35~50시간 (약 1주)** |

★ ①~③까지는 **화면이 안 바뀌므로 대표님 결정이 늦어도 미리 깔 수 있다.**

**⚠ 코드보다 먼저 필요한 결정 셋**
1. 라이트(9,900원)에 **SNS 발행을 줄 것인가** ← ⑤의 갈림길
2. 무료 30일을 **두 플랜 모두에** 줄 것인가
3. 기존 49,900원 정회원을 **프리미엄으로 자동 승계**할 것인가 (안 하면 마이그레이션 한 벌 더)

### ★ F 조사 중 나온 «지금도 틀린 것» — 하나는 고쳤다

🔴 **해지하면 홈페이지가 영원히 공짜로 공개**됐다. 해지는 `billing` 만 바꾸고
`sites.status` 는 그대로라, 정지 블록(trial 만 봄)에도 청구 블록(active 만 봄)에도 **안 걸렸다.**
약관 제11조가 「그 뒤 비공개로 바뀐다」고 **약속한 동작이 코드에 없었다.**
→ 만료 크론에 한 단계를 더했다. **돈 낸 기간(다음 결제일)까지는 그대로 쓰신다.**

⚠ 아직 안 고친 것: **카드가 3번 실패해 정지된 사장님에게 모든 화면이 「정회원」이라고
표시되고 결제 안내가 안 뜬다**(손님 쪽에서는 사이트가 이미 꺼져 있다).

### E. 유튜브 감사 — 찍을 수 있는 상태 (운영 실측)

| # | 확인 | 결과 |
|---|---|---|
| E1 | 심사관 로그인 | ✅ `/login/review` **200** |
| E2 | 게이트 | ✅ **심사용 비공개** · 감사표시 **꺼짐** · 허용 `[sample-interior]` |
| E3 | 데모 영상 | ✅ 홈페이지에 걸려 있다 |
| E4 | 유튜브 | ✅ **연결 가능 — 찍을 수 있다** |

⚠ Vercel 에 `REVIEW_SITE = sample-interior` 한 줄이 아직 필요하다(심사관이 빈 화면을 안 보려면).

---

### ⚠ 막힘 (G3)

| # | 무엇 | 왜 |
|---|---|---|
| 1 | **`npx supabase db push`** | 마이그레이션 둘(`story_caption`·`story_soft_delete`) — 대표님 아침 |
| 2 | **`REVIEW_SITE = sample-interior`** Vercel 한 줄 | 심사관이 로그인해도 빈 화면 |
| 3 | 🔴 **솔라피 수신(인바운드) 설정** | 안 하면 **STOP 이 아무 데도 안 온다** — 문구가 빈말이 된다 |
| 4 | 영상관리 최신 판본 반영 (약 1시간 10분) | 번역 문구 4곳 · [다시 시도] 버튼 · 「지우는 길」 셋. ⚠ 「지우는 길」은 **인스타 앱 실측**이 먼저다(틀린 안내는 없는 안내보다 나쁘다) |
| 5 | 노출/비노출 새 칸 | `visible` 은 점수 칸이라 재사용 불가 — 새 칸 + `db push` |
| 6 | `/interior2` 메뉴 「작업 사진」 중복 | 아직 |
| 7 | 카드 3회 실패 사이트가 「정회원」으로 보이는 것 | 아직 |
| 8 | 기획실 문서(`content/mainplan`)가 아직 「14일」 29곳 | 운영자 전용이라 급하지 않다 |
| 9 | 요금제 이원화 | 결정 셋이 먼저 |

**검사 137건 · 실패 0 · build·verify(금액 0·기간 0)·contrast·lint 통과.**

---

## 2026-09-13 (10) 유튜브 감사 제출 준비 · 랜딩 과장 6곳

### 1~3. 감사 제출 — **찍을 수 있는 상태**가 됐다

| # | 무엇 | 결과 |
|---|---|---|
| 1 | 데모(심사) 사이트 | **`sample-interior`** — 영상 12편 보유, 홈페이지에 한 편 **발행 완료**(실측) |
| 2 | 게이트 | **`review` + 허용 목록 [sample-interior]** · `auditPassed` 는 **꺼진 채** |
| 3 | 스크린샷 4종 | 경로 전부 확인 — 아래 |

**왜 `sample-interior` 인가**
- 진짜 사장님 사이트가 아니다 (`owner_id`·`anon_id` 둘 다 null → **운영자만 편집**)
- **인스타 심사 때 쓰던 그 사이트**라 심사관이 볼 화면이 이미 손질돼 있다(인스타 연결 active)
- 전화번호도 이미 `010-0000-0000` 으로 안전하다

**허용 목록이 실제로 듣는다 (실측)**

| 사이트 | 유튜브 |
|---|---|
| `sample-interior` | ✅ ok · 비공개로 올라간다는 안내 포함 |
| `barun`(진짜 사장님) | ❌ 「준비 중」 |
| `interior2`(진짜 사장님) | ❌ 「준비 중」 |

**찍기 전 확인한 것**
- 구글 동의 주소가 나오고 **로그인 화면까지 열린다** — `redirect_uri_mismatch`·`invalid_client` 없음
- `redirect_uri = https://onstori.com/api/sns/callback/youtube` · `scope = youtube.upload`
- `/privacy` 에 **YouTube API Services 문단** 있음
- ★ 심사관 화면에 뜰 **조사 오류**를 고쳤다 — 「유튜브 쇼츠**을** 먼저 연결」 → 「쇼츠**를**」

### 4. 신청서 6번 「Demo Account」 — ★ **이미 만들어져 있었다**

회장님 확인: 심사관 로그인은 어제 만들었고 Vercel 에 열쇠도 넣었다. 찾아서 확인했다.

| 항목 | 결과 |
|---|---|
| 주소 | **`onstori.com/login/review`** |
| 살아 있나 | ✅ **HTTP 200** — 열쇠(`REVIEW_ID`·`REVIEW_PASSWORD`)가 운영에 들어 있다 |
| 안전장치 | 열쇠가 없으면 **404**(없는 길) · 그 아이디 하나만 · 10분 5회·24시간 30회 제한 · 시간일정 비교 |

⚠ **그런데 막히는 것이 하나 더 있었다** — 그 계정이 **가진 사이트가 하나도 없다.**
저장소의 모든 사이트가 익명·운영자 소유라, 심사관이 로그인해도 **빈 화면**을 본다.
심사는 「이 앱이 정말 그 일을 하는가」를 보는 것이라 빈 화면은 곧 반려다.

★ 그래서 로그인 끝에 **심사용 사이트를 그 계정에 붙이는 걸음**을 넣었다. 조건을 좁게 걸었다:
  ① `REVIEW_SITE` 가 **있어야** 한다(없으면 아무 일도 안 한다)
  ② 그 사이트의 주인이 **아직 없을 때만** 붙인다 — 남의 사이트를 빼앗지 않는다
  ③ 붙이기에 실패해도 **로그인은 안 막는다**

### ⚠⚠ 회장님이 Vercel 에 **한 줄 더** 넣으셔야 합니다

```
REVIEW_SITE = sample-interior
```

넣고 Redeploy 하면, 심사관이 `/login/review` 로 들어오는 순간
그 사이트가 그 계정 것이 되고 — 편집화면·연결·올리기를 전부 해 볼 수 있습니다.


