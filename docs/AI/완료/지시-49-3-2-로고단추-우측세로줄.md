# [49]③ 우측 로고 단추 — 처리함 · 2026-09-18 (로고 파일이 와서 «막혀 있던 절반»을 마저 했다)

지시: `반장-지시.md` [49]③ + `대표님지시-2026-09-18.md` **B-19**(로고 위치) · **B-12**(숫자 폐기)

## ① 무엇을 했나

**가. 공식 로고 두 장을 저장소에 넣었다**

| 넣은 파일 | 어디서 온 것 | 크기 |
|---|---|---|
| `public/brand/instagram-white.svg` | Meta 공식 **White Glyph**(대표님이 주신 `insta logo/02 White Glyph`) | SVG(1.8KB) |
| `public/brand/youtube-white.png` | Google 공식 **White icon**(`youtube logo/03 White`)을 96px 로 줄인 것 | PNG(1.4KB · 96×82) |

🔴 **직접 그리지 않았다**(상표). 색도 안 바꿨다 — 대표님 조건(**SVG·흰색·컬러 없음**) 그대로다.
⚠ **유튜브는 공식 SVG 가 «없다»** — 준 폴더에 `.ai`·`.eps`·`.png` 뿐이다. 그래서 PNG 다.
  22px 로만 쓰므로 96px 원본이면 **고해상도 폰에서도 충분**하다. 더 키워 쓰지 마라.

**나. 「인스타에서 보기」 글자 알약 → 오른쪽 세로줄의 «로고 단추»**

```
전 : 좌하에 [● 인스타에서 보기] [● 쇼츠에서 보기]   ← 알약 하나가 120px. 폰에서 영상을 가림
후 : 우측 세로줄에 로고 원형 단추 2개(연결된 것만)  ← 릴스·쇼츠와 같은 자리
```

- **숫자(♡·💬)는 그리지 않는다** — 대표님 B-12 확정(폐기). `Reactions` 코드도 걷었다.
- **연결된 채널만** 그린다(`pillLinks` — 인스타·유튜브 둘뿐) ⇒ **0~2개**. 하나도 없으면 줄 자체가 없다.
- 로고 파일이 없는 채널이 들어오면 **그 단추만 안 그린다**(깨진 그림이 영상 위에 남는 것보다 낫다).

## ② 실측 (PC 1280×860 · 폰 375×812 둘 다)

| 잰 것 | PC | 폰 |
|---|---|---|
| 로고줄 — 영상 오른쪽에서 | 16px | 16px |
| 로고줄 — 바닥에서 | 90px | 90px |
| [숏폼 나가기] 와 겹침 | 없음(31px 여유) | 없음 |
| 제목 오른끝 ↔ 로고줄 왼끝 | **32px 여유** | **32px 여유** |
| 인스타 로고가 실제로 그려짐 | ✅ 150×150 → 22px | ✅ |
| 유튜브 로고 파일 | ✅ 200 · image/png | ✅ |
| 옛 알약(`.world-pill`) 남았나 | 0개 | 0개 |

## ③ 무엇을 «못» 했나 · 지시와 다르게 한 것

1. 🔴 **[49]③ 은 「PC 는 영상 «바깥» 오른쪽」이라 했는데 «안»에 뒀다.**
   2026-09-17 [42]§2 에서 **프레임 밖에 뒀다가 PC 에서 영상과 415px 떨어져 남남**이 된 실측이 있다.
   영상 옆에 붙어야 「이 영상의 링크」로 읽힌다. ⇒ **안쪽 16px** 로 뒀다. **대표님 한 마디면 바깥으로 옮긴다.**
2. **유튜브 링크가 달린 영상을 실제로 못 봤다** — 샘플 사이트의 이번 영상에는 인스타 링크만 있었다.
   그림·자리는 같은 규칙이라 문제는 없겠으나 **두 개가 나란히 선 모습은 못 봤다.**
3. **카드형태(`.shorts-pill`)의 글자 알약은 그대로 두었다.** [49]③ 은 몰입모드 이야기이고,
   카드형태는 알약이 작은 카드 «안»이라 영상을 덜 가린다. 대표님 지시가 오면 그때 같이 바꾼다.

## ④ 건드린 파일

```
public/brand/instagram-white.svg      (새 파일 · 공식)
public/brand/youtube-white.png        (새 파일 · 공식 PNG 를 96px 로)
components/sections/shorts-world.tsx  좌하 알약 → 우측 로고줄 · Reactions 걷어냄
app/globals.css                       .world-links/.world-pill/.world-react* → .world-side/.world-logo
```

## ⑤ 검사

```
tsc 0 · build 0 · eslint 0 · 검사 11건 실패 0
```

저는 클코님입니다.
한줄요약 : 대표님이 주신 공식 로고 두 장을 넣고, 영상을 가리던 「인스타에서 보기」 글자 알약을 우측 세로줄의 로고 단추로 바꿨습니다(숫자는 B-12 대로 폐기).
