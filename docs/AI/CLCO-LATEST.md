# 클코님 — 가장 최근 답변

> 2026-09-11 · 마디: SNS 연결·업로드·해제 화면 (5덩어리)

---

다섯 덩어리 전부 끝내고 push 했습니다.

## ⚠ 먼저 — 못 읽은 것이 있습니다

**상무님 어댑터 규격서와 김팀장 조사를 못 읽었습니다.** `fable51plandept/AI_Context/` 폴더가 아직 없고, 저장소에도 SNS·어댑터 문서가 없습니다.

회장님 지시문에 규격이 상세히 적혀 있어 **그것만 근거로** 만들었고, 지시문에 없는 것은 코드에 **「⚠ 확인 필요」**로 표시해 뒀습니다. 짐작으로 채우지 않았습니다.

## 만든 것 — 커밋 5개

| 덩어리 | 커밋 | 무엇 |
|---|---|---|
| ① 표 둘 | `f0c1c07` | sns_connections · sns_posts (마이그레이션만) |
| ② 어댑터 | `f84b9bf` | 겉모양 8개 + 인스타·유튜브 + 서랍 4칸 |
| ③ 연결 화면 | `5a874d1` | 영상 메뉴 → [SNS 연결] + 라우트 5개 |
| ④ 인스타 올리기 | `c380f25` | 컨테이너→확인→게시 + 규격 검사 |
| ⑤ 유튜브 | `5aa7c9e` | 어댑터 완성 + 게이트 손잡이 |

## ★ 제일 신경 쓴 것 세 가지

**1. 토큰이 손님 브라우저에 못 가게 — «말»이 아니라 «타입»으로 막았습니다**

`Connection` 타입에 **토큰 칸이 아예 없습니다.** 화면도 API 응답도 이 타입만 씁니다. 실수로도 실을 수 없습니다. 토큰은 `lib/sns/db.ts` 의 `readTokens()` 한 곳에서만 꺼냅니다.

**2. 같은 영상이 두 번 올라가지 않게 — 세 겹**

| 겹 | 무엇 |
|---|---|
| DB | 「살아 있는 시도」에만 걸리는 유일 인덱스 — 경쟁 상황에서도 두 줄이 안 생김 |
| 코드 | `isDuplicate()` 를 시작 전에 확인 |
| ★ 핵심 | `container_id` — ②에서 끊겨도 **①을 다시 하지 않음** |

★ 그리고 **서버가 5분을 기다리지 않습니다.** 서버리스가 5분을 붙들면 시간 초과로 끊기는데, **그 끊김이 곧 두 번 올리기의 원인**입니다. 서버는 「지금 됐나」만 보고 즉시 답하고, 화면이 1분 간격으로 최대 5번 이어 갑니다.

**3. 조용히 실패하지 않게**

- 못 쓰는 SNS 는 **왜 못 쓰는지**를 그 자리에 씁니다 (「유튜브는 아직 준비 중이에요」)
- 못 쓰는 것은 **체크가 아예 안 눌립니다**
- 오류는 네 값뿐 — **모르는 것은 전부 TRANSIENT**. 「모르니까 거절」로 단정하면 다시 하면 될 일을 영영 포기하게 만듭니다
- 영상 규격은 **파일을 직접 열어** 잽니다(크기·가로폭·moov 위치). ⚠ **못 잰 것은 막지 않습니다** — 모른다고 멀쩡한 영상을 버리면 안 됩니다

## 지킬 것 — 전부 통과

| 항목 | 결과 |
|---|---|
| 손님 사이트 JS | **577.7 KB → 577.7 KB · 0 B 증가** (SNS 문자열 0곳 확인) |
| 토큰이 손님 브라우저에 | 타입으로 차단 |
| 조용한 실패 | 없음 — 모든 실패에 이유 |
| build · verify · rec-contrast · lint | 전부 통과 · lint 신규 0 |
| 라우트 8개 | 빌드에 모두 잡힘 |

## ▶ 회장님이 해 주셔야 할 것 — 3가지

**1. `npx supabase db push`** — 표 둘이 아직 안 만들어졌습니다. 이걸 돌리기 전에는 연결 화면이 비어 보입니다.

**2. 열쇠 4개를 Vercel 환경변수에 넣어 주십시오**

```
META_APP_ID
META_APP_SECRET
GOOGLE_OAUTH_CLIENT_ID
GOOGLE_OAUTH_CLIENT_SECRET
```

**3. 돌아올 주소를 메타·구글에 등록해 주십시오** (글자까지 같아야 합니다)

```
https://onstori.com/api/sns/callback/instagram
https://onstori.com/api/sns/callback/youtube
```

## ⚠ 제가 확인 못 한 것 — 숨기지 않고 적습니다

| 무엇 | 왜 |
|---|---|
| **화면을 눈으로 못 봤습니다** | 편집화면은 주인 로그인이 필요합니다. 쿠키를 다시 심으려면 회장님 허락이 필요해 안 했습니다 |
| **API 엔드포인트·권한 목록** | 김팀장 조사를 못 읽었습니다. 코드에 「확인 필요」로 표시했습니다 |
| **표 칸 구성** | 상무님 규격서를 못 읽었습니다. 지시문의 요구사항만으로 설계했습니다 |
| **review 와 on 의 경계** | 심사 중 요구되는 공개 범위를 모릅니다. 지금은 review=비공개, on=공개 |

## ⚠ 알고 계셔야 할 제약 2가지

**1. SNS 연결은 로그인이 필요합니다.** 익명으로 홈페이지만 만드신 사장님은 못 합니다 — SNS 에서 돌아올 때 「누구의 홈페이지인지」를 세션으로만 확인할 수 있기 때문입니다. 화면이 **미리** 알려 줍니다.

**2. 올리는 중에 화면을 닫으면 「받는 중」으로 남습니다.** 다시 들어오시면 이어집니다. 자동으로 마무리하는 크론은 아직 없습니다 — 만들지 결정이 필요합니다.

---

**전체 진행상황:** SNS 화면 5덩어리 완료. **db push + 열쇠 4개 + 주소 2개**가 들어가면 실제 연결을 시험할 수 있고, 그 성공 화면이 곧 네 심사의 증빙이 됩니다.
**예상시간:** db push 2분 · 열쇠 등록 10분 · 첫 연결 시험 10분
**지금 현재 추천하는 모델: Opus 5 · effort: 높음**

저는 클코님입니다.
한줄요약 : SNS 연결·업로드·해제 화면을 다섯 덩어리로 전부 만들어 push 했고, 손님 사이트 JS 0바이트 증가·토큰 타입 차단·두 번 올리기 3중 방지를 확인했으며 db push 와 열쇠 4개가 남았습니다.
