# 2026-09-11 — 클코님 답변 기록

> 기록 규칙이 이 날 도입됐습니다. **이 날 앞선 답변 3건**(네트워크 오류 조사 · 화면 대비 · 카메라 전환)은
> 여기 없고 `docs/PROGRESS.md` 와 커밋 기록에 남아 있습니다.

---

## 03:20 · docs/AI 기록 체계 셋업

셋업 끝났습니다.

## 만든 것

| 파일 | 하는 일 | 지금 상태 |
|---|---|---|
| `docs/AI/CLCO-LATEST.md` | **답변 하나를 통째로** 담는다 — 답변할 때마다 덮어씀 | 이 답변이 들어 있습니다 |
| `docs/AI/LOG/2026-09-11.md` | 오늘 답변을 **아래로 이어붙임** | 이 답변이 첫 줄입니다 |
| `docs/AI/CLCO-HISTORY.md` | 마디마다 5~10줄 요약 | 오늘 마디 2개 기록 |

**원격 PC에서 쓰시는 법:** `onstori/docs/AI/CLCO-LATEST.md` 를 열고 **Ctrl+A → Ctrl+C**. 터미널을 긁지 않으셔도 됩니다.

표는 **마크다운 표로만** 씁니다. `┌─┬─┐` 같은 그림 문자는 넣지 않습니다 — 복사하면 깨집니다.

## CLAUDE.md 에 적은 자리

**§3-2** 로 넣었습니다. 서명 규칙(§3-1) 바로 아래입니다 — 「답변을 어떻게 마무리하나」가 한자리에 모입니다.

## ⚠ 하나 못 봤습니다 — 다른 팀 파일이 아직 없습니다

`fable51plandept\AI_Context\` **폴더 자체가 없습니다.** KIM · PARK · TECH 의 LATEST.md 도 없습니다.

지금 「김팀장 최신 확인해」라고 하시면 저는 **「없습니다」**라고 답할 수밖에 없습니다. 경로 규칙은 적어 뒀으니 그쪽 팀이 파일을 만들면 그때부터 바로 읽힙니다. **읽기만 하고 고치지 않습니다.**

## 기록 시작 시점

회장님 지시대로 **이 답변부터** 남깁니다. 오늘 앞서 드린 답변 셋(네트워크 오류 조사 · 화면 대비 21곳 · 카메라 전환)은 로그에 없습니다. 대신 `docs/PROGRESS.md` 와 커밋 4개에 남아 있습니다.

## 로그가 커지면

지금은 git 에 함께 넣습니다. 하루 한 파일이라 한 달에 30개 남짓입니다. **200개를 넘으면 제가 먼저 여쭙겠습니다** — 그때 .gitignore 로 뺄지 정해 주시면 됩니다.

세 파일과 CLAUDE.md 는 **한 커밋에 함께** 넣었습니다(규칙 4).

---

**전체 진행상황:** V-1 은 코드·저장소 설정 모두 배포 완료. **회장님 재촬영 한 번**이 최종 합격 기준이고 그것만 남았습니다. 이번 셋업은 그와 별개인 기록 체계입니다.
**예상시간:** 셋업 완료(5분). 다음은 회장님 촬영 5분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통**

저는 클코님입니다.
한줄요약 : 답변을 파일로 남기는 docs/AI 체계(LATEST · HISTORY · LOG)를 만들고 CLAUDE.md §3-2 에 규칙을 박아 커밋했습니다.

---

## 03:40 · 통신판매업신고번호 반영

넣었습니다. **그리고 빠진 곳을 하나 찾았습니다 — 결제하는 화면이었습니다.**

## 번호가 들어간 곳 — 셋이 아니라 **넷**

`config/company.ts` 한 줄을 고쳤고, 실제로 찍히는지 빌드 결과물에서 **글자로 세어** 확인했습니다.

| 화면 | 회장님이 아신 곳 | 확인 |
|---|---|---|
| 개인정보처리방침 `/privacy` §1 | ✅ | 번호 발견 |
| 이용약관 `/terms` 제1조 | ✅ | 번호 발견 |
| 본사 푸터 (첫 페이지·FAQ·작동방식 등 **9개 페이지 전부**) | ✅ | 번호 발견 |
| ★ **결제 모달** | — **없던 곳** | 새로 넣음 |

## ★ 왜 결제 화면이 빠져 있었나

전자상거래법 제13조는 **계약을 맺기 «전»에** 파는 사람이 누구인지 알리라고 합니다.
그런데 결제 모달은 `/my` 와 `/{상호}/edit` 두 화면에서 뜨는데, **그 두 화면에는 푸터가 없습니다.**

즉 **사장님이 돈을 내겠다고 카드를 등록하는 바로 그 순간, 파는 사람이 누구인지 화면 어디에도 없었습니다.**

결제 버튼 아래에 한 줄 넣었습니다 — 상호·대표·사업자등록번호·통신판매업신고·전화 + 「이용약관」·「개인정보처리방침」 링크(새 탭으로 열립니다. 같은 탭으로 나가면 결제가 끊깁니다).

## ⚠ 손님 사이트에는 **일부러 안 넣었습니다**

`/{상호}` 손님 홈페이지의 사업자는 **사장님이지 이안월드가 아닙니다.** 거기에 우리 번호가 뜨면 거짓말이 됩니다.

손님 사이트가 본사 푸터를 쓰지 않는지 확인했고(별도 파일을 씁니다), 빌드된 샘플 사이트에서 우리 번호·사업자등록번호가 **0곳**인 것을 확인했습니다.

## 같이 정리한 낡은 기록 5곳

번호가 없다는 전제로 쓰인 글들이 여기저기 남아 있었습니다.

| 파일 | 뭐가 낡았나 |
|---|---|
| `app/privacy/page.tsx` 주석 | 「발급 전이라 임시 문구」 |
| `config/admin-notes-seed.ts` | 「3~4일 내 발급 예정」 · 일정표 |
| `docs/PROGRESS.md` | 「응답 대기」 · 대기 항목 6번 |
| `docs/PLAN.md` | 토스 가맹 「보류: 통신판매업 신고 후」 → **보류 해제** |
| `content/mainplan/data.js` | 작업표 13번 → 「토스 가맹만 남음」 |

`docs/DECISIONS.md` 에도 결정 한 줄을 남겼습니다.

⚠ 어드민 메모장은 **회장님이 한 번이라도 저장하셨으면 저장된 값이 이깁니다.** 화면에 옛 글이 그대로 보이면 거기서 직접 고치셔야 합니다.

## ★ 회장님께 여쭐 것 2가지

**1. 번호 표기를 신고증과 대조해 주십시오**

주신 값은 「제 2026-다산-0825 호」였습니다. 공문서 관례대로 **띄어쓰기만 없애** `제2026-다산-0825호` 로 넣었습니다.

⚠ 다만 지자체 표기가 **「다산」이 맞는지 「경기남양주」인지** 신고증 원본을 한 번 봐 주십시오. 주소를 사업자등록증 글자에 맞춘 것과 같은 이유입니다 — 법정 표시 항목은 원본과 글자가 같아야 심사·분쟁에서 다툴 자리가 없습니다. 다르면 한 줄만 다시 고치면 됩니다.

**2. 구매안전서비스(KB에스크로) 표시가 화면 어디에도 없습니다**

어드민 메모에 「구매안전서비스 이용확인증 — KB에스크로, 2026-09-03 등록」이 있는데, **홈페이지에는 그 사실이 안 적혀 있습니다.**

우리는 월 구독을 **미리 받는** 선불식이라, 전자상거래법상 결제대금예치 사실을 **표시**해야 합니다. 이번 건과는 별개라 임의로 넣지 않았습니다 — **넣을까요?** 넣는다면 푸터와 결제 모달 두 곳이 맞습니다.

## 검증

build ✅ · tsc ✅ · verify ✅ · **lint 신규 0**
(`app/privacy/page.tsx:136` 에 lint 오류가 1건 있는데 **제 변경 전부터 있던 것**입니다. 확인하고 손대지 않았습니다.)

---

**전체 진행상황:** 통신판매업 번호 반영 완료 → **토스 가맹을 막던 서류가 없어졌습니다.** V-1 은 배포 완료 상태로 회장님 재촬영만 기다리고 있습니다.
**예상시간:** 이 작업 완료. 다음은 ①신고증 표기 대조(1분) ②에스크로 표시 여부 결정 ③V-1 재촬영(5분)
**지금 현재 추천하는 모델: Opus 5 · effort: 보통**

저는 클코님입니다.
한줄요약 : 통신판매업신고번호를 config/company.ts 에 넣어 네 화면에 반영했고, 판매자 정보가 아예 없던 결제 모달을 찾아 함께 채웠습니다.

---

## 15:05 · 카메라 전환 NotAllowedError 근본 수정

고쳤습니다. **전환에 기대지 않는 구조로 바꿨습니다.**

## 먼저 — 회장님이 가장 의심하신 것, 코드에서 확인했습니다

| 회장님 의심 | 코드 사실 |
|---|---|
| 옛 카메라를 먼저 놓는가? | **아니었습니다.** 445행에서 새 것을 먼저 열고, 458행에서 **성공한 뒤에만** 놓고 있었습니다 |
| 화면이 가려질 때 스트림을 끊나? | `visibilitychange`·`blur` 처리 **없음** |

즉 회장님이 말씀하신 그 순서는 **이미 그렇게 돼 있었는데도 실패**했습니다.

## ★ 그리고 결정적인 계산이 나왔습니다

코드상 실패해도 `camId`가 안 바뀌고(460행에서만 바뀜), 카메라 목록도 다시 세지 않습니다(367행 한 번뿐).

→ **버튼을 몇 번을 누르든 «완전히 똑같은 요청»을 보냅니다.**
→ 그런데 어떤 때는 실패하고 어떤 때는 성공합니다.
→ **널뛰는 곳은 우리 코드의 값이 아니라 브라우저 권한 쪽입니다.**

회장님 관측과 정확히 맞습니다 — 「허용이 돌아오기 전에 실패로 처리된 것 아닌가」, 「매번 권한이 사라지는 것 아닌가」. 우리가 고칠 수 있는 값은 다 똑같았습니다.

## ★★ 그래서 근본을 바꿨습니다 — 전환이 «필요 없게»

권한을 다시 묻는 것이 문제라면, **다시 묻지 않으면 됩니다.**

**카메라를 켜기 «전»에 어느 쪽으로 찍을지 먼저 여쭙습니다.**

```
어느 쪽으로 찍을까요?
[● 매장·제품]  [○ 내 얼굴]
얼굴이 안 나와요    앞쪽 카메라
```

그 다음 [카메라·마이크 켜기]를 누르면 **그 카메라로 바로 켜집니다.**
→ 허락을 묻는 것이 **딱 한 번**. 회장님 합격 기준 그대로입니다.

★ **기본을 「매장·제품」(뒷면)으로 뒀습니다.** 회장님 말씀대로 얼굴이 화면에 뜨는 것이 말문을 막는 가장 큰 벽이고, 현장을 비추는 쪽이 손님에게도 더 좋은 영상이기 때문입니다. 앞면으로 찍고 싶으시면 켜기 전에 한 번 누르시면 됩니다.

## 전환 버튼도 고쳤습니다 (없애지 않았습니다)

| 무엇 | 전 | 후 |
|---|---|---|
| 재시도 조건 | 「이미 쓰는 중」 계열만 | **오류 종류를 안 가린다.** NotAllowedError 도 재시도 |
| 재시도 방법 | — | 마이크는 **그대로 두고** 카메라 자리만 비운 뒤 다시 연다 |
| 되살리기 | 막힌 그 방식(deviceId)으로 | 막히면 **처음 켤 때와 똑같은 요청**으로 되돌린다 |
| 두 번 다 막히면 | 버튼이 남아 있었다 | **버튼을 숨긴다** (회장님 지시) |
| 갇히면? | — | **[앞·뒤 다시 고르기]** 로 검증된 경로로 데려간다 |

⚠ 마이크를 놓지 않는 이유: 소리 녹음이 살아 있으면 「촬영 중」이 안 끊겨서, 브라우저가 잡아 둔 허락도 함께 풀리지 않습니다. **카메라 자리만 비워 주는 것**입니다.

## 사실을 남기게 했습니다

- **권한 상태를 미리 읽습니다.** 「곧 물어요」 / 「이미 허용돼 있어요」 / 「차단으로 저장돼 있어요 — 자물쇠 🔒 → 권한 → 카메라」 를 **맞게** 말합니다.
- 실패하면 화면에 사실 한 줄이 남습니다:
  `전환 실패 · ①NotAllowedError ②NotAllowedError · 권한 granted · 카메라 2대 · 되살림 성공`
  ⚠ Vercel 기록을 제 컴퓨터에서 못 읽습니다. **화면이 유일한 창구**라 거기 남깁니다.
- 같은 내용을 서버에도 보냅니다.
- 고른 쪽과 **실제로 켜진 쪽이 다르면** 조용히 넘어가지 않고 말합니다(회장님 지적 1 — 안드로이드에서 facingMode 가 무시되는 기기).

## 검증 — 회장님 폰 상황을 흉내 내 4갈래를 돌렸습니다

| 상황 | 한 번 눌러서 | 미리보기 | 마이크 재요청 |
|---|---|---|---|
| 정상 | 전환 성공 | live | 0회 |
| ★ **다른 카메라가 열려 있으면 거절하는 폰** (회장님 상황) | **전환 성공** | live | 0회 |
| deviceId 가 영영 막힌 폰 | 실패하되 버튼 숨김 | **live (되살아남)** | 0회 |
| 탈출 경로 | 앞·뒤 다시 고르기 → 켜기 1회 | live | — |

★ 세 번째 갈래에서 처음엔 **미리보기가 죽었습니다.** 되살리기도 똑같이 막힌 방식을 쓰고 있었기 때문입니다. 그것도 고쳐서 지금은 살아납니다.

**검사:** 손님 사이트 JS 577.7 KB **변동 0 B** · 390/768/1097 넘침 0 · 대비 46건 미달 0 · build·tsc·verify·lint 통과

## ⚠ 제 잘못 하나 더 말씀드립니다

지난번 「일꾼 8개」는 여쭙지 않고 띄운 것이 맞습니다. **이번은 혼자 했고, 아까 돌던 조사도 중단시켰습니다.** 앞으로 여럿 띄우기 전에 먼저 여쭙겠습니다.

## ▶ 다시 찍어 주십시오

```
https://onstori.com/rec/sample-interior?k=2958.2e49a437ff53
```

**이번에 봐 주실 것:**
1. 「영상으로」 고르면 **「어느 쪽으로 찍을까요?」** 가 나오는지 — 기본이 「매장·제품」인지
2. [카메라·마이크 켜기] → **허용 한 번**으로 바로 켜지는지
3. 켜진 카메라가 **정말 뒷면**인지 (아니면 화면이 그렇게 말해 줍니다)
4. [카메라 전환]을 눌렀을 때 **한 번에** 바뀌는지
5. 실패하면 화면 아래 **사실 한 줄**(「전환 실패 · ①… ②… · 권한 …」)을 그대로 알려 주십시오

그다음 아직 못 보신 넷(글씨·카톡 경고·보내기·표지)도 이어서 봐 주십시오.

---

**전체 진행상황:** V-1 코드·저장소 설정 모두 배포 완료. 남은 것은 회장님 실측 한 번입니다.
**예상시간:** 촬영·확인 5분
**지금 현재 추천하는 모델: Opus 5 · effort: 엑스트라** (이번 실측에서도 안 되면 Fable 5.1 로 올리겠습니다)

저는 클코님입니다.
한줄요약 : 매번 같은 요청인데 결과가 널뛰는 것을 코드로 확정해 «전환에 기대지 않는 구조»로 바꿨습니다 — 카메라를 켜기 전에 앞/뒤를 골라 허용을 한 번만 누르면 됩니다.
