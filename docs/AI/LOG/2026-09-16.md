

## 2026-09-16 새벽 — 로고 만들기 전면 개편 (권반장 지시 [5])

> 이 문서에 적힌 요금·기간 숫자는 **작성 당시 값**이다. 지금 값은 `lib/trial.ts` 가 진실이다.

**배포 완료** `1c9821c` · `npm run check` **0** · `npx tsc --noEmit` **0** · `npm run build` **0**

### 무엇을 했나

옛 `wordmarks()` 4안을 통째로 걷어내고 지시하신 순서 그대로 다시 만들었다.
①파일 첨부(올리면 아래가 접힌다) → ②이름 → ③한글/영문 → ④모양 5 → ⑤글꼴 5 → ⑥심볼 15·이니셜

| 새 파일 | 무엇 |
|---|---|
| `lib/logo-maker.ts` | 모양·글꼴·심볼의 **단일 출처**. SVG 문자열만 만든다 |
| `lib/logo-embed.ts` | 저장할 때 **글꼴을 심는다** |
| `app/new/logo-picker.tsx` | 화면 전부 |
| `scripts/build-font-ranges.mjs` | 「글자 → 글꼴 조각」 지도 (prebuild) |
| `scripts/logo-test.ts` | 검사 29건 (`npm run check` 에 들어감) |
| `scripts/logo-sheet.ts` | 눈으로 볼 한 장 — `npx tsx scripts/logo-sheet.ts` |

### 만들면서 찾은 것 셋 — 이게 더 중요하다

**① 미리보기와 저장된 로고가 달라질 뻔했다.**
저장된 로고는 `<img>` 로 쓰이는데 **그렇게 쓰인 SVG 는 바깥 글꼴을 못 부른다.**
사장님 PC 에 Pretendard 는 거의 없으니 실제로는 「맑은 고딕」이 된다.
⇒ 미리보기는 **인라인 SVG**(웹폰트 먹음), 저장할 때는 **글꼴을 심는다**(30~60KB).

**② 🔴 `/fonts.css` 가 404 다.** (운영에서 실측)
Next 가 `app/fonts.css` 를 번들에 넣어 버려 주소로 못 받는다. 모르고 뒀으면
**글꼴 심기가 조용히 실패**해 ①이 그대로 재발했을 것이다.
⇒ 지도를 빌드 때 뽑아 `public/fonts/pretendard/ranges.json` 으로 둔다(`prebuild`).

**③ ⚠ 글꼴 5개 중 실물이 있는 것은 1개뿐이다.** — 아래 「남은 것」 참고

### ⚠ 한계 — 대표님이 아셔야 할 것

| 글꼴 | 파일 | 실제로 |
|---|---|---|
| **기본**(Pretendard) | ✅ 자체 호스팅 | **어디서 봐도 똑같다**(심어 넣음) |
| 아주 굵게·둥글게·명조·손글씨 | ❌ 없음 | 그 글꼴이 **깔린 PC 에서만** 그렇게 보인다 |

넷은 저장소에도 R2 에도 파일이 없다. 자간·굵기를 달리해 **골라도 티는 나게** 했지만
「고른 그 글꼴」이 보장되지는 않는다.

⚠ Pretendard 는 굵기 축이 **400~700** 으로 좁혀져 있어(`app/fonts.css`) **900 이 없다.**
「최대 굵은 워드」의 실제 최대는 700 이다.

⚠ 「펜타그리드」는 저장소에서 **못 찾았다** — 권반장 보고가 맞다. 그 이름의 글꼴·파일이 없다.

### ⚠ 못 한 확인

**`/new` 를 브라우저로 끝까지 통과해 보지 못했다.** 내장 브라우저가 `localhost` 를
`https` 로 바꿔 버려 연결이 안 됐다. 대신 로고 출력을 **검사 29건 + 눈으로 볼 14장**으로 증명했다.
⇒ 다음 세션에서 `/new` 실통과를 **먼저** 하는 것을 권한다.

### 권반장 지시함 — 남은 것 넷

| | | 예상 |
|---|---|---|
| [6] | 첫 화면 이미지 교체 + 프롬프트 | 40분 |
| [7] | 편집화면에서 채널 고치는 길 | 30분 |
| [2] | 가입에 사진 올리기(15장) | 1시간 |
| [3] | 🔴 가입에 문의 채널(CTA) — 본체 | 2시간 |

★ [4] 는 **권반장이 이미 했다.** 중복하지 말 것.

### ⚠ 미커밋 변경 9개는 «내 것이 아니다»

세션 끝 시점에 `CLAUDE.md`·`app/[slug]/edit/*`·`app/page.tsx` 등 **9개**가 미커밋 상태다.
`c36462c`(권반장 밤 워크플로) 이후에 생긴 것이라 **권반장이 작업 중인 것으로 보인다.**
**건드리지 않았다.** 내 로고 작업은 전부 `1c9821c` 로 커밋·푸시됐다.

---

저는 클코님입니다.
한줄요약 : 로고를 모양 5·글꼴 5·심볼 15로 전면 개편해 배포했고, 「저장된 로고가 미리보기와 달라지는 문제」와 「/fonts.css 가 404 라 글꼴 심기가 조용히 실패하는 구멍」을 함께 막았습니다.


---

# 클코님 마지막 답변 — 2026-09-16

지시함의 **[8] · [9] · [10] 세 건을 다 끝냈습니다.** 지시 하나마다 보고서를 따로 남기고 따로 커밋했습니다.

## 전체 진행상황

| 지시 | 무엇 | 상태 | 커밋 |
|---|---|---|---|
| **[8]** | 예시 사이트를 사이트맵에서 빼기 | ✅ 끝 | `8fc5f3b` |
| **[9]** | `/admin/copy` 저장값을 손님 화면이 읽게 배선 | ✅ 끝 | `fb896e6` |
| **[10]** | 「내 도메인(.com) 연결」 기술 검토 (문서만) | ✅ 끝 | `9f73ff4` |
| — | WAITING.md 에 새로 드러난 것 다섯 줄 | ✅ 끝 | `7cfc338` · `ceec1f4` · `e81f789` |

**검사: `npm run check` 0 · `npx tsc --noEmit` 0 · `npm run build` 0** (마지막 상태 기준)

⚠ **`git pull` 은 할 것이 없었습니다.** `origin/main` 이 오히려 **12개 뒤**에 있었고,
말씀하신 두 병합(T-0021 `003611a` · T-0025 `3434a06`)은 **이미 로컬 main 에 있었습니다.**
작업 중에 `8648c54`(T-0031)가 하나 더 들어왔고, 그 뒤에도 검사 셋이 전부 0인 것을 다시 확인했습니다.

⚠ **`app/new/wizard.tsx` 와 `app/[slug]/edit/*` 는 한 글자도 안 건드렸습니다.**

---

## [8] 예시 사이트 — **진짜로 위험했습니다**

운영 DB 를 직접 봤습니다. `sample-interior` 는 **`active`(결제한 곳)** 이고 전화도 첫 편집도 있습니다.
**완성도 문턱(75점) 하나만이** 막고 있었고 지금 점수가 65점입니다. 유튜브 심사용으로 채우면 넘습니다.

문턱을 잠깐 낮춰 흉내 내 보니 **고치기 전에는 `onstori.com/sample-interior` 가 사이트맵에 실렸고**,
고친 뒤에는 빠지면서 로그에 `"sample":1` 이 남습니다.

### 🔴 지시대로 «못» 한 것 하나

지시는 「`sites` 조회 `select` 에 `sample` 을 더하라」였는데 **그 표에 `sample` 칸이 없습니다.**
적었으면 PostgREST 가 400 을 내고 그 오류를 `catch` 가 삼켜 **사이트맵이 통째로 비었을 것**입니다
— 「예시 하나가 빠진다」가 아니라 **손님 사이트 전부가 구글에서 사라지는** 사고였습니다.

대신 예시 여부의 단일 출처인 `lib/sites.ts` 의 **`SAMPLE_SLUGS`** 로 걸렀습니다.

### 🔴 덤 — `main` 이 이미 타입 검사에서 깨져 있었습니다

`lib/sites.ts(260,9): Cannot find name 'isPremade'` — **제 변경과 무관합니다**(stash 한 상태에서도 납니다).
`5eaaf47`(「`/g/` 폐기」)이 import 한 줄을 **함께 지웠는데** 260줄의 사용처는 남아 있었습니다.
**부르는 줄 한 줄만** 되살렸습니다. `lib/premade.ts` 는 안 건드렸습니다(09-13 P0 자물쇠).

---

## [9] 카피 배선 — **4곳이 아니라 5곳이었습니다**

### 문장은 한 글자도 안 바꿨습니다 — 증명했습니다

`admin_notes` 에 `id="config"` 행이 **없습니다.** = 대표님이 아직 한 번도 저장하신 적이 없습니다.
그래서 지금은 코드 기본값으로 돌아야 합니다. `git stash` 로 제 변경을 뺀 화면과 되돌린 화면의
**SNS 문장을 글자 그대로 대조**했더니 **네 화면 모두 「같음」** 이었습니다.

흉내로 저장값을 넣으면 **다섯 화면 전부** 새 값을 읽고, 옛 값이 남은 곳은 **0곳**입니다.

### 🔴 목록에 없던 다섯 번째

첫 페이지 「**6곳 동시 발행**」 카드(`CHANNELS_PITCH`)가 빠져 있었습니다. 그냥 뒀으면
저장 뒤 **같은 첫 페이지 안에서** 자주묻는질문은 새 문구, 위 카드는 옛 문구가 됩니다. 함께 배선했습니다.
(되돌리기 쉽게 옛 상수는 그대로 남겼습니다.)

### 🔴 저장해도 한 시간 안 바뀔 뻔했습니다

`/faq` · `/how-it-works` · `/our-story` 는 **ISR 1시간**인데 저장 라우트가 `/` 하나만 풀고 있었습니다.
네 경로를 전부 풀도록 고쳤습니다. — **안 고쳤으면 이 지시의 병이 그대로 남았습니다.**

### 🔴 말씀만 드리고 «안» 고친 것 셋

1. **`/how-it-works` 1단계에 `${COPY.trialShort}` 가 «글자 그대로» 손님에게 보입니다.**
   그 줄만 백틱이 아니라 큰따옴표입니다(옛 버그). **한 글자 작업**이지만 손님이 보는 글이라 안 고쳤습니다.
2. `/admin/seo` ④칸의 「바꾸는 곳은 한 곳입니다」 설명이 이제 반만 맞습니다. 어드민 화면입니다.
3. **`copyright`(「Made with 온스토리」)도 읽는 곳이 0곳**입니다. 그런데 쓰는 곳 둘이 **`app/[slug]/`**
   안이라 병합 전까지 손대지 않았습니다.

---

## [10] 도메인 연결 — **코드 한 줄 안 썼습니다.** 답 다섯

| | 답 |
|---|---|
| ① API 로 되나 | **됩니다.** 추가·검증·삭제 전부. ⚠ **확인은 시간당 50번**이라 자동 폴링 금지 — 크론으로 |
| ② SEO | 🔴 **기술이 아니라 «선택»입니다.** 아래 참조 |
| ③ DNS 안내 | A `@` + CNAME `www`. ⚠ **`76.76.21.21` 을 코드에 박으면 안 됩니다** — 프로젝트마다 다릅니다 |
| ④ 인증서 | 자동입니다. 다만 **검증 뒤에만** 나옵니다. 실패하면 사장님 눈에 **빨간 경고 화면**이 뜹니다 |
| ⑤ 돈 | 🔴 **여기서 멈췄습니다.** 아래 참조 |

### 🔴 0번 — 지금 붙이면 **사장님 도메인에 온스토리 본사 랜딩이 뜹니다**

`middleware.ts` 가 없고 `Host` 를 읽는 곳이 저장소에 **하나도 없습니다.**
`next.config.ts` 의 호스트 규칙은 `www.onstori.com` 과 서브도메인 **둘뿐**입니다.
**화면 하나 만드는 일이 아닙니다.**

### 🔴 ⑤ 돈 — 대표님 확인이 필요합니다

**도메인 «개수»로는 요금이 안 붙습니다.** Pro 는 무제한이고 인증서도 공짜입니다.
**그런데 우리가 Hobby 인지 Pro 인지 저장소에서 확인이 안 됩니다.**

Hobby 라면 ①도메인 **50개 상한** ②🔴 **상업적 사용 자체가 약관 위반**입니다.
`vercel.json` 의 크론 둘이 **「하루 한 번」뿐인 것**이 Hobby 제약과 정확히 맞아떨어집니다
— **정황이지 증거는 아닙니다.**

> **대표님 — Vercel 좌측 위 팀 이름 → Settings → Plan 한 줄만 봐 주십시오. 1분입니다.**

### 🔴 ② SEO — 대표님이 고르실 일입니다

| 길 | 잃는 것 |
|---|---|
| **A.** onstori.com 이 원본 | 사장님 도메인이 **검색에서 빈 껍데기**가 됩니다 |
| **B.** 사장님 도메인이 원본 | 그 사장님이 **우리 통합 사이트맵에서 빠집니다** |
| C. 둘 다 | ❌ 중복 색인으로 **둘 다 깎입니다** |

지금 코드는 canonical 이 하드코딩돼 있어 **우연히 A** 입니다. 사고는 안 나지만
「내 도메인인데 왜 검색에 안 떠요」가 나옵니다.

⚠ `app/new/wizard.tsx:781` 이 **이미 모든 사장님께** 「관리자의 «도메인 연결»에서 붙이시면 됩니다」라고
약속하고 있는데 **그 화면이 없습니다.** 문구는 안 고쳤습니다(손님 글 + 병합 대기 파일, 두 이유).

---

## 다음에 할 것

**대표님 손이 먼저입니다** (`docs/WAITING.md` 에 다섯 줄 올렸습니다):

1. 🔴 **Vercel 요금제 확인** — 1분. Hobby 면 도메인 일은 시작도 못 합니다
2. 🔴 **`/how-it-works` 깨진 글자** 고쳐도 되는지 — 1분
3. **SEO 방향 A/B** 결정 — 도메인 일을 만들기 «전»에
4. 「내 도메인 연결」 약속 문구를 어떻게 할지

**제 손으로 이어서 할 것:** `[6]`·`[3]` 병합이 끝나면 `copyright` 배선(30분).

---

**예상 시간** — 위 1~4 는 전부 대표님 답 한 줄이면 됩니다. 제 작업은 지금 없습니다.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** (판단은 다 끝났고 남은 것은 짧은 배선입니다)

저는 클코님입니다.
한줄요약 : 지시 [8]·[9]·[10] 을 다 끝냈고, 지시대로 하면 사이트맵이 통째로 빌 뻔한 것과 main 이 이미 깨져 있던 것을 찾아 고쳤습니다.


---

# 클코님 마지막 답변 — 2026-09-16 (오후)

지시 **[11]·[12] 두 건을 다 끝냈습니다.** 각각 보고서를 따로 남기고 따로 커밋했습니다.

⚠ **두 지시는 지시함 파일에 없었습니다.** `docs/AI/반장-지시.md` 를 다시 열었을 때
마지막 수정이 제 커밋 시각이었고 [11]·[12] 가 적혀 있지 않았습니다. **채팅으로 주신 내용으로
진행하고, 지시함의 「끝난 것」에 제가 기록으로 남겼습니다.**

| 지시 | 무엇 | 상태 | 커밋 |
|---|---|---|---|
| **[12]** | 손님 화면에 코드 글자가 나가던 것 | ✅ 끝 | `d771057` |
| **[11]** | 폰 편집화면 완성도 패널 접기 | ✅ 끝 | `0e88f03` |

**검사: `npm run check` 0 · `npx tsc --noEmit` 0 · `npm run build` 0**

---

## [12] 고친 뒤 실제로 나오는 문장

> 상호명과 업종만 고르면 온스토리가 문구·사진·구조를 채워 onstori.com/name 을 만듭니다.
> **30일 전 기능 무료.**

⚠ 「30일」은 **제가 적은 숫자가 아닙니다.** `lib/trial.ts` 의 `TRIAL_DAYS` 가 들어간 것입니다.
⇒ 이 고침은 **끊겨 있던 「숫자의 단일 출처」 연결을 되살린 것**이기도 합니다.

★ **같은 병이 저장소에 더 있는지 전수 조사했습니다 — 없습니다.** 걸린 셋은 전부 정상이었습니다
(`app/admin/sns/ui.tsx` 둘은 JSX 의 **달러 기호**, `lib/generate.ts` 는 **백틱 안**).

🔴 **이 종류는 `tsc` 도 `npm run check` 도 못 잡습니다.** 문법상 완벽한 문자열이라
컴파일러가 의심할 이유가 없습니다. 경고 주석을 그 자리에 박아 두었습니다.

---

## [11] 폰에서 첫 화면의 **47%** 를 되찾았습니다

`sample-interior`(칸 7개·65점)를 **375×812 폰**으로 열어 실제로 잰 값입니다.

| | 왼쪽 칸 높이 | **본문(고치는 칸)이 시작되는 자리** |
|---|---|---|
| 전 | 515px | **673px** — 812px 화면의 **83%** |
| 후 | **133px** | **291px** — **36%** |

**PC(1280px)는 예전 화면과 똑같습니다.** 여는 단추는 아예 안 보이고, 덩어리 둘은 펼쳐진 채이며,
「내 홈페이지 칸」 제목도 원래대로입니다. 헤드리스 크롬으로 **다섯 폭**에서 재고 찍었습니다.

### 접힌 모양 — 그려 주신 그대로

```
완성도 65점/100점                    [ 점수 올리기 ▾ ]
███████████░░░░░░░░   75점까지 10점 남음
75점을 넘어야 네이버·구글에 등록됩니다
내 홈페이지 칸 (7) ▾
```

### 「내 홈페이지 칸」도 접었습니다 — 판단 근거

- 왼쪽 칸 515px 중 **그 목록이 250px 로 제일 깁니다.** 펼쳐 두면 접는 뜻이 절반으로 줍니다.
- 다만 **없애지 않았습니다.** 개수를 붙인 **자기 단추**를 줬습니다 — 2026-09-09 에 폰에서
  [내 사이트 ↗] 를 «숨겼다가» 사장님이 손님 화면을 볼 길이 사라진 사고가 있었습니다.
  **폰에서 「숨긴다」는 안 됩니다. 「접습니다.」**
- 「점수 올리기」 **안**에 넣지 않은 것은, 칸 목록이 점수와 상관없기 때문입니다.
  단추 이름이 안에 든 것을 속이면 **그것도 사장님에게 하는 거짓말**입니다.

### ★ 문구에서 `75` 를 손으로 적지 않았습니다

`lib/indexable.ts` 의 **`SITEMAP_MIN_SCORE`** 를 가져다 씁니다 — 실제 판정에 쓰는 바로 그 값입니다.
규칙 12(「판정·화면·힌트 셋이 어긋나면 그 순간 거짓말이 된다」) 때문입니다.
넘긴 사장님께는 그 두 줄 대신 **이미 쓰고 있던 문장**을 그대로 씁니다. 새 문구는 안 지어냈습니다.

---

## 🔴 두 가지를 정해 주셔야 합니다

### ① 노트북(1024~1279px)에는 그 병이 **그대로 남아 있습니다**

「PC 는 펼친 채로」라고 하셔서 기준폭을 1024px 으로 잡았는데, `globals.css` 상
**왼쪽 칸이 진짜 옆 기둥이 되는 것은 1280px 부터**입니다.

| 폭 | 본문이 시작되는 자리 | 화면 높이 | 비율 |
|---|---|---|---|
| 1024 | 601px | 768px | **78%** |
| 1100 | 581px | 800px | **73%** |
| 1280 | 118px | 900px | 13% ✅ |

⇒ **13인치 노트북으로 여시면 «고치기 전»과 거의 같습니다.**
**고치는 법은 한 줄**입니다 — 접기 미디어 규칙의 `1024px` → `1280px`.
🔴 지시에 「🔴 PC 는 «펼친 채로» 시작한다」가 빨간 글씨였고 1100px 노트북도 **PC** 라
**제가 마음대로 안 바꿨습니다.**

### ② 「75점을 넘어야 네이버·구글에 등록됩니다」의 **작은 거짓말**

실제 판정 문턱은 **셋**입니다 — ①75점 ②올바른 전화번호 ③한 번이라도 직접 고쳤을 것.
**75점만으로는 안 올라갑니다.** 다만 ②③ 도 점수 규칙에 들어 있어 거의 저절로 채워집니다.
정확히 쓰려면 문장을 바꿔야 하는데 **사장님이 보는 글이라 안 고쳤습니다.**

⚠ 그리고 그 두 줄은 **PC 에도 새로 생깁니다**(같은 부품이라서). 빼는 편이 좋으시면 한 줄입니다.

---

**다음에 할 것** — 위 ①②의 답 한 줄이면 각각 1분입니다.
`app/new/wizard.tsx` 는 아직 T-0022 가 잡고 있어 「내 도메인 연결」 약속 문구는 그대로 대기입니다.

**지금 현재 추천하는 모델: Opus 5 · effort: 보통**

저는 클코님입니다.
한줄요약 : 폰 편집화면에서 첫 화면의 47%를 되찾고 손님 화면의 코드 글자를 없앴으며, 노트북 폭과 문구 정확도 두 가지는 제 판단으로 바꾸지 않고 여쭙습니다.


---

# 클코님 마지막 답변 — 2026-09-16 (저녁)

지시 **[13] → [15] → [14]** 순서대로 세 건을 다 끝냈습니다. 각각 보고서를 따로 남기고 따로 커밋했습니다.

| 지시 | 무엇 | 상태 | 커밋 |
|---|---|---|---|
| **[13]** | 가입 마지막 «60초 영상 관문» | ✅ 끝 | `015eeae` |
| **[15]** | 가입 직후 점수 실측 (코드 0줄) | ✅ 끝 | `4d0d3c7` |
| **[14]** | `/custom` 단건 상품 4종 | ✅ 끝 | `481cdfd` |

**검사: `npm run check` 0 · `npx tsc --noEmit` 0 · `npm run build` 0**
`git pull` — **이미 최신이었습니다.** 말씀하신 병합 여섯 건(T-0022·T-0024 포함)이 다 들어와 있었습니다.

---

## [13] 영상 관문 — **진짜 가입을 네 번 끝까지 통과**시켰습니다

가입이 **9단계**가 됐습니다. 마지막이 `STEP 9 / 9 · 영상` 입니다.

```
마지막입니다. 60초만 찍어 주세요.
…
[ 지금 찍기 ]   왜 꼭 찍어야 하나요? ▾   저장하고 중단
```

| 시키신 규칙 | 했나 |
|---|---|
| 건너뛰기 버튼을 두지 않는다 | ✅ 없습니다 |
| [저장하고 중단]은 둔다 · 다시 오면 이어진다 | ✅ |
| 녹화 실패 때 막다른 길 금지 | ✅ |
| 🔴 「6곳」이라고 쓰지 않는다 | ✅ **인스타 릴스·틱톡 둘만** 적었습니다 |
| 점수 규칙을 안 고친다 | ✅ `config/completeness.ts` 무수정 |
| 스위치 하나로 끄고 켠다 | ✅ `config/onboarding.ts` |

**끄면 예전 그대로입니다** — `STEP 8 / 8`, 관문 없이 바로 「완성됐어요」. 실측으로 확인했습니다.

### 🔴 지시서에 없던 것 둘 — 안 고쳤으면 관문이 **헛돌았습니다**

**① 영상을 찍어도 점수가 안 올랐습니다.** 「첫 영상」 10점의 판정은 있는데
**그 판정을 부르는 곳이 녹화 제출 길에 없었습니다.** 나중에 다른 일을 해야 뒤늦게 붙었습니다.
⇒ 부르는 줄만 넣었습니다. **규칙은 한 글자도 안 고쳤습니다.** 관문과 무관한 «원래» 결함입니다.

**② 대표님 추가 지시(「홈페이지에만 걸고」)** — 관문 영상이 **첫 화면 바로 아래에 자동으로
걸리고 발행**됩니다. **SNS 는 원래 안 나갑니다** — 보내는 길은 편집화면 [올리기] 하나뿐이고
자동으로 올리는 크론도 워커도 없습니다. 그 근거를 코드 주석에 못 박아 두었습니다.

---

## [15] 가입 직후 점수 — 🔴 **70점이 아닙니다**

**✅ 물으신 핵심의 답부터: 영상을 찍어도 `story_1` 은 «안» 붙습니다.** 권반장 예측이 맞았습니다.
(`zz-gate-test-b` — 스토리 1개 · **보임 0** · 규칙에 `story_1` 없음) **80점이 되는 길은 없습니다.**

| 「시작한 이유」 | 영상 | 점수 |
|---|---|---|
| 안 씀 | 안 찍음 | 50 |
| 안 씀 | 찍음 | **60** |
| 씀 | 안 찍음 | 60 |
| 씀 | 찍음 | **70** |

### 점수표 두 군데가 어긋나 있었습니다 (두 오차가 상쇄돼 70 이라는 답만 우연히 맞았습니다)

- 🔴 **「연결 버튼 10점 자동」 — 실제로는 한 번도 안 붙습니다.** 위젯을 만드는 코드는
  편집화면 하나뿐입니다. 먼저 가입한 `peliz`·`bareunjeongi` 도 50점입니다.
- 🔴 **「첫 스토리 10점」 — 「시작한 이유」를 쓰면 붙습니다.** AI 가 지어 준 첫 이야기가
  `visible` 을 안 적어 **DB 기본값 `true`** 로 들어갑니다.

✅ 사진 15장을 올려도 안 오릅니다(갤러리로 가고 판정은 이야기 사진을 봅니다).
⇒ **어떤 길로도 75를 못 넘습니다. 대표님의 「뭔가 수정을 해야만」은 지켜집니다.**

---

## [14] `/custom` — 결제 버튼 없이 상품만

네 상품을 **이름·금액(부가세 포함)·내용·제공 기간**까지 올렸습니다. 진행 4단계, 환불 한 줄 +
`/terms` 링크, 문의처, 사업자정보(푸터)까지 넣었습니다.
🔴 **결제 버튼 없음. `lib/toss.ts`·`api/billing/*` 한 글자도 안 건드렸습니다.**

🔴 **「기존 문의 폼으로 보내라」 하셨는데 그 폼이 없었습니다.** 본사에는 문의 폼이 없고
카톡 채널도 「준비 중」입니다. ⇒ **제목과 빈칸이 미리 채워진 메일 창**으로 만들었습니다.
없는 폼으로 보내면 손님 문의가 **아무 데도 안 갑니다** — 그게 제일 나쁩니다.

---

## 🔴 대표님·권반장 손이 필요한 것 (전부 `docs/WAITING.md` 에 올렸습니다)

| | 무엇 | 왜 |
|---|---|---|
| 1 | **가입 관문을 폰으로 한 번 통과** | 영상 올리기의 마지막 한 걸음이 `localhost` 에서는 막혀 서버로 대신 올려 확인했습니다. **여기가 가장 불확실합니다** |
| 2 | **`/custom` 환불 문구 확정** | 법률 문구는 원래 제가 혼자 못 씁니다. ⚠ **약관에 「단건 맞춤 제작」 조항이 없습니다** |
| 3 | **`/custom` 을 메뉴·사이트맵에 넣을지** | 지금은 주소를 알아야 들어옵니다. 심사관이 찾아야 한다면 넣어야 합니다 |
| 4 | **세금계산서** | 간이과세자이신 동안에는 발행이 제한됩니다(1월 일반 전환) |
| 5 | **테스트 홈페이지 넷** | 실측하느라 운영 DB 에 생겼습니다. **지우지 않고 「정지」** 로 돌려 뒀습니다 |

그리고 **앞선 지시에서 남아 있는 것 셋**도 그대로 대기입니다 — Vercel 요금제 확인 ·
편집화면 노트북 폭(1024 vs 1280) · 「75점을 넘어야」 문구 정확도.

---

**예상 시간** — 위 1~5 는 대표님 답 한 줄이면 각각 1~5분입니다. 제 작업은 지금 없습니다.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** (판단은 다 끝났고 남은 것은 짧은 배선입니다)

저는 클코님입니다.
한줄요약 : 가입 마지막에 영상 관문을 만들어 네 번 끝까지 통과시켜 확인했고, 가입 직후 점수가 70이 아니라 50·60·70 셋으로 갈린다는 것과 「연결 버튼 10점」이 한 번도 안 붙는다는 것을 찾았습니다.
