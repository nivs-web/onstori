
---

# 세션 정리 — 2026-09-12

## 1. 이번 세션에 실제로 벌어진 일 (한 장 요약)

| 언제 | 무슨 일 | 결과 |
|---|---|---|
| 어제 밤 | 랄프 루프 12개 항목을 끝내고 커밋·push (`e4b04f5`) | **push 는 됐다** |
| 그런데 | **배포가 안 됐다.** 운영은 그 전 버전(`6c39b41`)에 멈춰 있었다 | 12개 전부 화면에 안 나와 있었다 |
| 오늘 | 원인을 문서로 확정하고 고쳐서 다시 올림 (`e56a1b1`) | **배포 완료 · 실물 확인함** |

## 2. ★ 왜 배포가 막혔었나 — 크론 한 줄

제가 「주 1회 알림」을 **매시 정각(`0 * * * *`)** 으로 적었습니다.
그런데 **Vercel 무료(Hobby) 요금제는 크론이 하루 1회뿐**이고, 하루에 두 번 이상 도는 표현식을 적으면
그 줄 하나 때문에 **배포 전체가 실패**합니다.

> Hobby accounts are limited to daily Cron Jobs.

즉 **급하다고 하신 심사관 로그인까지 포함해 12개 전부가 그 한 줄에 막혀 있었습니다.**
제가 어제 「push 했습니다」라고만 말씀드리고 **배포까지 확인하지 않은 것이 잘못**이었습니다.

### 어떻게 고쳤나 — 요금제를 몰라도 되게

| 전 | 후 |
|---|---|
| 「지금이 **딱 그 시각**인가?」로 판단 → 매시 크론이 **반드시** 필요 | 「그 시각이 **지났는가**?」로 판단 → 하루 1회 크론으로도 간다 |
| Hobby 에서는 배포 자체가 실패 | **어느 요금제에서도 배포된다** |

- 지금(하루 1회, 아침 7시): 고르신 요일의 **아침에** 나갑니다. 「화요일 오후 3시」로 고르셔도 **수요일 아침**에 나갑니다.
- Pro 로 올리시면: `vercel.json` 한 줄만 `0 * * * *` 로 바꾸면 **고르신 시각 그대로** 나갑니다. **코드는 안 고쳐도 됩니다.**
- 화면 문구도 사실대로 고쳤습니다 — 「시각은 **그 무렵**이고, 조금 늦어질 수 있어요.」 정확한 척하지 않습니다.

검사 파일을 새로 만들어(`scripts/weekly-test.ts`) **15건 전부 통과**했습니다 — 두 번 보내지 않는지, 한국 시각 환산이 맞는지까지.

## 3. 운영에서 실물로 확인한 것 (추측 아님)

| 확인한 것 | 결과 | 뜻 |
|---|---|---|
| `/api/cron/weekly` | **401 + 우리 JSON** | 주 1회 크론이 **살아 있다** (401 은 열쇠 없이 불러서 정상) |
| `/api/site/weekly` | **405** | 설정 저장 창구가 **있다** |
| `/api/auth/review` | **405** | 심사관 로그인 창구가 **있다** |
| 홈 「찍는 즉시 …」 | **보임** | 4단계 3·4번 새 문구(A안) 적용됨 |
| 옛 문구 「30분 안에 자막 영상」 | **0건** | 못 지킬 약속이 홈에서 사라졌다 |
| 「심사」 (금지어) | **0건** | 지워졌다 |
| `/login/review` | **404** | ⚠ **이건 정상입니다** — 아래 4번 참조 |

## 4. ★ 회장님 손이 필요한 것 (이게 없으면 안 켜집니다)

Vercel 환경변수에 넣어 주셔야 하는 값입니다. **값은 저에게 보내지 마시고 Vercel 화면에만** 넣으십시오.

| 이름 | 무엇에 쓰나 | 없으면 |
|---|---|---|
| `REVIEW_ID` · `REVIEW_PASSWORD` · `REVIEW_EMAIL` | **심사관 로그인** (메타·인스타 심사관이 볼 계정) | `/login/review` 가 **404** — 지금 이 상태입니다 |
| `INSTAGRAM_APP_ID` · `INSTAGRAM_APP_SECRET` | 인스타 연결 | 연결 버튼이 「준비 중」 |
| `X_BEARER_TOKEN` | X 잔액 대시보드 | 잔액칸이 「열쇠 없음」 |

> ★ `/login/review` 가 404 인 것은 **일부러 그렇게 만든 것**입니다. 열쇠가 없을 때 「비밀번호가 틀렸습니다」라고 하면
> 그 주소가 **있다는 사실**을 알려 주는 꼴이 됩니다. 그래서 아예 **없는 길처럼** 보이게 했습니다.
> 위 세 값을 넣고 재배포하면 그 순간부터 열립니다.

### 그 밖에 회장님이 정하실 것

| 무엇 | 왜 물어보나 |
|---|---|
| X 개발자 콘솔의 **자동 충전** 켜져 있는지 | 켜져 있으면 잔액이 **조용히 다시 채워집니다**. 「안 썼는데 $10 이 없어졌다」가 반복됩니다 |
| 통신판매업 번호 표기 — 「다산」인지 「경기남양주」인지 | 법정 표시는 **신고증 원본과 글자가 같아야** 합니다 |
| **구매안전서비스(KB에스크로) 표시** | 선불식 통신판매는 표시 의무가 있는데 화면 어디에도 없습니다 |
| Vercel **Pro** 로 올릴지 | 올리면 주 1회 알림이 **고르신 시각 정확히** 나갑니다 |

## 5. 제가 오늘 새로 발견한 것 (시키지 않으신 것)

**`/how-it-works` 4단계에 「30분」 약속이 그대로 남아 있습니다.**

홈에서는 지우신 그 약속이(자막 영상 30분) **작동방식 페이지에는 살아 있습니다.**
`「온스토리가 영상·글·사진 카드를 만듭니다 / 30분 / 무음 컷 · 한글 자막 …」`

★ **문구는 회장님 허락 없이 안 고칩니다.** 안만 올립니다:

| 안 | 바꿀 문구 |
|---|---|
| A (홈과 같은 결) | 「**찍는 즉시 홈페이지에 올라갑니다**」 · 시간 표시는 「바로」 |
| B (최소 수정) | 「30분」만 빼고 나머지는 그대로 |

## 6. 아직 못 한 것 — 「막힘」으로 남깁니다

| 항목 | 왜 막혔나 |
|---|---|
| 토큰 암호화 (상무님 결정) | 설계는 했으나 **암호 열쇠(`TOKEN_KEY`)를 회장님이 만들어 주셔야** 합니다 |
| 하루 한도 한국 자정 기준 | 지금은 UTC 기준. 고치면 **이미 오늘 올린 건수가 리셋**될 수 있어 회장님 확인이 필요합니다 |
| 상무님 규격서 · 김팀장 조사 | `fable51plandept/AI_Context/` 폴더가 **아직 없습니다.** 없는 것을 짐작해 채우지 않았습니다 |
| 주소(slug) 바꾸기 화면 | 이미 발행된 주소가 있으면 **손님 링크가 깨집니다.** 전달(리디렉션) 설계가 먼저입니다 |

## 7. 손대지 않은 것

`delete-all.ps1` · `delete.jq` · `keep6.jq` · `will_delete.txt` · `deployments2.json`
— 회장님 작업 파일이라 **읽지도 커밋하지도 않았습니다.** 그대로 있습니다.

---

**다음에 할 일:** 위 4번의 열쇠 세 줄을 Vercel 에 넣어 주시면, 심사관 로그인부터 확인해서 보고드리겠습니다.
**예상시간:** 열쇠가 들어온 뒤 확인까지 10분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** — 지금은 만드는 일이 아니라 **확인하고 정리하는 일**이라 높은 추론이 필요 없습니다.

저는 클코님입니다.
한줄요약 : 크론 한 줄이 배포 전체를 막고 있던 것을 찾아 고치고, 12개 항목이 운영에 올라간 것을 실물로 확인했습니다.

---

## ★ 추가 (회장님 지시 — 세션 정리)

`docs/PROGRESS.md` 맨 위 인수인계를 지시하신 항목으로 다시 썼습니다:
오늘 끝낸 12개 + 커밋 둘(`e4b04f5`·`e56a1b1`) · **열쇠 여섯** · 손대지 않은 회장님 파일 ·
다음 세션 할 일 9개 · **⚠ 박팀장 시험 사이트(`parkteamjang-test-cafe`) 정리를 0번으로**.

⚠ 그 사이트는 **제가 지우지 않았습니다.** 표의 줄을 지우는 것은 되돌리기 어려워서
규칙대로 먼저 여쭙습니다. 먼저 **보이지 않게**(공개·검색 제외) 내리고, 붙어 있는
손님 문의·영상 개수를 세어 보고드린 뒤 「지워라」 하시면 지우겠습니다.

---

# 랄프 루프 11개 — 끝난 것 8개 · 부분 1개 · 막힘 2개

_2026-09-12 · 회장님 외출(3~4시간) 중 작업_

## 0. 한 장 요약

| # | 할 일 | 결과 |
|---|---|---|
| 1 | ★ 인스타 토큰 갱신 장치 | ✅ **완료** — 게다가 «더 큰 구멍»을 찾았습니다(아래 2번) |
| 2 | ★ 가입 동의 절차 | ✅ 완료 — ⚠ **`db push` 필요** |
| 3 | 결제 경로 revalidatePath | ✅ 완료 (두 곳 다) |
| 4 | `app/not-found.tsx` | ✅ 완료 — 영어 화면이 한국어로 |
| 5 | 무료 끝난 뒤 손님 사이트 안내 | ✅ 완료 (안 B) |
| 6 | 글자 수·해시태그 세기 | ✅ 완료 |
| 7 | menu-video 앵커 | ✅ **이미 고쳐져 있었습니다** — 남은 약점 하나만 손봄 |
| 8 | SNS 마무리 크론 | ✅ 완료 |
| 9 | 상무님 목록 5건 | ⚠ **2건 완료 · 3건 막힘** |
| 10 | 무료기간 검색 조건 | ⚠ **전제가 코드와 달랐습니다** — 뜻을 살려 다르게 고침 |
| 11 | build·verify·lint·커밋 | ✅ 완료 — **린트 오류 4건도 0건으로** |

## 1. ★★ 1번에서 «더 큰 구멍»을 찾았습니다

김팀장님이 찾아 주신 것은 **「60일이면 만료된다」** 였습니다. 그런데 코드를 열어 보니
**우리는 60일짜리 토큰을 받은 적이 아예 없었습니다.**

| | 무슨 일이 벌어지고 있었나 |
|---|---|
| 인스타 로그인 직후 받는 첫 열쇠 | **1시간짜리** |
| 60일짜리로 바꾸는 절차 | **코드에 없었습니다** |
| 결과 | 사장님이 연결하고 **한 시간 뒤부터** 올리기가 실패 |

> 즉 「두 달 뒤에 끊긴다」가 아니라 **「한 시간 뒤에 끊긴다」** 였습니다.
> 오늘 회장님이 인스타 연결에 성공하셨으니, **그 연결도 한 시간이면 죽었을 것**입니다.

**고친 것 셋**

1. 연결하는 순간 **1시간짜리 → 60일짜리로 바꿉니다.**
2. **매일 한 번** 크론이 「만료 2주 안에 죽는 열쇠」를 미리 갱신합니다. 다시 60일이 됩니다.
3. 갱신이 **확실히 끝난 것**일 때만 「다시 연결해 주세요」로 내립니다.
   잠깐 안 되는 것(인스타가 잠시 죽은 것 등)은 **그냥 둡니다** — 멀쩡한 연결을 우리가 끊지 않습니다.

**화면 거짓말도 하나 고쳤습니다.** 연결이 풀리면 화면이 「[연결하기]를 눌러 주세요」라고 하는데
**그 버튼이 화면에 없었습니다.** [연결 끊기]만 있었습니다. 이제 [다시 연결하기]가 나옵니다. (규칙 12)

**새 크론을 만들지 않았습니다.** `vercel.json` 의 크론 한 줄이 어제 배포 전체를 막았고,
무료 요금제는 크론 개수에도 제한이 있습니다. 그래서 **이미 매일 도는 크론에 얹었습니다.**

**검사 16건을 만들었습니다** — `npx tsx scripts/sns-refresh-test.ts`. 60일을 기다려 볼 수는 없으니까요.

## 2. 2번 — 가입 동의 ⚠ 회장님 손이 필요합니다

손님은 체크박스로 보호받는데 **사장님 가입에는 하나도 없었습니다.** 넣었습니다.

- 가입 **마지막 단계**(분위기 고르기 아래)에 체크박스 셋
  - **[필수]** 이용약관 · **[필수]** 개인정보 수집·이용 — 각각 **새 창 링크**로 열립니다
  - **[선택]** 촬영 알림 문자 — 안 눌러도 가입됩니다
- 필수 둘을 안 누르면 [홈페이지 만들기] 버튼이 **안 눌립니다**
- **동의 시각을 새 표(`consents`)에 남깁니다.** 상무님 경고대로 `settings` 에 넣지 않았습니다
  — 거기 넣으면 사장님이 홈페이지를 저장할 때마다 **증거가 조용히 덮입니다**

> ⚠ **`npx supabase db push` 를 해 주셔야 합니다.** (새 표 `consents`)
> 하기 전까지는 동의 화면은 뜨지만 **기록이 안 남습니다**(가입 자체는 정상).

## 3. 10번 — 전제가 코드와 달랐습니다

상무님 보고는 **「지금은 완성도 60점이면 색인된다」** 였습니다. 저장소를 뒤졌는데
**그런 규칙이 어디에도 없습니다.** 지금 실제 규칙은 이렇습니다:

| 지금 실제 | |
|---|---|
| 무료(trial) 사이트 | **검색에서 아예 뺍니다**(noindex) |
| 결제한(active) 사이트 | 사이트맵에 넣습니다 |

즉 **이미 60점 규칙보다 엄합니다.** 고칠 「60점」이 없었습니다.

그래서 **권고의 «뜻»을 살렸습니다** — 「빈 사이트가 색인되면 다 같이 손해다」.
**결제한 사이트라도 속이 비어 있으면 우리가 먼저 검색엔진에 알리지 않습니다.**
문턱: **75점 + 전화번호가 유효 + 한 번 이상 편집.**

★ 다만 **noindex 를 걸지는 않았습니다.** 둘은 다릅니다:
- 사이트맵에서 빼기 = 「우리가 먼저 알리지 않는다」 (채우면 다음 갱신에 저절로 들어갑니다)
- noindex = 「검색에서 지워라」

**돈을 낸 사장님의 홈페이지를 우리가 검색에서 지우는 일은 하지 않습니다.**

★★ **함정 하나를 잡았습니다.** 점수표(`site_progress`)는 «주인만» 읽을 수 있는 표입니다.
사이트맵은 로그인이 없어서, 그대로 뒀으면 점수가 전부 「없음」이 되어
**모든 사이트가 사이트맵에서 조용히 사라질 뻔했습니다.** 운영자 열쇠로 읽게 고쳤습니다.

★ **실측**: 지금 결제·발행된 손님 사이트는 **샘플 하나(`sample-interior`, 55점)** 뿐이라
새 문턱에 걸려 빠집니다. 그건 우리 시연용 가짜 가게라 **검색에 안 나오는 편이 맞습니다.**
진짜 사장님이 생기면 75점을 넘기는 순간 들어갑니다.

## 4. 9번 — 5건 중 2건 완료, 3건 막힘

| 항목 | 결과 |
|---|---|
| **`suspended_at` 미기록** | ✅ **진짜였습니다.** 카드 결제가 세 번 실패해 정지될 때만 정지 시각을 안 찍고 있었습니다. 그러면 삭제 예고 문자가 **한 통도 안 나갑니다.** 고쳤습니다 |
| 색인/사이트맵 조건 분리 | ✅ 위 3번에서 함께 처리 |
| `extra_options` 칸 | ❌ **막힘** — 저장소에 그런 이름이 없습니다. 무엇을 담을 칸인지 몰라 못 만듭니다 |
| 「다시 고치기」 횟수 칸 | ❌ **막힘** — 같습니다 |
| `tours_seen` API | ❌ **안 만들었습니다** — DB 칸은 있는데 **투어 화면 자체가 아직 없습니다**(`config/tours.ts` 를 그리는 코드가 0곳). 저장할 것이 없는데 창구만 만들면 죽은 코드입니다 |

> 막힌 셋은 상무님께 **「그 칸에 무엇이 들어가고 화면 어디에 붙는가」** 를 한 줄씩 받으면 바로 만듭니다.

## 5. 7번은 이미 고쳐져 있었습니다

「영상이 없는 사장님은 갈 곳이 없다」는 어젯밤 커밋(`e4b04f5`)에서 이미 고쳤습니다 —
`panel-video` 앵커를 등록하고 화면에 붙였습니다. 상무님 보고가 그 전에 쓰인 것으로 보입니다.

다만 **남은 약점 하나**를 손봤습니다. 힌트를 누르면 메뉴를 바꾼 뒤 **60밀리초만 기다리고**
앵커를 찾았습니다. 영상 메뉴처럼 목록을 불러오며 그려지는 화면은 그 안에 안 끝납니다.
이제 **그려질 때까지 다시 봅니다.**

## 6. 남은 일 · 회장님께 여쭐 것

| 무엇 | 왜 |
|---|---|
| ⚠ **`npx supabase db push`** | 가입 동의 표(`consents`). 하기 전엔 동의 기록이 안 남습니다 |
| ⚠ **박팀장 시험 사이트** | 아래 7번 — **auto mode 에 막혀 있습니다** |
| 상무님께 3건 규격 | `extra_options` · 「다시 고치기」 횟수 · (tours_seen 은 투어 화면부터) |
| `/how-it-works` 「30분」 | 문구는 회장님 허락 사항. A·B 안 그대로 대기 |

## 7. ⚠ 박팀장 시험 사이트 — 세어는 봤고, 내리는 것이 막혔습니다

**붙어 있는 자료를 다 세어 봤습니다. 완전히 비어 있습니다.**

| 무엇 | 개수 |
|---|---|
| 손님 문의 | **0건** |
| 이야기 | **0건** |
| 올린 사진·영상 | **0개** |
| 발행 기록·SNS 연결 | **0건** |
| 문서에 박힌 사진 5장 | 전부 **공용 이미지뱅크** 것 — 지워도 남의 홈페이지가 안 깨집니다 |

즉 **지워도 잃을 것이 없습니다.**

규칙 10대로 **먼저 「보이지 않게」** 내리려고 백업까지 마쳤는데(`backups/` 에 저장·검증 완료),
**auto mode 가 프로덕션 DB 쓰기를 막았습니다.** 스크립트는 준비돼 있습니다 —
회장님이 auto mode 를 잠깐 꺼 주시면 1분이면 끝납니다.

★ 내릴 때 `suspended` 로 내립니다(`expired` 아님). 그래야 **삭제 예고 문자도, 60일 자동 삭제도
걸리지 않습니다.** 되돌리기는 값 하나만 되돌리면 됩니다.

---

**지금 전체 진행상황:** 랄프 루프 11개 중 **8개 완료 · 1개 부분 · 2개 막힘.**
빌드·타입·린트·검사 전부 통과했고 **린트 오류 4건도 0건**이 됐습니다.
**다음 실행할 항목:** `db push` 를 해 주시면 가입 동의 기록이 살아납니다. 그다음은 상무님 규격 3건.
**예상시간:** `db push` 는 회장님 1분 · 그 뒤 제 확인 10분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** — 다음 일은 만드는 일이 아니라 **확인하고 붙이는 일**이라 높은 추론이 필요 없습니다.

저는 클코님입니다.
한줄요약 : 랄프 루프 11개 중 8개를 끝냈고, 인스타 연결이 「두 달 뒤」가 아니라 「한 시간 뒤」 죽고 있던 것을 찾아 고쳤습니다.
