
---

# 세션 정리 — 2026-09-12

## 1. 이번 세션에 실제로 벌어진 일 (한 장 요약)

| 언제 | 무슨 일 | 결과 |
|---|---|---|
| 어제 밤 | 랄프 루프 12개 항목을 끝내고 커밋·push (`e4b04f5`) | **push 는 됐다** |
| 그런데 | **배포가 안 됐다.** 운영은 그 전 버전(`6c39b41`)에 멈춰 있었다 | 12개 전부 화면에 안 나와 있었다 |
| 오늘 | 원인을 문서로 확정하고 고쳐서 다시 올림 (`e56a1b1`) | **배포 완료 · 실물 확인함** |

## 2. ★ 왜 배포가 막혔었나 — 크론 한 줄

제가 「주 1회 알림」을 **매시 정각(`0 * * * *`)** 으로 적었습니다.
그런데 **Vercel 무료(Hobby) 요금제는 크론이 하루 1회뿐**이고, 하루에 두 번 이상 도는 표현식을 적으면
그 줄 하나 때문에 **배포 전체가 실패**합니다.

> Hobby accounts are limited to daily Cron Jobs.

즉 **급하다고 하신 심사관 로그인까지 포함해 12개 전부가 그 한 줄에 막혀 있었습니다.**
제가 어제 「push 했습니다」라고만 말씀드리고 **배포까지 확인하지 않은 것이 잘못**이었습니다.

### 어떻게 고쳤나 — 요금제를 몰라도 되게

| 전 | 후 |
|---|---|
| 「지금이 **딱 그 시각**인가?」로 판단 → 매시 크론이 **반드시** 필요 | 「그 시각이 **지났는가**?」로 판단 → 하루 1회 크론으로도 간다 |
| Hobby 에서는 배포 자체가 실패 | **어느 요금제에서도 배포된다** |

- 지금(하루 1회, 아침 7시): 고르신 요일의 **아침에** 나갑니다. 「화요일 오후 3시」로 고르셔도 **수요일 아침**에 나갑니다.
- Pro 로 올리시면: `vercel.json` 한 줄만 `0 * * * *` 로 바꾸면 **고르신 시각 그대로** 나갑니다. **코드는 안 고쳐도 됩니다.**
- 화면 문구도 사실대로 고쳤습니다 — 「시각은 **그 무렵**이고, 조금 늦어질 수 있어요.」 정확한 척하지 않습니다.

검사 파일을 새로 만들어(`scripts/weekly-test.ts`) **15건 전부 통과**했습니다 — 두 번 보내지 않는지, 한국 시각 환산이 맞는지까지.

## 3. 운영에서 실물로 확인한 것 (추측 아님)

| 확인한 것 | 결과 | 뜻 |
|---|---|---|
| `/api/cron/weekly` | **401 + 우리 JSON** | 주 1회 크론이 **살아 있다** (401 은 열쇠 없이 불러서 정상) |
| `/api/site/weekly` | **405** | 설정 저장 창구가 **있다** |
| `/api/auth/review` | **405** | 심사관 로그인 창구가 **있다** |
| 홈 「찍는 즉시 …」 | **보임** | 4단계 3·4번 새 문구(A안) 적용됨 |
| 옛 문구 「30분 안에 자막 영상」 | **0건** | 못 지킬 약속이 홈에서 사라졌다 |
| 「심사」 (금지어) | **0건** | 지워졌다 |
| `/login/review` | **404** | ⚠ **이건 정상입니다** — 아래 4번 참조 |

## 4. ★ 회장님 손이 필요한 것 (이게 없으면 안 켜집니다)

Vercel 환경변수에 넣어 주셔야 하는 값입니다. **값은 저에게 보내지 마시고 Vercel 화면에만** 넣으십시오.

| 이름 | 무엇에 쓰나 | 없으면 |
|---|---|---|
| `REVIEW_ID` · `REVIEW_PASSWORD` · `REVIEW_EMAIL` | **심사관 로그인** (메타·인스타 심사관이 볼 계정) | `/login/review` 가 **404** — 지금 이 상태입니다 |
| `INSTAGRAM_APP_ID` · `INSTAGRAM_APP_SECRET` | 인스타 연결 | 연결 버튼이 「준비 중」 |
| `X_BEARER_TOKEN` | X 잔액 대시보드 | 잔액칸이 「열쇠 없음」 |

> ★ `/login/review` 가 404 인 것은 **일부러 그렇게 만든 것**입니다. 열쇠가 없을 때 「비밀번호가 틀렸습니다」라고 하면
> 그 주소가 **있다는 사실**을 알려 주는 꼴이 됩니다. 그래서 아예 **없는 길처럼** 보이게 했습니다.
> 위 세 값을 넣고 재배포하면 그 순간부터 열립니다.

### 그 밖에 회장님이 정하실 것

| 무엇 | 왜 물어보나 |
|---|---|
| X 개발자 콘솔의 **자동 충전** 켜져 있는지 | 켜져 있으면 잔액이 **조용히 다시 채워집니다**. 「안 썼는데 $10 이 없어졌다」가 반복됩니다 |
| 통신판매업 번호 표기 — 「다산」인지 「경기남양주」인지 | 법정 표시는 **신고증 원본과 글자가 같아야** 합니다 |
| **구매안전서비스(KB에스크로) 표시** | 선불식 통신판매는 표시 의무가 있는데 화면 어디에도 없습니다 |
| Vercel **Pro** 로 올릴지 | 올리면 주 1회 알림이 **고르신 시각 정확히** 나갑니다 |

## 5. 제가 오늘 새로 발견한 것 (시키지 않으신 것)

**`/how-it-works` 4단계에 「30분」 약속이 그대로 남아 있습니다.**

홈에서는 지우신 그 약속이(자막 영상 30분) **작동방식 페이지에는 살아 있습니다.**
`「온스토리가 영상·글·사진 카드를 만듭니다 / 30분 / 무음 컷 · 한글 자막 …」`

★ **문구는 회장님 허락 없이 안 고칩니다.** 안만 올립니다:

| 안 | 바꿀 문구 |
|---|---|
| A (홈과 같은 결) | 「**찍는 즉시 홈페이지에 올라갑니다**」 · 시간 표시는 「바로」 |
| B (최소 수정) | 「30분」만 빼고 나머지는 그대로 |

## 6. 아직 못 한 것 — 「막힘」으로 남깁니다

| 항목 | 왜 막혔나 |
|---|---|
| 토큰 암호화 (상무님 결정) | 설계는 했으나 **암호 열쇠(`TOKEN_KEY`)를 회장님이 만들어 주셔야** 합니다 |
| 하루 한도 한국 자정 기준 | 지금은 UTC 기준. 고치면 **이미 오늘 올린 건수가 리셋**될 수 있어 회장님 확인이 필요합니다 |
| 상무님 규격서 · 김팀장 조사 | `fable51plandept/AI_Context/` 폴더가 **아직 없습니다.** 없는 것을 짐작해 채우지 않았습니다 |
| 주소(slug) 바꾸기 화면 | 이미 발행된 주소가 있으면 **손님 링크가 깨집니다.** 전달(리디렉션) 설계가 먼저입니다 |

## 7. 손대지 않은 것

`delete-all.ps1` · `delete.jq` · `keep6.jq` · `will_delete.txt` · `deployments2.json`
— 회장님 작업 파일이라 **읽지도 커밋하지도 않았습니다.** 그대로 있습니다.

---

**다음에 할 일:** 위 4번의 열쇠 세 줄을 Vercel 에 넣어 주시면, 심사관 로그인부터 확인해서 보고드리겠습니다.
**예상시간:** 열쇠가 들어온 뒤 확인까지 10분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** — 지금은 만드는 일이 아니라 **확인하고 정리하는 일**이라 높은 추론이 필요 없습니다.

저는 클코님입니다.
한줄요약 : 크론 한 줄이 배포 전체를 막고 있던 것을 찾아 고치고, 12개 항목이 운영에 올라간 것을 실물로 확인했습니다.

---

## ★ 추가 (회장님 지시 — 세션 정리)

`docs/PROGRESS.md` 맨 위 인수인계를 지시하신 항목으로 다시 썼습니다:
오늘 끝낸 12개 + 커밋 둘(`e4b04f5`·`e56a1b1`) · **열쇠 여섯** · 손대지 않은 회장님 파일 ·
다음 세션 할 일 9개 · **⚠ 박팀장 시험 사이트(`parkteamjang-test-cafe`) 정리를 0번으로**.

⚠ 그 사이트는 **제가 지우지 않았습니다.** 표의 줄을 지우는 것은 되돌리기 어려워서
규칙대로 먼저 여쭙습니다. 먼저 **보이지 않게**(공개·검색 제외) 내리고, 붙어 있는
손님 문의·영상 개수를 세어 보고드린 뒤 「지워라」 하시면 지우겠습니다.

---

# 랄프 루프 11개 — 끝난 것 8개 · 부분 1개 · 막힘 2개

_2026-09-12 · 회장님 외출(3~4시간) 중 작업_

## 0. 한 장 요약

| # | 할 일 | 결과 |
|---|---|---|
| 1 | ★ 인스타 토큰 갱신 장치 | ✅ **완료** — 게다가 «더 큰 구멍»을 찾았습니다(아래 2번) |
| 2 | ★ 가입 동의 절차 | ✅ 완료 — ⚠ **`db push` 필요** |
| 3 | 결제 경로 revalidatePath | ✅ 완료 (두 곳 다) |
| 4 | `app/not-found.tsx` | ✅ 완료 — 영어 화면이 한국어로 |
| 5 | 무료 끝난 뒤 손님 사이트 안내 | ✅ 완료 (안 B) |
| 6 | 글자 수·해시태그 세기 | ✅ 완료 |
| 7 | menu-video 앵커 | ✅ **이미 고쳐져 있었습니다** — 남은 약점 하나만 손봄 |
| 8 | SNS 마무리 크론 | ✅ 완료 |
| 9 | 상무님 목록 5건 | ⚠ **2건 완료 · 3건 막힘** |
| 10 | 무료기간 검색 조건 | ⚠ **전제가 코드와 달랐습니다** — 뜻을 살려 다르게 고침 |
| 11 | build·verify·lint·커밋 | ✅ 완료 — **린트 오류 4건도 0건으로** |

## 1. ★★ 1번에서 «더 큰 구멍»을 찾았습니다

김팀장님이 찾아 주신 것은 **「60일이면 만료된다」** 였습니다. 그런데 코드를 열어 보니
**우리는 60일짜리 토큰을 받은 적이 아예 없었습니다.**

| | 무슨 일이 벌어지고 있었나 |
|---|---|
| 인스타 로그인 직후 받는 첫 열쇠 | **1시간짜리** |
| 60일짜리로 바꾸는 절차 | **코드에 없었습니다** |
| 결과 | 사장님이 연결하고 **한 시간 뒤부터** 올리기가 실패 |

> 즉 「두 달 뒤에 끊긴다」가 아니라 **「한 시간 뒤에 끊긴다」** 였습니다.
> 오늘 회장님이 인스타 연결에 성공하셨으니, **그 연결도 한 시간이면 죽었을 것**입니다.

**고친 것 셋**

1. 연결하는 순간 **1시간짜리 → 60일짜리로 바꿉니다.**
2. **매일 한 번** 크론이 「만료 2주 안에 죽는 열쇠」를 미리 갱신합니다. 다시 60일이 됩니다.
3. 갱신이 **확실히 끝난 것**일 때만 「다시 연결해 주세요」로 내립니다.
   잠깐 안 되는 것(인스타가 잠시 죽은 것 등)은 **그냥 둡니다** — 멀쩡한 연결을 우리가 끊지 않습니다.

**화면 거짓말도 하나 고쳤습니다.** 연결이 풀리면 화면이 「[연결하기]를 눌러 주세요」라고 하는데
**그 버튼이 화면에 없었습니다.** [연결 끊기]만 있었습니다. 이제 [다시 연결하기]가 나옵니다. (규칙 12)

**새 크론을 만들지 않았습니다.** `vercel.json` 의 크론 한 줄이 어제 배포 전체를 막았고,
무료 요금제는 크론 개수에도 제한이 있습니다. 그래서 **이미 매일 도는 크론에 얹었습니다.**

**검사 16건을 만들었습니다** — `npx tsx scripts/sns-refresh-test.ts`. 60일을 기다려 볼 수는 없으니까요.

## 2. 2번 — 가입 동의 ⚠ 회장님 손이 필요합니다

손님은 체크박스로 보호받는데 **사장님 가입에는 하나도 없었습니다.** 넣었습니다.

- 가입 **마지막 단계**(분위기 고르기 아래)에 체크박스 셋
  - **[필수]** 이용약관 · **[필수]** 개인정보 수집·이용 — 각각 **새 창 링크**로 열립니다
  - **[선택]** 촬영 알림 문자 — 안 눌러도 가입됩니다
- 필수 둘을 안 누르면 [홈페이지 만들기] 버튼이 **안 눌립니다**
- **동의 시각을 새 표(`consents`)에 남깁니다.** 상무님 경고대로 `settings` 에 넣지 않았습니다
  — 거기 넣으면 사장님이 홈페이지를 저장할 때마다 **증거가 조용히 덮입니다**

> ⚠ **`npx supabase db push` 를 해 주셔야 합니다.** (새 표 `consents`)
> 하기 전까지는 동의 화면은 뜨지만 **기록이 안 남습니다**(가입 자체는 정상).

## 3. 10번 — 전제가 코드와 달랐습니다

상무님 보고는 **「지금은 완성도 60점이면 색인된다」** 였습니다. 저장소를 뒤졌는데
**그런 규칙이 어디에도 없습니다.** 지금 실제 규칙은 이렇습니다:

| 지금 실제 | |
|---|---|
| 무료(trial) 사이트 | **검색에서 아예 뺍니다**(noindex) |
| 결제한(active) 사이트 | 사이트맵에 넣습니다 |

즉 **이미 60점 규칙보다 엄합니다.** 고칠 「60점」이 없었습니다.

그래서 **권고의 «뜻»을 살렸습니다** — 「빈 사이트가 색인되면 다 같이 손해다」.
**결제한 사이트라도 속이 비어 있으면 우리가 먼저 검색엔진에 알리지 않습니다.**
문턱: **75점 + 전화번호가 유효 + 한 번 이상 편집.**

★ 다만 **noindex 를 걸지는 않았습니다.** 둘은 다릅니다:
- 사이트맵에서 빼기 = 「우리가 먼저 알리지 않는다」 (채우면 다음 갱신에 저절로 들어갑니다)
- noindex = 「검색에서 지워라」

**돈을 낸 사장님의 홈페이지를 우리가 검색에서 지우는 일은 하지 않습니다.**

★★ **함정 하나를 잡았습니다.** 점수표(`site_progress`)는 «주인만» 읽을 수 있는 표입니다.
사이트맵은 로그인이 없어서, 그대로 뒀으면 점수가 전부 「없음」이 되어
**모든 사이트가 사이트맵에서 조용히 사라질 뻔했습니다.** 운영자 열쇠로 읽게 고쳤습니다.

★ **실측**: 지금 결제·발행된 손님 사이트는 **샘플 하나(`sample-interior`, 55점)** 뿐이라
새 문턱에 걸려 빠집니다. 그건 우리 시연용 가짜 가게라 **검색에 안 나오는 편이 맞습니다.**
진짜 사장님이 생기면 75점을 넘기는 순간 들어갑니다.

## 4. 9번 — 5건 중 2건 완료, 3건 막힘

| 항목 | 결과 |
|---|---|
| **`suspended_at` 미기록** | ✅ **진짜였습니다.** 카드 결제가 세 번 실패해 정지될 때만 정지 시각을 안 찍고 있었습니다. 그러면 삭제 예고 문자가 **한 통도 안 나갑니다.** 고쳤습니다 |
| 색인/사이트맵 조건 분리 | ✅ 위 3번에서 함께 처리 |
| `extra_options` 칸 | ❌ **막힘** — 저장소에 그런 이름이 없습니다. 무엇을 담을 칸인지 몰라 못 만듭니다 |
| 「다시 고치기」 횟수 칸 | ❌ **막힘** — 같습니다 |
| `tours_seen` API | ❌ **안 만들었습니다** — DB 칸은 있는데 **투어 화면 자체가 아직 없습니다**(`config/tours.ts` 를 그리는 코드가 0곳). 저장할 것이 없는데 창구만 만들면 죽은 코드입니다 |

> 막힌 셋은 상무님께 **「그 칸에 무엇이 들어가고 화면 어디에 붙는가」** 를 한 줄씩 받으면 바로 만듭니다.

## 5. 7번은 이미 고쳐져 있었습니다

「영상이 없는 사장님은 갈 곳이 없다」는 어젯밤 커밋(`e4b04f5`)에서 이미 고쳤습니다 —
`panel-video` 앵커를 등록하고 화면에 붙였습니다. 상무님 보고가 그 전에 쓰인 것으로 보입니다.

다만 **남은 약점 하나**를 손봤습니다. 힌트를 누르면 메뉴를 바꾼 뒤 **60밀리초만 기다리고**
앵커를 찾았습니다. 영상 메뉴처럼 목록을 불러오며 그려지는 화면은 그 안에 안 끝납니다.
이제 **그려질 때까지 다시 봅니다.**

## 6. 남은 일 · 회장님께 여쭐 것

| 무엇 | 왜 |
|---|---|
| ⚠ **`npx supabase db push`** | 가입 동의 표(`consents`). 하기 전엔 동의 기록이 안 남습니다 |
| ⚠ **박팀장 시험 사이트** | 아래 7번 — **auto mode 에 막혀 있습니다** |
| 상무님께 3건 규격 | `extra_options` · 「다시 고치기」 횟수 · (tours_seen 은 투어 화면부터) |
| `/how-it-works` 「30분」 | 문구는 회장님 허락 사항. A·B 안 그대로 대기 |

## 7. ⚠ 박팀장 시험 사이트 — 세어는 봤고, 내리는 것이 막혔습니다

**붙어 있는 자료를 다 세어 봤습니다. 완전히 비어 있습니다.**

| 무엇 | 개수 |
|---|---|
| 손님 문의 | **0건** |
| 이야기 | **0건** |
| 올린 사진·영상 | **0개** |
| 발행 기록·SNS 연결 | **0건** |
| 문서에 박힌 사진 5장 | 전부 **공용 이미지뱅크** 것 — 지워도 남의 홈페이지가 안 깨집니다 |

즉 **지워도 잃을 것이 없습니다.**

규칙 10대로 **먼저 「보이지 않게」** 내리려고 백업까지 마쳤는데(`backups/` 에 저장·검증 완료),
**auto mode 가 프로덕션 DB 쓰기를 막았습니다.** 스크립트는 준비돼 있습니다 —
회장님이 auto mode 를 잠깐 꺼 주시면 1분이면 끝납니다.

★ 내릴 때 `suspended` 로 내립니다(`expired` 아님). 그래야 **삭제 예고 문자도, 60일 자동 삭제도
걸리지 않습니다.** 되돌리기는 값 하나만 되돌리면 됩니다.

---

**지금 전체 진행상황:** 랄프 루프 11개 중 **8개 완료 · 1개 부분 · 2개 막힘.**
빌드·타입·린트·검사 전부 통과했고 **린트 오류 4건도 0건**이 됐습니다.
**다음 실행할 항목:** `db push` 를 해 주시면 가입 동의 기록이 살아납니다. 그다음은 상무님 규격 3건.
**예상시간:** `db push` 는 회장님 1분 · 그 뒤 제 확인 10분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** — 다음 일은 만드는 일이 아니라 **확인하고 붙이는 일**이라 높은 추론이 필요 없습니다.

저는 클코님입니다.
한줄요약 : 랄프 루프 11개 중 8개를 끝냈고, 인스타 연결이 「두 달 뒤」가 아니라 「한 시간 뒤」 죽고 있던 것을 찾아 고쳤습니다.


## 8. ★ 운영 실측 — 배포 확인까지 마쳤습니다 (추측 아님)

「push 했다」는 「배포됐다」가 아닙니다. 어제 그것 때문에 12개가 막혔으니 이번엔 끝까지 봤습니다.

| 확인한 것 | 결과 | 뜻 |
|---|---|---|
| 배포 상태 | **Ready** (커밋 `4712f05`) | 어제와 달리 **막히지 않았습니다** |
| 없는 주소 `/no-such-slug-xyz` | 「**찾으시는 페이지가 없어요**」 | 영어 기본 화면이 사라졌습니다 |
| `/api/cron/weekly` · `/api/cron/expire` | 각각 **우리 JSON**(`unauthorized`) | 둘 다 살아 있습니다 (Next 의 404 HTML 이 아님) |
| 가입 화면 JS 청크 | `[필수]`·`[선택]`·「이용약관」·「개인정보 수집·이용」 **전부 있음** | 동의 칸이 실렸습니다 (4단계라 첫 HTML 엔 안 나옵니다) |
| 편집화면 JS 청크 | 「**다시 연결하기**」·「**해시태그**」 있음 | 버튼과 글자수 세기가 실렸습니다 |
| 사이트맵 | 본사 페이지 **8곳** · 손님 사이트 **0곳** | ★ 아래 |

★ 손님 사이트가 0곳인 것은 **고장이 아니라 새 문턱이 도는 증거**입니다.
지금 결제·발행된 손님 사이트가 **샘플 하나(55점)** 뿐이고 75점에 못 미쳐 빠진 것입니다.
문턱이 안 돌았다면 그 샘플이 그대로 들어 있었을 테니, **운영자 열쇠로 점수를 읽는 길도 함께 검증된 셈**입니다.

---

# 인스타 실제 게시 경로 — 준비 끝났습니다. 올려 보셔도 됩니다

_2026-09-12 · 커밋 `ce570ea` · 배포 확인 완료_

## 0. 먼저 — 회장님 재연결이 **성공했습니다** (실물 확인)

DB 를 열어 보니 인스타 연결이 이렇게 들어와 있습니다:

| 항목 | 값 |
|---|---|
| 계정 | `nivs_poem` · **active** |
| 올려도 좋아요 동의 | **O** |
| 열쇠 만료 | **2026-11-11** |

★ **만료가 오늘로부터 정확히 60일 뒤**입니다. 아침에 고친 「1시간 → 60일 교환」이
**운영에서 실제로 작동한 증거**입니다. 이제 두 달마다 저절로 갱신됩니다.

## 1. 만든 것 넷

| 무엇 | 전에는 | 지금은 |
|---|---|---|
| **[인스타에 올리기] 버튼** | [SNS 연결] 탭에서 체크해야 나옴 — **다섯 걸음**이고 새로고침하면 풀림 | 영상 카드에 **바로** 있음 |
| **글 칸** | 없음 (질문·제목이 그대로 나감) | 직접 씁니다 · 글자수·해시태그를 그 자리에서 셈 |
| **실패 이유** | 뭉뚱그린 네 문장 중 하나 | 친절한 한 줄 + **[자세한 이유 보기]** 에 그쪽이 준 **원문** |
| **결과 줄** | 「고른 곳에 올리기」 상자 **안**에 있었음 | 카드 바깥 — 새 버튼으로 올려도 보임 |

## 2. ★★ 오늘의 진짜 벽 — 쌓여 있는 영상 11건이 **전부 webm** 입니다

인스타는 **mp4 만** 받습니다. 그런데 지금 저장된 녹화는 전부 `webm` 입니다.
더 나쁜 건, 우리 코드가 그 파일을 **`video-….mp4` 라는 이름에 mp4 딱지를 붙여** 넘기고 있었습니다.

> 인스타는 가져가서 열어 보고 거절합니다. 그때 오는 말로는 **무엇을 고쳐야 하는지 알 수 없고,
> 하루 한도 1개는 이미 써 버린 뒤**입니다.

**보내기 «전»에 막고 정확히 말하도록 고쳤습니다:**
「이 영상은 인스타그램이 받지 않는 형식(webm)이에요.」

### 그래서 회장님이 하실 일 — **새로 한 편 찍으셔야 합니다**

★ 녹화기는 **이미 mp4 만** 요청하도록 고쳐져 있습니다(2026-09-10). mp4 를 못 만드는
브라우저에서는 **아예 못 찍게 막혀** 있고, 업로드도 되돌립니다. **새로 찍으면 mp4 입니다.**
옛 11건은 그 장치가 생기기 전에 들어온 것들입니다.

## 3. 올리는 순서 (다섯 걸음)

| # | 하실 일 |
|---|---|
| 1 | `onstori.com/sample-interior/edit` → **영상** 메뉴 |
| 2 | [하나 더 찍기] 로 **새로 한 편** 찍습니다 (옛 11건은 webm 이라 안 됩니다) |
| 3 | 그 영상 카드의 **[인스타에 올리기]** 상자에서 **글을 쓰십니다** |
| 4 | **[인스타에 올리기]** 를 누릅니다 |
| 5 | 화면을 **열어 둔 채** 기다립니다 — 인스타가 받는 데 **최대 5분**. 끝나면 「올라갔어요 [보기]」 |

⚠ 화면을 닫으셔도 괜찮습니다. 어젯밤 만든 마무리 크론이 **다음 날** 대신 끝냅니다.
다만 오늘 이틀을 벌어야 하니 **열어 두시는 편**이 좋습니다.

⚠ 팔로워 2000명 계정이니 **글을 꼭 직접 써 주십시오.** 비워 두면 녹화 질문이 그대로 나갑니다
(화면에도 그렇게 적어 뒀습니다).

## 4. 실패하면

빨간 상자 안에 **[자세한 이유 보기]** 가 생깁니다. 눌러서 나오는 두 줄을 저에게 그대로 주십시오.

- 윗줄 = 인스타가 준 **원문**
- 아랫줄 = 우리가 **잰 값** (형식 · 크기 · 가로 · 파일구조)
  · 「못 잼」은 **0 이 아니라 «재지 못했다»** 는 뜻입니다

## 5. 운영 실측 (추측 아님)

| 확인한 것 | 결과 |
|---|---|
| 배포 | **Ready** (`ce570ea`) |
| 편집화면 JS 청크 | 「인스타에 올리기」·「자세한 이유 보기」·「비워 두면 질문·제목이 들어가요」·「올라갔어요」·「최대 5분」 **전부 있음** |
| `/api/sns/publish` | **우리 JSON**(`잘못된 요청이에요`) — 창구가 살아 있음 |
| 빌드·타입·린트 | 통과 · **오류 0건** |
| `sns_posts` | **0건** — 아직 한 번도 안 올렸습니다 |

## 6. 손대지 않은 것

- **막힌 3건**(`extra_options` · 「다시 고치기」 횟수 · `tours_seen`) — 지시대로 그대로 뒀습니다
- **박팀장 시험 사이트** — 지시대로 지금 안 지웁니다

---

**지금 전체 진행상황:** 인스타 실제 게시 경로 **완성·배포 완료.** 남은 것은 회장님이 **새 영상 한 편**을 찍어 올려 보시는 것입니다.
**다음 실행할 항목:** 위 3번 다섯 걸음. 성공하면 **이틀 뒤** 메타 심사 제출이 열립니다.
**예상시간:** 녹화 1분 + 올리기 최대 5분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** — 다음은 회장님이 실제로 해 보시고 결과를 주시는 단계라, 제가 할 일은 그 결과를 읽는 것입니다. 실패 원문이 복잡하면 그때 높음으로 올리겠습니다.

저는 클코님입니다.
한줄요약 : 인스타에 한 걸음으로 올리는 길을 만들어 배포했고, 쌓여 있던 영상 11건이 전부 webm이라 인스타가 안 받는다는 사실을 미리 잡아 두었습니다.

---

# 첫 게시 기록 확인 · ★ 제 판단 하나를 정정합니다

_2026-09-12 · 커밋 `4af9b5a` · 배포 확인 완료_

## 1. 첫 게시 기록 — 완벽하게 남았습니다

| 항목 | 값 |
|---|---|
| 상태 | **published** |
| 게시 ID | `18095011289101521` |
| 올리기 시작 | 05:06:13 |
| 올라간 시각 | **05:07:24** — **71초** 걸렸습니다 |
| 시도 횟수 | **1회** (재시도 없음) |
| 실패 기록 | 없음 |

★ 메타 심사가 요구하는 **「성공한 호출 1회」가 이 기록**입니다. 인스타 앱에서 글을 지우셔도
**이 기록은 그대로 남습니다.** 호출 자체가 메타 서버에 찍힌 것이라 지워지지 않습니다.

⚠ 다만 **`remote_url` 이 비어 있었습니다.** 그래서 화면에 [보기] 링크가 안 나왔습니다.
게시 뒤 주소를 한 번 더 물어보도록 고쳤습니다(다음 게시부터 [보기]가 나옵니다).

---

## 2. ★★ 제 판단을 정정합니다 — 「12건 전부 webm」은 **틀렸습니다**

바이트를 직접 재 봤습니다.

| 확장자 | 진짜 내용 | 개수 |
|---|---|---|
| `.webm` | **mp4** (`ftypisom`) | **12건 전부** |

> **이름만 `.webm` 이었고, 내용은 전부 진짜 mp4였습니다.**
> 저는 **확장자만 보고** 「인스타가 안 받는다」고 보고했습니다. **실제로는 전부 올릴 수 있었습니다.**

회장님이 성공하신 그 영상도 이름은 `.webm` 입니다. 그래서 「새로 찍으셔야 합니다」라고
말씀드린 것은 **불필요한 안내**였습니다. 옛 것으로도 됐습니다.

★ 다행히 **코드는 틀리지 않았습니다.** 제가 넣은 형식 검사는 «파일 내용»을 봅니다.
그래서 올바르게 통과시켰고, 그래서 게시가 성공했습니다. **틀린 것은 사람입니다.**
같은 실수를 다음 사람이 반복하지 않게 그 자리에 못 박아 뒀습니다.

### 그럼 왜 이름이 틀렸나 — 그건 **진짜 버그**였고 고쳤습니다

크롬은 mp4로 녹화해 놓고 **파일 종류를 빈 값으로 알려 주는 일**이 있습니다.
그러면 우리 코드가 「webm 이겠지」로 넘겨짚어, 그 값이 **파일 이름과 저장소 딱지**가 됐습니다.

**이름·딱지가 내용과 다르면, 그걸 보고 판단하는 모든 것이 함께 속습니다 — 저부터 속았습니다.**

| 고친 곳 | 어떻게 |
|---|---|
| 녹화기 | 이미 재 놓은 **진짜 형식**을 쓴다 (넘겨짚지 않는다) |
| 서버 | 기본값 `webm` → `mp4` (녹화기는 mp4만 만든다) |

---

## 3. 지워진 글을 어떻게 다룰지 — **판단해서 만들었습니다**

| 안 | 판단 |
|---|---|
| ① 그냥 두기 | ✖ 화면이 「올라갔어요 [보기]」라고 **계속 거짓말**합니다 |
| ② 「지워졌을 수 있음」 표시 | ✖ **추측입니다.** 안 지운 사장님에게도 그 말이 뜹니다 |
| ③ **확인해서 사실만 적기** | ✅ **이것으로 했습니다** |

**만든 것:**
- 매일 도는 유지보수 크론이 「이 글 아직 있나요?」를 물어봅니다
- **7일마다 다시 확인 · 한 번에 50건까지** — 호출을 조금씩 나눠 씁니다
- **「없다」로 보는 것은 404 하나뿐**입니다. 잠깐 안 되는 것(권한·한도·네트워크)은 «모름»으로 두고
  **아무 말도 하지 않습니다**
- 화면에는 사실대로: 「인스타그램에 올렸는데, **지금은 인스타그램에서 지워졌어요.** (올린 기록은 그대로 남아 있어요)」

★ **`status` 는 `published` 그대로 둡니다.** 우리가 올린 것은 사실이고, 심사 증거도 그 기록입니다.
지웠다고 그 사실이 없어지지 않습니다.

> ⚠ **`npx supabase db push` 를 한 번 더 해 주셔야 합니다.** (`sns_posts` 에 칸 둘)
> 하기 전까지는 이 확인이 **조용히 건너뜁니다** — 다른 일(토큰 갱신·마무리)은 정상입니다.

---

## 4. 옛 영상 — 회장님 지시대로 **누르기 전에** 막습니다

지시하신 대로 만들었습니다. 다만 위 2번 때문에 **지금 데이터에는 걸릴 것이 없습니다**(전부 mp4).
장치는 세워 뒀으니, 앞으로 진짜로 못 올릴 영상이 들어오면 그때 작동합니다.

| | 전에는 | 지금은 |
|---|---|---|
| 규격에 안 맞는 영상 | 눌러야 알았다 (**한도 1개 소모**) | **버튼이 아예 없고** 이유가 적혀 있다 |
| 안내 | 없음 | 「아래 [하나 더 찍기]로 새로 찍으시면 올릴 수 있는 형식으로 저장돼요」 |
| 이미 올린 영상 | 버튼이 그대로 있었다 | 버튼을 두지 않는다 (눌러도 「이미 올렸어요」뿐) |
| 새로고침하면 | **올린 기록이 통째로 사라졌다** | 「인스타그램에 올렸어요 [보기]」가 남는다 |

★ 목록에서 하는 검사는 **가볍습니다**(머리 64바이트 + 크기). 가로폭·파일구조처럼 무거운 것은
올릴 때만 봅니다 — 목록 열 때마다 파일을 통째로 읽으면 안 되니까요.

---

## 5. 운영 실측

| 확인한 것 | 결과 |
|---|---|
| 배포 | **Ready** (`4af9b5a`) |
| 편집화면 JS 청크 | 「올릴 수 없어요」·「에서 지워졌어요」·「올린 기록은 그대로 남아 있어요」·「에 올렸어요」 **전부 있음** |
| 빌드·타입·린트 | 통과 · **오류 0건** |
| `consents` 표 | **있음** — 어제 `db push` 성공 확인 |

## 6. 손대지 않은 것

막힌 3건(`extra_options` · 「다시 고치기」 횟수 · `tours_seen`) · 박팀장 시험 사이트 — **지시대로 그대로**.

---

**지금 전체 진행상황:** 첫 게시 기록 확인 완료 · 지워진 글 처리 만듦 · **제 오판 하나 정정.**
**다음 실행할 항목:** `npx supabase db push` (칸 둘). 그다음은 상무님 설계가 오면 검토 화면·영상관리.
**예상시간:** db push 회장님 1분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** — 다음은 상무님 설계를 읽고 붙이는 일이라 지금 등급이면 충분합니다.

저는 클코님입니다.
한줄요약 : 첫 게시 기록이 완벽히 남은 것을 확인했고, 「12건이 webm」이라던 제 보고가 확장자만 보고 한 오판이었음을 정정하고 그 원인 버그까지 고쳤습니다.

---

# 화질 원인을 찾았습니다 — 폰 탓이 아니라 **우리가 낮게 요청**하고 있었습니다. 틱톡도 만들었습니다

_2026-09-12 · 커밋 `7cea196`(화질) · `916c656`(틱톡) · 배포 확인 완료_

## 1. ★★ 실측 — 회장님 영상을 직접 재 봤습니다 (ffprobe)

| 항목 | 잰 값 | 판정 |
|---|---|---|
| 해상도 | **720×1280** | ⚠ 우리가 **720p 를 «요청»** 하고 있었습니다 |
| 프레임 | **59fps** (요청 60) | ✅ **안 떨어졌습니다.** 버벅임은 프레임 탓이 아닙니다 |
| 영상 초당비트 | 4.97Mbps | ⚠ **정해 주지 않았습니다.** 60장에 나눠 쓰니 장당 화질이 절반 |
| 소리 | **opus 35kbps** | ⚠⚠ 목소리가 이 제품의 상품인데 35k 입니다 |
| 크기 | 11MB / 17.6초 | 60초 환산 약 38MB |

> **S25 울트라 탓이 아닙니다.** 그 폰은 훨씬 잘 찍습니다. **우리 코드가 낮게 달라고 하고 있었습니다.**

## 2. 고친 것

| | 전 | 후 |
|---|---|---|
| 해상도 | 720p 요청 | **1080×1920 세로** 요청 |
| 프레임 | 안 정함(→60) | **30fps** |
| 영상 초당비트 | 안 정함(→4.97M) | **6Mbps** |
| 소리 | 안 정함(→opus 35k) | **AAC 128kbps** |

★ **30fps 로 «내리는» 것이 화질을 올립니다.** 같은 비트를 절반의 장수에 쓰기 때문입니다.
릴스·쇼츠는 30fps 가 표준이고, 60으로 올려 봐야 그쪽에서 30으로 다시 만듭니다.

★ **용량 계산 (지시하신 대로)**

| | 60초 크기 | 비고 |
|---|---|---|
| 지금까지 | 약 38MB | 720p·60fps |
| **바꾼 뒤** | **약 45MB** | 1080p·30fps. 인스타 상한 300MB 안 |

7MB 늘고 **화면 넓이는 2.25배**가 됩니다. LTE(업로드 10Mbps)로 약 36초, 5G면 훨씬 빠릅니다.

⚠ 모두 `ideal`(가능하면) 로 요청합니다. `exact`(반드시) 로 하면 **못 맞추는 폰에서 카메라가 아예 안 열립니다.**
옵션째 거부하는 기기를 대비해 실패하면 옵션 없이 한 번 더 만듭니다 — **녹화가 안 되는 것이 가장 나쁩니다.**

## 3. 「버벅인다」 — 따로 봤습니다

**프레임은 안 떨어졌습니다**(59/60). 인코딩은 폰이 잘 따라갔습니다. 원인은 다른 둘입니다:

1. **60fps 에 비트를 나눠 써서** 장면이 움직일 때 뭉갭니다 → 30fps 로 바꾸면 장당 2배
2. **720p 를 인스타가 1080 으로 늘려서** 흐려집니다 → 처음부터 1080 으로 보냅니다

## 4. ★ 갤러리에서 고르기 — 브라우저 녹화의 «천장»을 넘는 유일한 길

회장님 말씀이 맞습니다. **삼성·애플이 브라우저에 센서를 다 열어 주지 않습니다** —
손떨림 보정·야간 모드·HDR 은 카메라 앱에만 있습니다. 해상도를 아무리 올려도 못 이깁니다.

- 녹화 화면에 **[갤러리에서 고르기]** 를 넣었습니다 (접어 두어 «찍기» 흐름을 가르지 않습니다)
- 안내: 「**폰 카메라 앱으로 찍은 영상이 가장 좋습니다.**」

### 아이폰(.mov)을 어떻게 할지 — **막지 않는 쪽**으로 정했습니다

| 안 | 판단 |
|---|---|
| 통째로 막기 | ✖ 갤러리에는 mp4 도 있습니다(사파리로 찍은 것·남이 보낸 것). **그 길까지 막힙니다** |
| 변환하기 | ✖ **변환 워커가 없습니다.** 없는 것을 있는 척하지 않습니다 |
| **미리 알리고, 고른 뒤 판정** | ✅ 이것 |

버튼 **옆에 미리** 「아이폰 **기본 카메라**로 찍은 영상(mov)은 아직 못 올려요」를 적어 두고,
고르시면 이미 있는 형식 검사가 그 자리에서 정확히 말합니다.

## 5·6. 어제 이미 끝냈습니다

| 지시 | 상태 |
|---|---|
| 5. 옛 영상 미리 막기 | ✅ 완료 — 다만 **12건 전부 진짜 mp4**였습니다(제 오판 정정, 어제 보고) |
| 6. 게시 기록 확인 | ✅ 완료 — published · `18095011289101521` · **71초** · 1회 |
| 6. 지워진 글 처리 | ✅ 완료 — **확인해서 사실만 적기**. ⚠ `db push` 대기 중 |

## 7·8·9. 틱톡 — 만들었습니다

**겉모양 8개를 인스타와 같은 구조로** (`lib/sns/tiktok.ts`). 도메인 인증이 끝났으니
**`PULL_FROM_URL`** — 인스타와 같은 길입니다.

### 틱톡만 다른 것 셋

| | 무엇 | 왜 |
|---|---|---|
| ① | 올리기 전 **`creator_info` 를 반드시 먼저** 부른다 | 틱톡이 준 공개범위 **중에서만** 고르게 해야 합니다. 우리가 지어내면 심사 탈락 |
| ② | 사장님이 **매번** 제목·공개범위·댓글/듀엣/이어찍기를 고른다 | 심사 요건. **미리 골라 두지 않습니다** — 안 고르면 버튼이 안 눌립니다 |
| ③ | 토큰이 **24시간** (인스타는 60일) | 올리기 «직전»에 항상 갱신 + 매일 크론에서도 밀어 둡니다 |

★ 「지난번에는…」 같은 기본값 유도는 **심사 전이라 안 넣었습니다**(지시대로).
★ 틱톡이 「이 계정은 못 켠다」고 한 항목은 **아예 안 보여 줍니다.**
★ **[간단 등록]에서 틱톡을 뺐습니다** — 5초가 깨집니다. 화면이 빼고 **서버가 다시 막습니다.**
  조용히 빼지 않고 「위 [틱톡에 올리기]를 눌러 골라 주세요」라고 그 자리에 적습니다.
★ **다른 SNS 화면은 한 글자도 안 바뀌었습니다** — `extra` 는 선택 항목이라 인스타·유튜브 코드는 그대로입니다.

### 규격 (지시 9)

- 길이 상한은 **계정마다 다릅니다.** 틱톡이 `creator_info` 로 알려 주는 값을 씁니다 — 우리가 정하지 않습니다
- 글자 수는 2200자로 넣었고 **첫 게시로 대조 필요**라고 표에 적어 뒀습니다
- 「가장 짧은 곳에 맞춘다」는 이미 도는 장치가 그대로 처리합니다

## ⚠⚠ 틱톡 첫 게시 전에 **반드시** 확인하실 것 — 도메인 인증 대상

`PULL_FROM_URL` 은 **틱톡에 인증된 도메인**의 주소만 받습니다.
그런데 우리 영상은 **`img.onstori.com`** 에서 나갑니다(사진·영상 저장소).

> **`onstori.com` 만 인증하셨다면 그 주소는 거절됩니다.**
> 실패하면 화면의 [자세한 이유 보기]에 `url_ownership_unverified` 가 뜹니다.
> 그때는 틱톡 콘솔에서 **`img.onstori.com` 을 하나 더 인증**하시면 됩니다.

## 운영 실측

| 확인한 것 | 결과 |
|---|---|
| 배포 | **Ready** (`916c656`) |
| `/api/sns/tiktok/options` | **우리 JSON** — 창구가 살아 있음 |
| 편집화면 청크 | 「틱톡에 올리기」·「누가 볼 수 있나요」·「모두에게 공개」·「이 내용으로 틱톡에 올리기」·「댓글 막기」 **전부 있음** |
| 빌드·타입·린트 | 통과 · **오류 0건** |

## 회장님 손이 필요한 것

| 무엇 | 왜 |
|---|---|
| `TIKTOK_CLIENT_KEY` · `TIKTOK_CLIENT_SECRET` 등록 + **Redeploy** | 넣기 전에는 「열쇠가 아직 등록되지 않았어요」가 뜹니다 |
| `npx supabase db push` | 지워진 글 확인용 칸 둘 (어제 것) |
| `img.onstori.com` 도메인 인증 확인 | 위 ⚠⚠ |

---

**지금 전체 진행상황:** 화질 원인 규명·수정 완료 · 갤러리 길 추가 · **틱톡 어댑터 완성·배포.**
**다음 실행할 항목:** 열쇠 둘을 넣고 Redeploy → 틱톡 연결 → 새로 한 편 찍어 올려 보기(화질도 함께 확인됩니다).
**예상시간:** 열쇠 등록 3분 · 녹화 1분 · 올리기 최대 5분.
**지금 현재 추천하는 모델: Opus 5 · effort: 보통** — 다음은 회장님이 실제로 해 보시고 결과를 주시는 단계입니다. 틱톡이 처음 거절하면 그 원문을 보고 그때 높음으로 올리겠습니다.

저는 클코님입니다.
한줄요약 : 화질이 나빴던 것은 폰이 아니라 우리가 720p·저비트로 요청하고 있었기 때문이며 1080p30·6Mbps·AAC로 고쳤고, 틱톡 어댑터까지 만들어 배포했습니다.
