# 2026-09-18 — 토스 급건 + A·B·C + [50][51] 까지 한 바퀴

## 1. 🔴 토스 급건 — 끝났습니다 (커밋 `2a409d2`)

`app/my/page.tsx` 에서 **로그인한 메일이 `REVIEW_EMAIL` 과 같을 때만** 목록 자르기를 건너뜁니다.

```
심사 계정   → 가진 홈페이지 전부 (두 장)
그 밖 계정  → 지금 그대로 1개
```

- 🔴 `SITES_PER_ACCOUNT`(1) 는 **손대지 않았습니다** — 일반 회원의 제품 규칙입니다
- `REVIEW_EMAIL` 이 없는 곳(로컬)에서는 **아무 일도 일어나지 않습니다**
- **순서(`updated_at`)로는 풀지 않았습니다** — 심사관은 1~2개월 뒤에 봅니다

**배포 «전»에 운영 DB 를 읽어 미리 답한 것 셋**

| 확인 | 미리 잰 답 |
|---|---|
| 두 장이 되나 | ✅ 두 샘플의 주인이 **같은 한 계정**입니다(`re***@onstori.com`) |
| 「9999일」이 뜨나 | ✅ **안 뜹니다.** `sample-toss` 는 체험끝 10/16 ⇒ **「무료 D-28」** |
| 다산 리모델링 | ✅ `status=active` ⇒ 「정회원」 · 결제창 안 열림이 **정상** |

⚠ **카드 두 장이 실제로 보이는지 · 토스 카드창이 열리는지는 배포 뒤 대표님 손**입니다.
심사 열쇠가 **로컬에 없어서**(운영에만 있습니다) 제가 심사 계정으로 로그인해 볼 수 없습니다.

## 2. 오늘 고친 나머지 넷

| 커밋 | 무엇 |
|---|---|
| `1e18772` | **A-2** 첫 페이지 03 문구 · **B-8·B-9·B-10** 나가기 하나로 · **C** 카드형태 버그 넷 |
| `e84d053` | **[49]③** 글자 알약 → 우측 세로줄 **공식 로고 단추** (숫자는 B-12 로 폐기) |
| `51636a8` | **[51]** 영상 비트 환경변수 · 카메라에 **처음부터 720** 요청 · `rec_start` 로그 |
| `8b5dc58` | 지난 창이 써 놓고 커밋 못 한 **[50][51][52] 지시서** |

**몰입모드가 이렇게 바뀝니다 (실측 · PC 1280×860 · 폰 375×812 둘 다 같음)**

```
우상    소리 단추            (✕ 는 없앴습니다 — B-9)
우측    공식 로고 단추 세로줄 (영상에서 16px · 바닥에서 90px)
하단중앙 [숏폼 나가기]        (화면 바닥에서 «정확히 10px» — B-10)
```

## 3. 🔴 「짐작이 틀렸던 것」 둘을 숫자로 잡았습니다

**① 랜덤이 안 섞이는 것은 «버그가 아니었습니다».** 운영 DB 실측:
`sample-interior = {"order":"newest", "style":"cards", "stageN":5}` — **랜덤이 켜진 적이 없습니다.**
저장 자체는 됩니다(`style:cards`·`stageN:5` 가 기본값이 아닌 값으로 저장돼 있습니다).
⇒ **누르실 때 무엇이 막는지**를 봐야 합니다.

**② 카드형태 좌우 이동 — `scrollIntoView` 가 «1px 도» 안 움직였습니다.**
같은 자리에서 `rail.scrollTo({left:900})` 는 1028 로 제대로 움직였습니다.
⇒ 가야 할 자리를 **셈해서 직접** 옮기도록 바꿨습니다. 무한 회전도 숫자로 확인했습니다
(첫 편에서 [이전] → 스크롤 0 → 2057 · 점 0 → 8).

## 4. ⚠ 못 쟀다고 밝혀 둘 것 넷

- **소리가 두 번 나는지**는 실기기 소리로 못 쟀습니다(시험 브라우저는 소리를 안 냅니다)
- **한 칸 부드러운 스크롤**은 «숨은 창»에서 진행되지 않아 못 쟀습니다(셈은 확인 — 중앙 오차 0px)
- **[51] 을 실제 폰으로 못 찍어 봤습니다** — `/rec` 는 문자 서명 링크로만 들어갑니다
- **유튜브 링크가 달린 영상이 없어** 로고가 «둘 나란히» 선 모습은 못 봤습니다

## 5. 대표님 손이 필요한 것

1. 🔴🔴 **푸시 11건** — 「푸시해」 한 마디. **토스 회신이 여기에 묶여 있습니다**
2. 🔴 **폰으로 한 번 녹화** — `rec_start` 로그로 카메라가 실제로 준 크기를 봅니다
3. 🔴 **랜덤 다시 눌러 보기** — 빨간 글자가 뜨면 그대로 주십시오
4. ⚠ **문구 둘** — 「해야할지」를 그대로 둘지 · 두 번째 「03」도 바꿀지
5. ⚠ **자리 둘** — 무대 나가기를 10px 로 내릴지 · PC 로고줄을 영상 «바깥»으로 뺄지

검사: `tsc` 0 · `build` 0 · `검사` 11건 실패 0 · `eslint` 0(전부터 있던 경고 1건 제외)

저는 클코님입니다.
한줄요약 : 토스 심사 계정이 `/my` 에서 홈페이지 두 장을 보게 고쳤고, A-2·B-8~B-10·C 버그 넷·[49]③ 로고 단추·[51] 영상 비트까지 커밋해 «푸시 한 마디»만 남았습니다.
