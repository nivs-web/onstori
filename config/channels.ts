/**
 * 발행 채널 — **문구·목록의 단일 출처.** (2026-09-11 상무님 목록으로 개편)
 *
 * ★★ 전에는 목록이 **두 군데**에 있었다 — 여기(글자)와 `components/site/chrome.tsx`(로고용 객체).
 *   같은 것을 두 곳에 두면 한쪽만 고쳐진다. 실제로 순서·표기가 갈라져 있었다.
 *   **하나로 합쳤다.** chrome.tsx 는 이제 여기서 가져다 쓴다.
 *
 * ★ 2026-09-11 확정 6곳(회장님): 유튜브 쇼츠 · 인스타 릴스 · 틱톡 · 쓰레드 · X · 페이스북
 *   ⚠ **네이버 블로그를 뺐다** — 자동 게시 API 가 2020년에 없어졌다(김팀장 확인).
 *     공식 목록에 「블로그 글쓰기」가 없다. 카페에는 있는데 블로그만 빠졌다.
 *   ⚠ **「온스토리 사이트」도 뺐다** — 그건 «퍼뜨리는 곳»이 아니라 사장님 홈페이지 자체다.
 *
 * ⚠ 숫자를 손으로 적지 마라. 「6곳」이 필요하면 `CHANNEL_COUNT` 를 써라.
 *   손으로 적으면 목록이 바뀔 때 화면이 거짓말을 한다(2026-09-11 전수 교체).
 */

export const CHANNELS = [
  { id: "youtube", name: "유튜브 쇼츠", short: "Shorts" },
  { id: "instagram", name: "인스타 릴스", short: "Reels" },
  { id: "tiktok", name: "틱톡", short: "TikTok" },
  { id: "threads", name: "쓰레드", short: "Threads" },
  { id: "x", name: "X(트위터)", short: "X" },
  { id: "facebook", name: "페이스북", short: "Facebook" },
] as const;

/** 한 줄로 이어 붙인 것 — 화면에서는 거의 항상 이걸 쓴다 */
export const CHANNELS_LINE = CHANNELS.map((c) => c.name).join(" · ");

/** 개수 — "6곳" 처럼 숫자를 적는 자리. 채널이 늘면 여기도 같이 바뀐다 */
export const CHANNEL_COUNT = CHANNELS.length;

/** 「6곳 동시 발행」 카드의 설명 한 줄 */
export const CHANNELS_PITCH =
  `${CHANNELS_LINE}. 동영상 60초만 찍었는데 ${CHANNEL_COUNT}곳에 동시에 발행됩니다.`;

/**
 * 해지해도 남는 것 — 「전부 사장님 것이다」 자리에 쓴다.
 * ⚠ 전에는 "홈페이지·영상·기록은 해지해도 가져간다" 였는데, 우리에게 **내보내기 기능이 없어서**
 *   사실과 달랐다(2026-09-07 회장님 지적). 아래는 사실이다 — 이미 올라간 글은 우리가 못 지운다.
 *
 * ★★ 2026-09-11 — 전에는 `slice(0, 5)` 였다. 마지막 하나가 「온스토리 홈페이지」라
 *   해지하면 사라지는 곳이어서 뺀 것이었다. 그런데 목록에서 온스토리가 빠진 지금
 *   그대로 두면 **페이스북 하나가 문장에서 조용히 사라진다.** 이제 전부 쓴다.
 */
export const KEEP_AFTER_CANCEL =
  `해지하셔도 ${CHANNELS_LINE}에 올라간 기록은 그대로 남습니다.`;
