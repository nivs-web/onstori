/**
 * 발행 채널 6곳 — **문구의 단일 출처**.
 *
 * ⚠ 이 목록을 화면에 복사해 적지 마라. `CHANNELS_LINE` 을 import 해서 써라.
 *   2026-09-07 전에는 페이지마다 순서·표기가 제각각이었다
 *   («네이버 블로그(복사 30초)·온스토리 사이트» / «네이버·홈페이지» / «X(트위터)» / «X» …).
 *   나중에 «네이버 블로그(복사 30초)» 처럼 다시 바뀔 수 있으므로 한 곳에서만 고친다.
 *
 * 확정 문구·순서 (2026-09-07 회장님):
 *   유튜브 쇼츠 · 인스타 릴스 · 쓰레드 · X(트위터) · 네이버 블로그 · 온스토리 홈페이지
 */
export const CHANNELS = [
  "유튜브 쇼츠",
  "인스타 릴스",
  "쓰레드",
  "X(트위터)",
  "네이버 블로그",
  "온스토리 홈페이지",
] as const;

/** 한 줄로 이어 붙인 것 — 화면에서는 거의 항상 이걸 쓴다 */
export const CHANNELS_LINE = CHANNELS.join(" · ");

/** 개수 — "6곳" 처럼 숫자를 적는 자리. 채널이 늘면 여기도 같이 바뀐다 */
export const CHANNEL_COUNT = CHANNELS.length;

/** 「6곳 동시 발행」 카드의 설명 한 줄 */
export const CHANNELS_PITCH =
  `${CHANNELS_LINE}. 동영상 60초만 찍었는데 ${CHANNEL_COUNT}곳에 동시에 발행됩니다.`;

/**
 * 해지해도 남는 것 — 「전부 사장님 것이다」 자리에 쓴다.
 * ⚠ 전에는 "홈페이지·영상·기록은 해지해도 가져간다" 였는데, 우리에게 **내보내기 기능이 없어서**
 *   사실과 달랐다(2026-09-07 회장님 지적). 아래는 사실이다 — 이미 올라간 글은 우리가 못 지운다.
 */
export const KEEP_AFTER_CANCEL =
  `해지하셔도 ${CHANNELS.slice(0, 5).join(" · ")}에 올라간 기록은 그대로 남습니다.`;
