/**
 * 🔴 **운영자 로그인이 유지되는 날수 — 단 하나의 출처.** (2026-09-17 지시 [34]·[32])
 *
 * ⚠ 이 숫자를 화면·주석에 **직접 타이핑하지 마라.** 세 곳(심는 곳·지우는 곳·안내 글)이
 *   갈라지면 화면이 거짓말을 한다. `scripts/verify.mjs` 가 그것을 잡는다(규칙 9).
 * ⚠ 요금·무료기간과는 **무관한** 숫자다. 다만 **같은 병**(숫자를 여러 곳에 적는 것)을 막으려고
 *   같은 방식으로 한곳에 둔다.
 *
 * ⚠⚠ **`lib/admin-auth.ts` 에 두지 마라.** 그 파일은 `next/headers` 를 쓴다 —
 *   **서버에서만** 도는 파일이다. 화면(클라이언트) 파일이 그것을 끌어오면 **빌드가 깨진다**
 *   (2026-09-17 실측: `tsc` 는 통과하는데 `npm run build` 가 잡았다).
 *   그래서 **아무 데서나 읽을 수 있는 `config/` 에** 둔다 — 여기가 제품 정책의 단일 출처다.
 */
export const ADMIN_SESSION_DAYS = 30;
