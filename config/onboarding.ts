/**
 * 가입(온보딩) 흐름의 스위치 — **단일 출처.** (2026-09-16 대표님 결정으로 신설)
 *
 * ★★ 대표님 원문: 「동영상을 안 찍을 사업자는 가입도 하지 말아라.」
 *   우리가 파는 것은 홈페이지가 아니라 **「60초로 SNS 여러 곳에 한 방에」**다.
 *   영상을 한 번도 안 찍은 사장님은 그 값어치를 영영 모르고, 그래서 떠난다.
 *
 * 🔴 **이 값 하나로 관문이 열리고 닫혀야 한다.** 전환율이 떨어지면 **배포 없이**
 *   (환경변수로 바꿔) 되돌릴 수 있어야 한다는 것이 지시의 핵심이다.
 */

/**
 * 가입 마지막에 60초 영상을 **반드시** 찍게 할까.
 *
 * ⚠ `false` 로 두면 **2026-09-16 이전과 글자 하나까지 같게** 동작해야 한다 —
 *   만들기가 끝나는 즉시 「홈페이지가 완성됐어요」 화면이 뜬다.
 * ⚠ 환경변수 `ONBOARDING_REQUIRE_VIDEO` 로 덮어쓸 수 있다(`"off"`·`"false"`·`"0"` → 끔).
 *   🔴 **`NEXT_PUBLIC_` 접두사가 붙어야 브라우저까지 내려온다.** 가입 마법사는 클라이언트
 *     컴포넌트라, 접두사 없는 변수는 여기서 항상 `undefined` 로 읽힌다.
 *     Vercel 에 넣으실 때 이름을 그대로 쓰십시오 — 오타 하나면 스위치가 조용히 안 먹는다.
 */
export const REQUIRE_VIDEO_TO_FINISH: boolean =
  !/^(off|false|0)$/i.test((process.env.NEXT_PUBLIC_ONBOARDING_REQUIRE_VIDEO ?? "").trim());

/**
 * 관문에서 찍은 «첫 영상»을 홈페이지에 **자동으로 걸까.**
 *
 * ★ 대표님(2026-09-16): 「관문에서 찍은 첫 영상은 **홈페이지에만 걸고**
 *   SNS 발행은 **기본 꺼짐**으로 두십시오. 빈 컵·하늘이 사장님 인스타에 진짜로 올라가면 사고입니다.」
 *
 * ⚠ **SNS 는 이 스위치와 무관하게 «원래» 안 나간다.** 저장소 전체에서 SNS 로 보내는 길은
 *   `app/api/sns/publish` 하나뿐이고, 그것을 부르는 곳은 편집화면의 [올리기] 버튼뿐이다
 *   (`app/[slug]/edit/videos-panel.tsx`). 자동으로 올리는 크론도 워커도 **없다.**
 *   그러니 이 스위치를 켜도 사장님 인스타에는 아무것도 안 올라간다. 그 사실을 여기 적어 둔다 —
 *   나중에 자동 발행을 만드는 사람이 **이 문단을 먼저 읽어야 한다.**
 *
 * ⚠ 홈페이지에 걸어도 **완성도 점수는 안 오른다.** 거는 것은 `story_entries.visible` 을
 *   건드리지 않기 때문이다(`app/api/site/video/route.ts` 의 주석 참조). 그래서 가입 직후
 *   점수가 **70점 그대로**다 — 지시 [15] 가 요구한 숫자다.
 */
export const ATTACH_FIRST_VIDEO_TO_SITE = true;
