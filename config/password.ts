/**
 * 🔴 **비밀번호 최소 길이 — 단 하나의 출처.** (2026-09-17 지시 [41])
 *
 * ⚠⚠ 2026-09-17 까지 **숫자 8 이 세 곳에** 따로 박혀 있었다 —
 *   `app/login/ui.tsx` · `app/login/new-password/ui.tsx` · `lib/admin-pw.ts`.
 *   한쪽만 고치면 **가입 화면은 10자를 요구하는데 바꾸기 화면은 8자를 받아 주는** 식이 된다.
 *   ⇒ 화면이 말하는 길이와 서버가 받는 길이가 **갈라지는 순간 그게 곧 거짓말**이다(불변 규칙 12).
 *
 * ⚠ **왜 `lib/` 이 아니라 `config/` 인가:** 이 값을 **화면과 서버가 같이** 읽는다.
 *   `lib/` 에 두면 서버 전용 코드(`next/headers`·S3)가 **손님 브라우저 뭉치로 딸려 갈 위험**이 있다
 *   (2026-09-17 에 `lib/admin-auth.ts` 로 실제로 빌드가 깨졌다 · `config/shorts.ts` 주석 참조).
 *
 * ★ **운영자 비밀번호(`lib/admin-pw.ts` 의 `ADMIN_PW_MIN`)도 같은 값을 쓴다.**
 */
export const PW_MIN = 8;
